# Increment 001 — R-TUI-115 · `Seed the living canon: batch-87 mirror + pilot re-author refresh`

| Field | Value |
|---|---|
| Batch | `2026-08-24-batch-87` |
| Increment | `001` (the batch's only planned increment) |
| Lane (if the batch forked) | none — single lane |
| Requirement(s) | `R-TUI-115` (new) mirroring all thirteen batch-87 heading ids · `R-TUI-113` refreshed (pilot re-author) · `R-TUI-114` amended (parent boundary followed the split) |
| Acceptance | gate **G8** (live): per-id canon greps ≥ 1, one grep per id · V22 census shrink · `0 block` · source-scope pin |
| Agent | `software-dev` (supervised-incremental-development) |
| Date | `2026-08-24` |

---

## 1 · What changed

**The living canon now reflects batch-87, and the V22 seeding backlog shrank by exactly the
thirteen ids this batch declares — 289 → 276 of 544.** `REQUIREMENTS.md` is the AUTHORED plane
of the D-VII hybrid; the Atlas is the derived one, regenerated by command at the gate and never
hand-edited. Three edits, one authored file, exactly as §7 of the batch record planned:

1. **`R-TUI-113` (the batch-85 pilot row) refreshed from KNOWN-DEFECT to RE-AUTHORED.** Its
   Status bullet now records that `D-II` — the open question that row's previous status
   carried — was ruled *re-author* and discharged by this batch, that the contract is
   re-declared **in place** at §8 of the batch-85 record under `D-87-A` (decided by executed
   probe, not by reading), and that the former union output became **six** outputs:
   `panel_handle`, `slots_container`, `artifact_row_set`, `unload_all_row`, `artifact_slots`,
   `project_row`. The `What` bullet's section pointer moved from §5 to §8 for the same reason.
   **Its `Allocation` bullet is untouched**, as §7 required, and the row still says in as many
   words that the pilot's MEASUREMENTS stand as history — the re-author replaced the contract,
   not the record of what was measured. **Scoped after review F1**, which was right that the
   first version over-claimed: the headline now reads *contract* RE-AUTHORED and is followed by
   an enumeration of what batch-87 did **not** discharge — `HLR-85.2` still unsatisfiable as
   written (repo-wide Statement at the batch-85 record `:164` versus its own frozen-records
   threshold at `:180`, pilot defect #7 at handoff `:139`, whose named remedy is a ruling nobody
   has made), `R-1` still Open at that record's `:449`, and batch-85's CLOSED-UNFINISHED P5
   state at handoff `:33` as frozen fact. The trap worth naming: batch-87's §5.5 row 7 says that
   defect class is *"not reproduced"*, which is true **of batch-87's own text** and is not a
   repair of `HLR-85.2` — reading a disposition table as a discharge is how a defect gets
   erased from the canon while staying alive in the tree. Every line citation above was resolved
   by reading the cited line before it was written.
2. **`R-TUI-114` (surface #2) amended for the re-export change.** Measured first: the literal id
   of the child's former union output occurs **0 times** in `REQUIREMENTS.md` and always did, so
   there was no stale literal to delete — what the canon carried was the stale **arity** that
   literal produced. The amendment states the mechanism (balancing is transitive set
   containment, so the parent had to re-declare the split ids in the same batch — `D-87-D`,
   forced by a pre-fix BLOCK, not chosen), moves declared outputs **30 → 31** and the pilot
   re-export **five → six**, and marks the superseded figures in place rather than rewriting the
   batch-86 record's history. **Corrected after review F3:** the bullet's second figure, `106
   resolved consumers`, is superseded too — it is the **corpus-wide** V14 total, not surface
   #2's own, decomposed by batch-86 as 15 + 91 and re-measured by batch-87 as 251 = 18 + 94 +
   139. I re-derived that decomposition independently before writing it (§4, Gate 5) rather
   than copying the record's arithmetic.
3. **`R-TUI-115` seeded** as a new section immediately before `## Retired ids` (the same slot
   `R-TUI-114` took, and the anchor `tests/test_id_registry.py` G4 depends on — not moved, not
   renamed). It mirrors **all thirteen** batch-87 heading ids **one id per line**, each on its
   own sub-bullet, because per-id greps are the primary predicate and a packed line is what the
   M7 lesson was written about. The Status bullet carries the batch's two load-bearing results
   honestly: the re-export cost compounds with depth (surface #3 declares fewer own outputs than
   surface #2 and is still the corpus's most expensive record), and this component's own parent
   is not the root, so one V12 notice stands one level up by design.

**Count reconciliation, because §7 and the increment brief disagreed with the file.** §7's edit 3
says "eleven batch-87 heading ids" and the P3 brief said twelve; the record's own declaration is
**thirteen** — 2 US, 3 HLR, 8 LLR. Both smaller figures are traceable and neither is a defect of
the record: §7's *eleven* is the count of ids **P1 authoring added** (278 → 289), and the batch's
own scaffold had already put `US-87-1` and `US-87-2` into the corpus before P1 opened (P-12
refuted the 276 baseline for exactly this reason). Eleven added + two pre-existing = thirteen
declared, and the V22 census retires all thirteen because it does not care when an id arrived.
The measurement is in §4; the M7 lesson applied is that this discrepancy was found by counting
ids one at a time rather than trusting a stated total.

No `#`-prefixed literal, no `query(...)` string and no rail-screen class literal was written into
any of the three edits, so the edit adds **no new V13 reacher pair** — confirmed by the notice
count holding at 284 across the before/after runs.

---

## 2 · Files modified

**The budget counts SOURCE files only. Tests are not capped. Product docs and `.dev-flow/**` are outside the count.**

| File | Kind | Change |
|---|---|---|
| `REQUIREMENTS.md` | doc | three edits: `R-TUI-113` Status + `What` pointer refreshed · `R-TUI-114` gloss + amendment bullet · new `R-TUI-115` section inserted before `## Retired ids` |
| `.dev-flow/2026-08-24-batch-87/01-requirements.md` | doc (batch record) | G8 gate row flipped OPEN → GREEN with its citation |
| `.dev-flow/_derived/ATLAS-TRACE.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |
| `.dev-flow/_derived/ATLAS-ORPHANS.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |
| `.dev-flow/_derived/ATLAS-IFC.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |
| `.dev-flow/_derived/ATLAS-BATCHES.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |

| Count | Value |
|---|---|
| **SOURCE files** | **0 / 4** — no file under `s19_app/` touched; pin verified in §4 |
| Test files | 0 (uncapped) — tests untouched |
| Doc files | 6 (outside the count) — 2 authored, 4 derived-by-command |

**All four derived files moved, not the two a canon edit alone would explain.** `--atlas --write`
regenerates the set as a unit and V20 guards all four by digest, so the two extra files are the
generator's normal output over a changed corpus — which this increment changed twice, once in the
canon and once by adding this packet (`.dev-flow/**` is corpus input). None was hand-edited; the
derived plane is regenerated by command only.

- **Why this increment could not be cut smaller.** It is one doc edit against one authored file,
  and its three parts are not separable: the gate is a *census*, and a census that runs after a
  partial seeding measures a state the batch never intends to ship. Splitting edit 2 out is
  arithmetically impossible for a different reason — the parent's arity is stale only *because*
  edit 1 happened, so shipping edit 1 alone would leave the canon self-contradicting between two
  adjacent sections.

---

## 3 · How to test

```bash
cd "C:\Users\jjgh8\Github\s19_app"

# 1 — per-id grep loop: one grep per id, one id per line, every batch-87 heading id >= 1
for id in "US-87-1" "US-87-2" "HLR-87.1" "HLR-87.2" "HLR-87.3" \
          "LLR-87.1" "LLR-87.2" "LLR-87.3" "LLR-87.4" \
          "LLR-87.5" "LLR-87.6" "LLR-87.7" "LLR-87.8"; do
  printf "%-10s %s\n" "$id" "$(grep -cF "$id" REQUIREMENTS.md)"; done

# 2 — validator: V22 unreflected drops 289 -> 276, final line 0 block
#     (if V20 blocks on stale Atlas: regenerate with --atlas --write, then re-run)
python ~/.claude/docs/tools/devflow-validate.py .

# 3 — source-scope pin: the batch's source diff is empty
git diff --stat 943e54a -- s19_app/
git diff --stat 943e54a -- s19_app/ tests/ tools/ pyproject.toml

# 4 — the canon's real in-repo consumer, executed
python -m pytest tests/test_id_registry.py -q
```

---

## 4 · Test results

**One complete run each; every figure below is pasted from that run's own output, never stitched
and never inferred.**

| Layer | Nodes | Result |
|---|---|---|
| **0 · unit** | none — doc-only increment, no function authored | n/a |
| **A · white-box** | none — no TC in this increment's scope | n/a |
| **B · black-box** (gate G8, live) | 13-id grep loop · validator V22 census · source pin · registry guard | all four PASS (transcripts below) |

**Gate 1 — per-id grep loop, executed AFTER the edit (all 13 ≥ 1):**

```
US-87-1    2
US-87-2    1
HLR-87.1   2
HLR-87.2   1
HLR-87.3   1
LLR-87.1   2
LLR-87.2   1
LLR-87.3   1
LLR-87.4   1
LLR-87.5   3
LLR-87.6   1
LLR-87.7   1
LLR-87.8   1
```

Counts above 1 are accounted for, not waved at: `US-87-1` and `HLR-87.1` are named once in the
new section and once in the refreshed `R-TUI-113` Status bullet; `LLR-87.1` likewise, via that
bullet's `LLR-87.1` through `LLR-87.5` range citation; `LLR-87.5` reaches 3 because the
`R-TUI-114` amendment cites it as the requirement that forced the parent edit. Every count is
≥ 1, which is what G8 asserts.

**Gate 2 — validator, before and after, both measured on this tree.**

BEFORE the edit:

```
  [!] V22  REQUIREMENTS.md: 289 of 544 batch-declared ids are not reflected in the living canon (HLR 78 · LLR 201 · US 10; e.g. HLR-007a, LLR-001.6, US-87-1) — the seeding backlog, retired one tranche at a time
0 block · 284 notice · 15 not applicable
```

AFTER the edit (final run, on the shipped tree at this exact packet content, Atlas regenerated):

```
  [-] V20  .dev-flow/_derived/: atlas current (4 files, census 1)
  [!] V22  REQUIREMENTS.md: 276 of 544 batch-declared ids are not reflected in the living canon (HLR 75 · LLR 193 · US 8; e.g. HLR-007a, LLR-001.6, US-B63-1) — the seeding backlog, retired one tranche at a time
0 block · 284 notice · 15 not applicable
```

**Delta accounting, exact.** 289 − 276 = **13 retired**, and the family split reconciles term by
term with the ids seeded: HLR 78 → 75 (−3: `HLR-87.1`, `HLR-87.2`, `HLR-87.3`) · LLR 201 → 193
(−8: `LLR-87.1`, `LLR-87.2`, `LLR-87.3`, `LLR-87.4`, `LLR-87.5`, `LLR-87.6`, `LLR-87.7`,
`LLR-87.8`) · US 10 → 8 (−2: `US-87-1`, `US-87-2`). No id outside this batch was retired and no
id was added to the backlog, so the debt shrank by its whole seeding and nothing else moved. The
denominator held at 544, which is the check that the edit declared no new ids. The census's own
US sample corroborates it independently: the notice names the first missing id per family, and
that sample moved from `US-87-1` before the edit to `US-B63-1` after — `US-87-1` left the missing
set, which is the same fact the grep loop reports from the other direction.

**Against the record's own target.** §7 set CLOSE at **≤ 278** against the measured baseline of
278. The shipped figure is **276** — 2 under the floor, because two of the thirteen ids retired
here predate the 278 baseline. This is not a retirement of another batch's backlog: no id outside
batch-87 was seeded, and §7's framing that "a batch that seeds only its own ids ends level" holds
— the level in question is 276, batch-86's close, which is exactly where the corpus lands.

**Intermediate state, disclosed rather than smoothed.** The first post-edit run read `2 block ·
284 notice · 14 not applicable`: both V20 rows tripped on Atlas staleness, which is the expected
consequence of any canon edit, cleared by `--atlas --write` after this packet was written (the
batch-86 F1 lesson — `.dev-flow/**` is corpus input, so writing the packet re-stales the Atlas,
and regenerating before the packet exists only buys a second staleness).

**Gate 3 — source-scope pin.** `git diff --stat 943e54a -- s19_app/` produced empty output at
exit 0, as did the wider `git diff --stat 943e54a -- s19_app/ tests/ tools/ pyproject.toml`.
Zero source files, which is what a spec-only batch claims. The checkout also holds another
session's untracked WIP under `prototypes/` and `build/`; it was not read, written, or included
in any count here.

**Gate 4 — the canon's real in-repo consumer, executed:**

```
13 passed in 2.23s
```

`tests/test_id_registry.py` reads `REQUIREMENTS.md` and its G4 rule anchors on the `## Retired
ids` heading, under which a non-live id may be named. The new section was inserted **before**
that heading, which was neither moved nor renamed. Arguing that would have been cheap; running
it is the evidence.

**Gate 5 — the V14 decomposition, re-derived rather than cited (review F3).** The canon
amendment claims `106 → 251` and names the per-surface split. I did not take that from the
record: I loaded the validator's own `_ifc_corpus` against this tree and counted consumer
entries per component.

```
loaded_panel       2026-08-21-batch-85   outputs   6  consumer entries   18
screen_workspace   2026-08-24-batch-86   outputs  31  consumer entries   94
workspace_body     2026-08-24-batch-87   outputs  42  consumer entries  139
TOTAL consumer entries 251
```

251 = 18 + 94 + 139, matching the `V14  251 declared consumer(s), every one resolved` line of
the same run and the record's `:1426`. The 15 + 91 = 106 decomposition of the superseded figure
is batch-86's own, at its record `:198` (*"V14 resolves 106 consumers (15 + 91)"*) and `:169`
(*"V14 15 → 106"*), which is what establishes that the figure was corpus-wide and never surface
#2's alone.

### RED counterfactual — executed, not predicted

| Field | Value |
|---|---|
| Mutation applied | in the **sole** occurrence of `LLR-87.6` inside the new section's LLR list, the separator character between the batch number and the ordinal was replaced by an underscore. **The corrupted token is not reproduced anywhere in this packet** — `.dev-flow/**` is scanner corpus and the Atlas would adopt a spelled corruption as an id (C-56) |
| Where it ran | my own checkout at `C:\Users\jjgh8\Github\s19_app`, the tree this increment ships; no other lane reading it |
| Transcript | occurrence count of the target token before mutation: **1** · file length unchanged at 483,287 bytes (a one-for-one byte substitution) · per-id loop under mutation printed `LLR-87.6   0` with all twelve siblings unchanged · **the shipped validator went RED on the same mutation**: V22 read `277 of 544 … (HLR 75 · LLR 194 · US 8)`, i.e. the LLR family rose by exactly 1 |
| Restore proven by | sha256 returned to its pre-mutation value `ca1b6f7a4f0323aabd7f4e31520c25ab244ca939d781f0f83acc31d3410691cb`, and CRLF endings confirmed intact on the restored bytes. The edit and the restore were done through binary read/write for exactly this reason — batch-86 hit a file-wide CRLF→LF normalization on this file and had to catch it by hash, `git status` being blind to it |
| Bytecode cache | n/a — no Python module was touched or imported from a mutated state |
| Arms resolved at baseline | **13** — the loop prints exactly 13 rows and the rows are pasted in full, so a silently dropped arm is visible rather than inferred from a total |
| Verdict granularity | **per id**, one grep per id. Never a process exit code, and never one family-wide grep over the batch's LLR ids: such a pattern would have matched the range citation in the `R-TUI-113` bullet and reported the family present while a member was missing — the M7 defect this gate exists to prevent. **The pattern itself is described here rather than written**, because a family wildcard is an id-minting token (see the phantom-stem note in §6) |
| Arms that stayed GREEN | the other **twelve** — `US-87-1`, `US-87-2`, `HLR-87.1`, `HLR-87.2`, `HLR-87.3`, `LLR-87.1`, `LLR-87.2`, `LLR-87.3`, `LLR-87.4`, `LLR-87.5`, `LLR-87.7`, `LLR-87.8` — all held their pre-mutation counts, as they must, since the mutation targeted one occurrence of one id |

### Load-bearing emptiness — what is this resting on that is only true today? (C-55)

| Field | Value |
|---|---|
| Does any claim here rest on the tree holding NO instance of some case? | **Yes, twice.** (1) "the edit adds no new V13 reacher pair" rests on no `#`-literal, `query(...)` string or rail-screen class literal existing in the three edits. (2) "the canon never carried the child's former union output id" rests on that literal occurring 0 times in `REQUIREMENTS.md` |
| If the result is an ABSENCE, what made the search wide enough | (1) V13's own census over the **merged** corpus, not a local grep — it runs every validation and attributes each stray pair to a `(output_id, file)`. (2) a fixed-string grep over the whole canon, whose width is the whole file |
| Guard labelled as protecting a CONCLUSION, not a behaviour | V13 and V22 themselves — corpus rules that run on every validation. No new local guard was added, and none should be: the conclusion here is a property of the corpus, not of a function |
| Conjunctive criteria: one mutation per conjunct | G8 is a conjunction of id-presence and census-shrink. Both conjuncts were mutated by the **same** RED arm and both went RED independently — the loop printed 0 for one id, and V22's LLR family rose by 1. The source-scope pin's RED arm lives in the batch's own selftest, not re-mutated here |
| Synthetic instance of the absent case | not built for (1): the absent case is a literal an author would have to type, and V13 detects it on every future run rather than at authoring time. For (2) the synthetic instance already exists in the corpus — the batch-86 record still contains the union output id in its superseded prose, and V13/V22 read that file, so the probe was demonstrably capable of finding the literal where it lives |
| **Positive control for every probe that returned an ABSENCE** | (1) the same V13 run that reports no new pair from this edit reports **62** classified stray pairs for surface #3 and names `REQUIREMENTS.md` as a reacher for several pre-existing addresses — the probe finds pairs attributable to this very file where they exist, so its silence about the new section is a measurement, not a blind spot. (2) the same fixed-string grep returns non-zero counts for all thirteen seeded ids in the same file on the same run |

### Reverse census — trigger family B

| Probe | Command | Result |
|---|---|---|
| B1 symbols asserted by **other** tests | `grep -rl "R-TUI-115" tests/` | **did NOT fire** — 0 hits; the id is new and nothing in the suite cites it yet |
| B2 file moved on disk | no file moved or renamed by this increment | **did NOT fire** |
| B3 byte-identical golden captures this source | no golden in `tests/` captures `REQUIREMENTS.md` | **did NOT fire** |
| B4 artifact produced here is consumed elsewhere | **FIRED** — `REQUIREMENTS.md` is read by `devflow-validate` (V13/V20/V22) and by `tests/test_id_registry.py` (G4 anchors on `## Retired ids`) | both consumers were **executed**, not argued: the validator run above and `13 passed` from the registry guard |

| A3 | interface consumed by another module changed | no code interface touched; source pin empty | **did NOT fire** |

### Signed-balance test ledger

`post = base − deleted + added` → post = base − 0 + 0 ✓ reconciles — no test node added or
deleted; the suite is byte-identical to the pin ref per Gate 3.

---

## 5 · Risks

- **The `R-TUI-115` Status bullet freezes P2-era figures** (42 outputs · 139 consumer entries ·
  20 files · 62 pairs). If a later batch re-measures and they move, this one summary bullet must
  be re-pointed. The canonical text stays in the batch record by design, which is what bounds the
  drift to a bullet instead of a section.
- **The `R-TUI-114` amendment records an arity that a fourth surface will move again.** Every
  ancestor re-declares its descendants' ids, so the declared-output count of *every* row above a
  changed child is a maintenance liability. That is R-87-2's compounding, and it is registered
  there as a flow-side question (D-87-C) — this increment inherits it rather than fixing it.
- **~~One figure in the `R-TUI-114` Status bullet was deliberately NOT touched: `106 resolved
  consumers`.~~ WRONG, and corrected in this pass (review F3).** My first version called the 106
  "batch-86's own measurement on a different plane" and declared the reconciliation unrun. Both
  halves were false and a re-measurement showed it: the 106 is the **corpus-wide** V14 total,
  which batch-86 decomposed itself as 15 + 91 (its record `:198`), and batch-87 had **already**
  reconciled it at `:1426`. I re-derived it independently by parsing the live corpus through the
  validator's own `_ifc_corpus`, which returns 18 · 94 · 139 per component, totalling **251** —
  matching the current V14 line exactly. The canon now says the first TWO figures of that bullet
  are superseded, with the decomposition and both citations. **The lesson is the batch's own:** I
  reached for "different plane, not measured" instead of running the parse, which is the shape of
  every defect this batch spent P2 correcting.
- **The five false in-source arity claims about the shell are still in the tree** (R-87-3). This
  batch is spec-only and may not touch source; the canon now describes a shell whose own source
  comments contradict it in five places until the next batch that edits those files.

## 6 · Pending items / spec deviations

- **Deviation from §7's stated count, resolved by measurement.** §7 edit 3 says eleven ids; the
  record declares thirteen. Seeded thirteen, reconciled in §1 and §4. §7's sentence is left as
  written in the record — it is accurate about what P1 *added*, and rewriting a closed section to
  match a later count is the habit this batch spent P2 arguing against.
- **CARRY for the close's backlog reconciliation — the phantom bare-stem ids, measured wider
  than reported.** The Atlas id regex (`devflow-validate.py:1575`) reduces a backtick-quoted
  family wildcard to a bare stem with no allocation, and the Atlas then adopts that stem as an
  id row. Review F2 caught mine and put the pre-existing feeder count at 5; **a full census over
  `.dev-flow/**/*.md` plus the canon returns 8 authored feeders across 3 distinct phantom
  stems**, not 5 across 1. Enumerated: the bare batch-87 LLR stem is fed by the batch-85 record
  `:500` and by the batch-87 record at `:637`, `:1273`, `:1289` and `:1314` — **four sites in
  that record, not three** — plus mine, now removed, leaving **5 pre-existing**; the bare
  batch-86 HLR stem and the bare batch-86 LLR stem are each fed once, both from batch-86 record
  `:62`. All three stems have already been adopted into `ATLAS-TRACE.md` (`:1565`, `:2384`,
  `:2394`), which is the observable that makes this a real corpus defect rather than a
  typography quibble. Not fixed here: every feeder lives in a frozen record section this
  increment may not edit. **The general rule worth registering with it — the `X-NN` family
  wildcard mints a phantom every time it is written, so a family should be named in prose.**
- **§7's floor framing lands 2 under, not level.** Recorded in §4 rather than treated as a bonus:
  no id outside batch-87 was retired here, so the seeding backlog for the other 276 is untouched
  and remains the standing tranche-at-a-time obligation.

## 7 · Suggested next task

Phase 4 validation: execute the batch's kill-mutation plan from `01b-qa-validation-plan.md`
against the shipped record + canon, then close with the `05-close.md` figure table at n=3 —
`HLR-87.3` and `LLR-87.8` are the only requirements whose verification is still owed, and both
are close-record analysis rather than anything this increment could discharge.

---

## Increment gate checklist

| # | Item | ✓/⚠/✗ | Evidence (node id · command output · file:line) |
|---|---|---|---|
| 1 | ≤4 source files, or reason declared | ✓ | 0 source files; §2 table + Gate 3 pin (empty diff, exit 0) |
| 2 | Tests written in this same increment | ⚠ | none — doc-only increment; the live gate G8 (per-id loop + validator census + pin + registry guard) is the verification, all four executed in §4 |
| 3 | Layer 0 written where the criterion applies | ✓ | criterion does not apply — no function authored |
| 4 | RED counterfactual captured **and restored by hash** | ✓ | §4 RED table; sha256 `ca1b6f7a…` restored, CRLF intact; validator went RED independently (276 → 277) |
| 5 | Reverse census run on every touched symbol | ✓ | §4 B1–B4/A3 table; B4 fired and **both** its consumers were executed |
| 6 | `code-reviewer` passed — a HIGH blocks | ✓ | independent review RAN on this increment and returned BLOCK with 4 findings on the canon text (1 HIGH, 2 MED, 1 LOW); all four fixed in this pass and re-measured — see §1 and §6. A doc-only diff is reviewable and this review proves it |
| 7 | No file from another lane touched | ✓ | §2 table; the other session's untracked WIP under `prototypes/` and `build/` was neither read nor written |
| 8 | Frozen interfaces untouched (or returned to the trunk) | ✓ | source pin empty over `s19_app/ tests/ tools/ pyproject.toml` |
| 9 | Coverage claims verified **on disk**, not from intent | ✓ | every §4 figure pasted from a real run on this tree, including the BEFORE census measured pre-edit rather than reconstructed by arithmetic |
| 10 | Load-bearing emptiness declared, with its synthetic instance (C-55) | ✓ | §4 C-55 table — two absences, each with its width and its positive control |
| 11 | Mutation verdicts recorded **per arm**, inert arms named (C-40 rider) | ✓ | §4: per-id granularity, 13 arms resolved, the 12 expected-GREEN arms enumerated by id |

**An item without a citation is not satisfied — it is asserted.**
