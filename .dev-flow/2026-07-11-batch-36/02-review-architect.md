# 02 — Architect Cross-Review — batch-36 (US-058 / US-059 / US-060)

> Independent Phase-2 review. Reviewer did NOT author `01-requirements.md`. Every cited
> `file:line` re-verified against the worktree at `7df60dd`, 2026-07-11. Adversarial posture:
> claims in the ledger were re-run, not trusted.

**Verdict: BLOCKERS PRESENT → iterate.** One blocker (A-01: an acceptance predicate that is
mathematically unsatisfiable and self-contradictory with a surviving pin), two majors, five
minors. US-059 colour-constant claims and US-060 census/sizes are otherwise sound.

## Findings

| id | severity | story | summary | evidence (file:line) | suggested fold |
|----|----------|-------|---------|----------------------|----------------|
| A-01 | **BLOCKER** | US-058 | AT-058a's five-region **pairwise-disjoint** predicate is unsatisfiable by construction: `#patch_doc_controls` and `#patch_checks_controls` are **direct children** of `#patch_doc_file_row`, so their rectangles are contained within it and can never be disjoint from it. Two independent proofs: (1) geometry — parent always overlaps child; the QA overlap idiom (`test_tui_patch_layout.py:161-176`) counts containment as overlap; (2) test-conflict — the disjoint predicate demands non-nesting, but the **surviving** TC-319 demands nesting. No implementation can turn both green. | compose: `screens_directionb.py:1848-1923` (`#patch_doc_file_row`:1908 wraps `#patch_doc_controls`:1879 and `#patch_checks_controls`:1906); predicate: `01-requirements.md` §3 AT-058a :138-139 + LLR-058.2 :275-281; conflict: `tests/test_tui_patch_layout.py:416-422` | Redefine the disjoint set to **five sibling leaf containers**: introduce a dedicated change-file leaf group id (holding only the `#patch_doc_file_select` + `#patch_doc_path_input`) that is a sibling of the patch-script and checks groups, and compare THOSE five leaves — OR drop `#patch_doc_file_row` from the compared set and assert child⊂parent **containment** (not disjointness) for nested pairs. Reconcile with TC-319 (A-03). |
| A-02 | major | US-059 | Requirements' draft-time finding claims adding `"Hex"` breaks **THREE** `test_tui_legend.py` assertions, item (iii) being the modal-header equality `headers == list(LEGEND_TABLE)` (:322). **FALSE on re-verification.** Both consumers render `LEGEND_TABLE` dynamically, so a `"Hex"` header appears in BOTH `headers` and `list(LEGEND_TABLE)` → :322 still passes. Only **TWO** assertions break: :78 (artifact-set equality) and :70 (orphan-colour, for the new "Yellow"). The QA artifact (§6 row 3) correctly says test_tc023_1 SURVIVES — the two Phase-1 docs contradict. | claim: `01-requirements.md` LLR-059.3 acceptance :386-389; dynamic renderers: `screens.py:527-528` + `report_service.py:1318`; assertion: `tests/test_tui_legend.py:322`; contradicting QA row: `01b-...md` :219 | Correct the requirements to "**two** assertions break" (:70, :78). Do NOT modify :322 — it is a surviving single-source regression guard; leave it to re-run green. Align the count with the QA §6 census. |
| A-03 | major | US-058 | **Supersession-census gap.** TC-319 (`test_tc319_regroup_section_structure_census`) is absent from BOTH the requirements' LLR-058.3 census and the QA §6 supersession table (QA cites only `test_tui_patch_layout.py:79-215`; TC-319 lives at :351-438). It pins `#patch_doc_controls`/`#patch_checks_controls` as ordered children of `#patch_doc_file_row` and that `#patch_paste_row` survives — the exact structure any US-058 mechanism perturbs, and the pin that makes A-01 undeniable. | `tests/test_tui_patch_layout.py:351-438` (parentage asserts :416-422; id census incl. `patch_paste_row`/`patch_pane_changefile` :332-348); absent from `01-requirements.md` LLR-058.3 :286-303 and `01b-...md` §6 :215-229 | Add TC-319 to the supersession census with an explicit disposition (SURVIVES if paste-only reparent; EXTEND with a docstring note if file_row is restructured for A-01). Its disposition and A-01's fix must be co-designed. |
| A-04 | minor | US-059 | Anti-drift coupling under-specified. `MAC_ADDRESS_OVERLAY_STYLE = "bold orange3"` yields token `orange3`, but the existing legend convention writes `"Orange"`; `FOCUS_HIGHLIGHT_STYLE = "bold yellow"` → `yellow` vs display `"Yellow"`. LLR-059.3's "colour names **shall correspond to**" never defines the canonicalization, so TC-322's coupling risks being a fragile substring/case match or an inconsistent literal. QA §9.2 flags this; the requirements did not supply the mapping. | constants: `color_policy.py:13-14`; convention: `legend.py:60,88` ("Orange"); vague coupling: `01-requirements.md` LLR-059.3 :343-344, :381-382; QA flag: `01b-...md` :278-279 | Expose an explicit `hex_colour_name → style_constant` dict in `legend.py`; TC-322 asserts against it with a defined canonicalization (strip leading `bold `, case-fold). Pin whether the display name is `Orange` or `Orange3`. |
| A-05 | minor | US-058 | Pinned N (paste visible lines) = 6 @80x24 / 8 @120x30 is **below** the 11-line seed `DUMMY_CHANGESET_TEXT`; the editor height is fixed at 8 (`styles.tcss:950`), so the full seed can never be shown without internal scroll. QA's suggested floor was N ≥ seed line count. The story value ("read/edit a multi-line change-set") is only partially met. | seed = 11 lines (`s19_app/tui/changes/io.py::DUMMY_CHANGESET_TEXT`, imported `screens_directionb.py:63`); height cap `styles.tcss:949-951`; N pin `01-requirements.md` AT-058a :134-136; QA floor `01b-...md` :115-117 | Reconcile: either explicitly accept internal scroll (state the editor scrolls to reach lines 9-11), or raise `#patch_paste_text` height toward 11 — LLR-058.1 :270 already permits "retained or increased." |
| A-06 | minor | US-059 | AT node-granularity mismatch. Requirements fold both surfaces into a single AT-059a (C-18, incl. the C-12 report-file reread); QA §2.3 lists AT-059a (modal) + **AT-059b** (report reread) as two nodes in different files. §5 traceability then drops AT-059b entirely. Reconciliation was deferred to the Phase-1 gate but never closed in §5. | `01-requirements.md` §3 AT-059a :167-184 + §5 :503,515 (no AT-059b); QA two-node map `01b-...md` :90-96 :49-51 | Pick one accounting and make §5 consistent. If one node, keep AT-059a's stated dual-surface deliverable (modal Labels + generated-and-reread report file) — it is already C-12-correct. |
| A-07 | minor | US-058 | Snapshot cell-id provisional mismatch: LLR-058.4 names `patch-80x24` / `patch-comfortable-120x30` (self-flagged `assumed`), while QA §4 names `patch-comfortable-80x24` / `patch-comfortable-120x30` (cited from `test_tui_snapshot.py:504,519`). Not a false claim (flagged ✗ open), but the precise ids already exist in the QA artifact. | `01-requirements.md` LLR-058.4 :311, checklist :604-605; QA `01b-...md` §4 :159-160 | Adopt QA's `patch-comfortable-80x24` / `patch-comfortable-120x30` in Phase 3; close the ✗ checklist item. |
| A-08 | minor | US-060 | I-060-1 (duplication equivalence) is an **un-discharged** precondition to an irreversible delete, correctly gated by QA §5 but only PROVENANCE-addressed in requirements LLR-060.2 (DF-2), not STRUCTURALLY. The coverage-preservation claim assumes the 54 MB A2L is a pure SCALE duplicate of the 36 MB one — unverified. | `01-requirements.md` LLR-060.2 :437-446 (provenance only); QA gate `01b-...md` §5 :180-186, §9.1 :269-273 | Add I-060-1 as an explicit LLR-060.2 precondition. Phase-3 method: parse both `case_06/firmware.a2l` with `parse_a2l_file`; assert the 54 MB copy's construct-kind set (MEASUREMENT/CHARACTERISTIC/AXIS_PTS/COMPU_METHOD/RECORD_LAYOUT + nesting depth) ⊆ the 36 MB's; record the census as evidence BEFORE `git rm`. If a kind is present only in the 54 MB copy → BLOCK the delete. |

## Re-verified spec claims (adversarial)

**TRUE (held up):**
- `FOCUS_HIGHLIGHT_STYLE = "bold yellow"`, `MAC_ADDRESS_OVERLAY_STYLE = "bold orange3"` — `color_policy.py:13-14`. ✓
- Hex cells apply exactly those two overlay styles and no third colour; `> ` focus marker is unstyled — `hexview.py:397-433`, marker `:406` (`text.append("> ")`, no `style=`). ✓
- `"orange3"/"Orange"` already a `COLOUR_SEVERITY` key → WARNING (spec's disambiguation caveat is real) — `legend.py:106`. ✓
- Both legend consumers iterate `LEGEND_TABLE` dynamically → one added block reaches both surfaces; modal tolerates unknown colour via `COLOUR_SEVERITY.get(...) → ""` — `screens.py:527-535`, `report_service.py:1318-1321`. ✓
- C-13 budget arithmetic sound: 2-col ⇒ ~35/pane @80, button cell ≈11 ≥ 8 ("Validate" fits); 3-col reflow ⇒ ~23/pane, cell ≈7 < 8 → clips → reject. `styles.tcss:695-708, 902-908`, `#patch_paste_text{height:8}` :949-951. ✓
- US-060 census COMPLETE: `stress_smoke` has **0** consumer refs (only batch-36 dev-flow docs) ✓; `professional_validation`/`case_06` observers all caught (`docs/architecture.md:152`, `test_examples_smoke.py:44`+docstring, `test_tui_snapshot.py:690` guard unaffected, `MANIFEST.md:21` refers to the retained 36 MB, pilot-gif discovery branch `:56` survives). Sizes exact: 37,742,888 B / 56,046,631 B; `tmp/stress_smoke` = 3 git-tracked files, `tmp/` not gitignored; `examples/` = 96 M. ✓
- Normative keywords: `shall` only in HLR/LLR statements; no modal `should` in any normative statement (`should` appears only in informative prose :64 and meta :112). ✓

**FALSE / inaccurate on re-verification:**
- **A-02:** LLR-059.3 :386-389 — adding `"Hex"` does NOT break `test_tui_legend.py:322`; it breaks two assertions (:70, :78), not three. The `:322` header-equality survives because both sides are dynamic.
- **A-01:** AT-058a / LLR-058.2 — the "five region rectangles pairwise disjoint" outcome is unachievable with the named ids (`#patch_doc_file_row` vs its own descendants) and contradicts the surviving TC-319.

## Evidence checklist (review dimensions)

- ✓ **Completeness / derivation** — 3 US → 3 HLR → 11 LLR; every HLR→one US, every LLR→parent; §5 tables consistent EXCEPT the dropped AT-059b (A-06). US-058 chain is internally contradictory at the AT layer (A-01/A-03).
- ✓ **Normative keyword** — no modal `should` in any HLR/LLR statement; grep-verified (`01-requirements.md` :64/:112/:584 are non-normative).
- ✗ **Two-layer blockers** — (a) every story has a black-box AT ✓; (b) output-producing reqs name deliverable+observation ✓; (c) both chains present but §5.1/§5.2 omit AT-059b (A-06); (d) **AT-058a is NOT genuinely realizable** — its predicate is unsatisfiable and references nested containers (A-01). Blocks the gate.
- ✗ **C-15 symbol-identity (US-059)** — constant NAMES and VALUES verified TRUE (`color_policy.py:13-14`); hex cells apply exactly two styles (`hexview.py:397-433`); BUT the "breaks exactly 3 assertions" claim is FALSE — breaks 2 (A-02); coupling canonicalization under-specified (A-04).
- ✓ **C-13 geometry (US-058)** — 3-col rejection arithmetic sound and re-verified; the "dedicated paste region" (rung-1 grid-size 2 4) IS realizable without touching a handler and survives the `_assert_two_by_two` oracle (it inspects only the four `#patch_pane_*` and tolerates extra cells) — but the disjoint-region ACCEPTANCE is defective (A-01).
- ✓ **C-14 location-move census (US-060)** — independently grepped; the spec's observer set is COMPLETE, `stress_smoke` 0-refs claim TRUE, `SLOW_CASE_IDS` edit + docstring counts are the smoke-file edits (one stale comment `test_examples_smoke.py:41-43` also worth cleaning — trivial).
- ✓ **I-060-1 gate** — correctly identified as a hard precondition by QA; requirements under-carry it (structural vs provenance) — A-08 states the Phase-3 verification method to run before the delete.
