# Requirements — s19_app — 2026-08-24-batch-88 · rule-integrity closures + RC-2 verifiable backup

> **Station state.** Authored at P1 to the batch-87 exemplar
> (`.dev-flow/2026-08-24-batch-87/01-requirements.md`) under the execute-first discipline:
> **every figure in this document was produced by a command this session ran, and the command's
> output is in §2.8.** Where a figure here disagrees with what the command prints, the command is
> right. Three figures inherited from `PLAN.md` and from the prior analysis agent's `full.diff`
> were re-executed and **two came back different** — see P-18 and P-21.

> **Id discipline (C-56), applied to this document's own text.** No proposal-style label of the
> form `US-` plus a bare letter appears anywhere below; the operator's stories are named **Story A
> / Story B / Story C / Story E**, exactly as `PLAN.md` §2 names them. No range notation appears:
> every id is written out singly, because the Atlas requirement tokenizer harvests a two-dot range
> as one id (`PLAN.md`, seventh flow finding — 97 such phantoms measured). **Two probes in §2.8
> operate on bare short acceptance ids and on a three-digit story id; neither token is reproduced
> in this document.** The probe is described by position and operation instead, because writing the
> token here would mint it into the census the probe is about.

---

## 0 · SCOPE RULING — asked for first, answered first

**Verdict: NOT authorable as scoped. This is two batches. The proposed scope passes every budget
the flow states out loud and fails the one it does not state — and the unstated one is the one that
has cost this project money twice this week.**

### 0.1 The counts, so the verdict is arguable rather than assertable

**Authored source files the proposed scope touches — 6.** (The `~/.claude/skills/dev-flow/**`
copies are a **build output** regenerated by `--sync-bundle`, not authored surface: measured, the
sync command derives them from the manifest table, and `PLAN.md` §3 B2 already measured both
`close-template.md` copies byte-identical at `sha256 3e8f07ab9d63fb55`. They are counted as
derived, not as source.)

| # | Authored source file | Which pieces of the proposed scope land in it |
|---|---|---|
| 1 | `~/.claude/docs/tools/devflow-validate.py` | V24 · V25 · Story B's guard rule · V22 oracle · V22 message · V23 grammar · V5 · V2 · V6 · `_artifacts` · V8 casing · V8 root message — **12 of the 14 pieces** |
| 2 | `~/.claude/templates/dev-flow/phase-checklists.md` | Story C's INTAKE row · Story B's CLOSE row · Story A's VALIDATION row |
| 3 | `~/.claude/templates/dev-flow/close-template.md` | Story B — assurance limits + the review-debt ledger |
| 4 | `~/.claude/templates/dev-flow/validation-template.md` | Story A — the Layer C table |
| 5 | `~/.claude/docs/FLOW-VERSION.md` | the rev47 bump and re-hash |
| 6 | `C:/Users/jjgh8/Github/s19_app/pyproject.toml` | the imaging dependency |

**Increments the proposed scope requires — 10**, decomposed so that no increment mixes two
oracle-moving changes: V22 · `_artifacts` · V5+V2+V6 · V23+V8×2 · Pillow · V25+checklist ·
V24+validation-template+checklist · Story B template · Story B guard rule+checklist · rev bump.
**Highest SOURCE count in any one of them: 3** (Story A's increment: the validator, the validation
template, the checklist).

### 0.2 Against the flow's own stated constraints, it passes — and that is the problem

| Stated constraint | Measured against the proposed scope | Verdict |
|---|---|---|
| **≤4 SOURCE files per increment** (`v9_source_budget`, `devflow-validate.py:262` — 4 is a notice, above 4 must be declared) | max **3**, one file of headroom | **PASS** |
| **Trigger family E fires at ≥3 stories or ≥3 increments** | 3 stories, 10 increments — fires hard | **FIRES**, and it already fired at P0 on 4 stories / 5+ increments (`PLAN.md` §3). Firing E is a **tax**, not a bar: it makes estimation and schedule obligatory (`PLAN.md` §4). Already paid. |

**So by the letter, the scope is authorable. The letter is measuring the wrong thing.** A file count
cannot see that **`devflow-validate.py` is edited by 8 of the 10 increments**. `PLAN.md` §7's
parallelisation proof — `modules(1) ∩ modules(2) = {}` — is true and is about the *lanes*; inside
Lane 1 there is no parallelism whatsoever. Lane 1 is a **single-file queue of eight increments on a
3,176-line file**, and the DDR joins two lanes of which one is empty once Story E leaves.

### 0.3 The binding number the budget cannot see: **five oracle moves, and what re-execution did to them**

> **⚠ RE-EXECUTED 2026-08-25** per `PDR-2026-08-25-batch-88#D1`. The station-4 review found this
> table's derivation refuted: two of its four figures had expired, a **fifth move existed and was
> missing**, and no row carried the date it was read. Every row below is re-derived on 2026-08-25;
> **superseded figures are struck beside the corrections, never removed.** The conclusion the scope
> cut rests on **survives** — its derivation does not, so cite the rows, not the memory of them.

The operator has already ruled that **one** moving oracle forces a story out of the batch, because
*"the number moves under the story that measures it."* That ruling is right. The question this
table answers is how many oracles are **still moving today**, after a station of work.

| # | Change | Oracle it moves | Measured pre → post (re-derived 2026-08-25) | Still moving? | Sequenced? |
|---|---|---|---|---|---|
| 1 | V22 substring → token | the canon census | **302 → 306 of 570**, delta **+4** — `HLR-053`, `HLR-056`, `US-064`, `US-068`. ~~276 → 280 of 544~~ superseded; the denominator grew because batch-88 declared its own ids | ✅ **yes** | ✅ operator-ruled: lands first |
| 2 | `_artifacts` F-7 residue | **the batch's own gate line** | **0 block → 0 block.** ~~14 block → 0 block~~ — **the window CLOSED when P1 authored.** Executed: the loader's `01-requirements.md` is now byte-identical to the **active** batch-88 copy and **not** to `.dev-flow/2026-05-05-batch-01/`'s May copy | ❌ **no — stilled by the batch's own progress** | n/a — nothing left to sequence |
| 3 | V2 substring → token | every acceptance id's resolution | **0 live verdicts here.** V2's pattern requires a digit immediately after the prefix; this project's acceptance ids are letter-initial, so V2 SKIPs (P-7). Corpus under `tests/` re-derived: **915** distinct acceptance/test-case tokens. ~~989~~ — **not reproducible without its tokenizer definition**, so it is retired rather than carried forward | ❌ **no — synthetic by construction** | n/a |
| 4 | V5 · V8 root message | the SKIP/NOTICE **sentences** 57 and 1 records rely on | **57 of 61 confirmed.** The 4 that parse are batches **09, 10, 11, 74**. **36** of the 57 mention a ledger in prose while V5 reports none was found (keyword scan for `ledger` / `post =`; the definition is stated here because the prior figure's was not) | ✅ **yes** | ❌ **not sequenced** |
| 5 | **V23 shipped → strict grammar** — **THE MISSING FIFTH MOVE** | the V23 notice count, and every design-review citation's resolution | **9 notices → 0** — but **the grammar moves none of it.** All 9 were self-inflicted by this document's constructed-token table, and de-minting alone cleared them. Post-de-minting corpus = **26 citations, 26 / 26 conforming under BOTH the shipped and the strict grammar** | ❌ **no — stilled by the de-minting in `#D2`** | ✅ **sequenced: lands after the de-minting**, which is already done |

**What re-execution changed about the ruling.** At P0, four oracles were moving and one was
sequenced. Today **two are moving** — V22's census and V5's sentences — and the fifth move, once
found, turned out to be **already still**. Two were stilled not by design but by ordinary work:
move 2 by the batch reaching P1, move 5 by fixing a hygiene defect in this very file. **The scope
cut stands on the two that remain**, and it stands more narrowly than when it was argued — worth
saying out loud, because a conclusion that survives a refuted derivation is easy to keep citing
for the wrong reason.

**Move 2 was the sharpest, and it is self-referential — ⚠ its figures have since expired; the
paragraph is kept for its reasoning, not its numbers (see row 2 above).** At P0 the loader handed
V1–V9 a document from May: all **14** live BLOCKs were V4 findings naming ids declared in
`.dev-flow/2026-05-05-batch-01/01-requirements.md` (M-2). The moment the loader is fixed, V1–V9
begin judging **this document** — and V6's statement-scope fix and V2's token fix are in the same
batch, so **the first document ever judged by the corrected rules is the requirements record of the
batch that corrects them.** That is not a defect. It is a coupling that makes *"0 BLOCK
attributable to this batch"* unmeasurable against a stable rule set at any point in the batch.

### 0.4 And Story B is outside the sealed design record — this half is not a judgement call

`PDR-2026-08-24-batch-88#D1` through `#D8` are sealed with verdict `approved with conditions`
(`PLAN.md` §8). What that record reviewed for Story B was *"declared limits of assurance … the
close carries the section with its **three fixed lines**"* (`PLAN.md` §2). What the operator has
since required is a **five-field review-debt ledger** (`reviewed-by` · `authored-by` · date · phase
· reserved `reviewer-id`) **plus a guard rule**. Measured against the declared scope:

- The scope allocates **V24** and **V25**. Story B's guard is a **third new rule with no allocated
  number** — see D-88-1 in §6.2. Phase 1 cannot mint a rule id the operator has not allocated.
- `PLAN.md` §8 already declares the PDR reviewed *"the design proposal, not the authored
  requirements"* and says in terms that **it does not discharge the station-4 PDR**. Authoring a
  requirement for a schema three times the size of the one the sealed record saw is not a Phase-1
  refinement; it is new design entering through the requirements door.

### 0.5 The cut

**batch-88 (this document) — the nine rule-integrity fixes + Story C.**
Rationale, in the PLAN's own words: *"Story C is lowest-risk and goes first deliberately — it
debuts the new-rule pattern on the smallest surface"* (`PLAN.md` §2). And it is the **only** new
rule in the proposed scope whose oracle is **external to the validator**: V25 reads `git ls-remote`
and a committer date, so its GREEN sentence is not measured through any number this batch moves.
Measured this session: `git ls-remote --exit-code --heads origin` → **exit 0, 9 refs**; newest
remote-tracking commit age **0.003 days** (M-9).

| | proposed | **ruled** |
|---|---|---|
| Authored source files | 6 | **4** |
| Increments | 10 | **7** |
| Max SOURCE per increment | 3 | **2** |
| Increments touching `devflow-validate.py` | 8 of 10 | **5 of 7** |
| New rules | 3 (one unnumbered) | **1** |
| Unsequenced oracle moves | 3 | **0** — every move gets its own increment and its own recorded re-baseline |

**batch-89 — Story A (V24) + Story B (review-debt ledger + its guard rule) + Story E (canon
seeding), on a still oracle.** Story A carries a hard dependency that lands here and nowhere else:
`_read_ci` (LLR-88.10). Story B carries a PDR obligation. Story E was already deferred by the
operator and now re-baselines against **280**, measured, rather than against a number that moved
under it.

### 0.6 Which half of the cut is firm, and what would change it

- **Cutting Story B is firm.** It is outside the sealed PDR and needs a rule number the operator
  has not allocated. Nothing measurable changes that; only an operator decision does (D-88-1).
- **Cutting Story A is a recommendation, and here is the price of overturning it.** Keeping A costs
  **+3 increments** (10 → 7 becomes 10), **+2 source files** (`validation-template.md`, and a
  second edit region in `phase-checklists.md`), and **puts V24's debut in the same batch as four
  oracle moves.** What would change my recommendation: if the operator wants Story A in 88, the
  minimal safe form is to land it as the **last** increment, after every fix's re-baseline is
  recorded, and to re-stamp `docs/architecture.md` §11's freeze declaration — which currently reads
  *"frozen for `2026-08-24-batch-88`"* on all three interfaces (M-8) and would otherwise leave V24
  with **zero real consumers**, which is exactly the synthetic-fixture shape acceptance criterion
  #1 forbids. That re-stamp is itself a decision (D-88-2).
- **What would change the whole ruling:** if the operator declares the four oracle moves acceptable
  because this batch's own acceptance is scoped *per increment* rather than per batch — i.e. each
  increment re-baselines and the batch never claims one stable number — then the oracle argument
  weakens and the file-count argument alone would let all three stories through. I do not recommend
  it: `PLAN.md` §12 and the V5 finding are both instances of this project discovering, late, that a
  number it had been reporting for months meant something other than it said.

---

## 1 · Context

Third batch under the living canon, second under flow rev46. **One objective, stated honestly:
the validator's rules stop saying things their behaviour cannot back.**

`PLAN.md` and the P0/ARQ stations found **seven** defects in the flow's own rules, and all seven
share one shape — *a rule whose behaviour does not match what its description promises*. The
largest, V5, has been reporting `SKIP: "no ledger expression found"` for **57 of 61** batches while
**38** of those records' prose mentions a reconciling ledger (M-5). A file with a correct ledger
and a file containing only a comment print the identical sentence — executed, both directions, M-5.

This batch closes nine of them and debuts one new rule on the smallest available surface. It does
**not** introduce the two larger new rules; see §0.

**Zero product code planned.** The diff touches the shipped application in **0** files; the only
`s19_app` edit is `pyproject.toml`, and it is a **dev-extra** declaration that does not enter the
runtime footprint (LLR-88.12).

---

## 2.6 · Story intake & refinement (Definition of Ready)

### US-88-1 — every rule reports a sentence its behaviour can back

**As** the flow operator standing at a gate, **I want** every rule the validator prints to mean what
its sentence says — a rule that could not check saying so in different words from a rule that
checked and passed — **so that** a green or skipped line is evidence rather than a shape, and a
batch's *"0 block attributable to this document"* is a claim about this document rather than about
whichever document `os.walk` reached first.

- **Context of use.** **User:** the flow operator (single operator, expert in this repo, reading
  the validator's 300-line output at a station gate). **Task:** deciding whether a station may
  close. **Environment:** Windows terminal, `~/.claude/docs/tools/devflow-validate.py` run from the
  repo root, no CI (measured: **0** validator references in `.github/workflows/`, **0** live git
  hooks — M-7). **Observable outcome:** the gate line stops counting another batch's findings, the
  census names the population it counted, and no rule prints a sentence that is true of two
  different states.
- **INVEST.** **I** — independent of US-88-2 in authoring order; they share one file and are
  sequenced, not coupled. **N** — the operator has ruled the V22 half and the V23 half explicitly.
  **V** — 14 of the gate's 14 BLOCKs are currently attributable to a loader defect (M-2), and 57 of
  61 validation records were verified by a rule that found nothing to verify (M-5). **E** —
  bounded: 9 named defects, 6 of them with a measured patch already executed to green (M-1). **S** —
  5 increments, max 1 source file each. **T** — every half is observable through the validator's
  own printed output.
- **Evaluability (T), behavioral.** *When the validator is run at a station where the active batch
  has authored its requirements record, the operator observes a gate line whose findings all name
  ids declared in the active batch's own documents* — the observable that is FALSE today, measured
  at **14 findings naming a May 2026 document** (M-2).
- **Open questions:** the imaging dependency's tier (resolved at P1 by the project's own stated
  convention — see P-18 and D-88-3); Story B's guard rule id (**not resolved** — D-88-1, and it is
  why Story B is not in this batch).
- **DoR: READY.**

### US-88-2 — the batch records verifiable backup before it derives anything

**As** the single operator of this repository — who carries *more* backup exposure than a team,
not less, because no colleague's clone is a second copy — **I want** the INTAKE station to record
that the remote is reachable and recently written, beside RC-1, **so that** *"the work is backed
up"* stops being an assumption and becomes a line with a command behind it.

- **Context of use.** **User:** the same single operator. **Task:** opening a batch at INTAKE,
  immediately after recording RC-1. **Environment:** repo root, network available, `origin` is
  `https://github.com/jav201/s19_app.git` (measured, M-9). **Observable outcome:** the INTAKE
  checklist shows an **RC-2** row with executed evidence, and the validator prints a V25 line
  naming the measured age.
- **INVEST.** **I** — its oracle is `git`, external to every number this batch moves; that is the
  reason it is the story kept (§0.5). **N** — operator-declared, ASPICE SUP.8.BP8. **V** — closes
  the audit gap and debuts the new-rule pattern on the smallest surface. **E** — one rule, two
  checks, one checklist row. **S** — 1 increment, 2 source files. **T** — observable in the
  validator's output and in the checklist's evidence column.
- **Evaluability (T), behavioral.** *When the validator runs in a repository whose remote is
  reachable and last written within 30 days, the operator observes a V25 line stating the measured
  age; when the remote is unreachable, and again when it is stale, the operator observes two
  different sentences, neither of which is the pass sentence.*
- **Open questions:** none. Threshold is operator-declared at 30 days.
- **DoR: READY.**

### Batch premise (both stories)

**0 files under `s19_app/`, `tests/` or `tools/`.** The only `s19_app` edit is `pyproject.toml`
(dev extra). The flow repo (`~/.claude`) is the primary surface. Canon-mirror seeding
(`REQUIREMENTS.md`) is **out of scope** — Story E is deferred to batch-89 and re-baselines at
**280**, measured (M-4), not at the 276 the objective still declares (D-88-4).

### P1 corrections to `PLAN.md`, recorded rather than rewritten

| `PLAN.md` claim | P1 verdict | Where |
|---|---|---|
| §12: *"Read the loader (`devflow-validate.py:2236`)"* | **Off by 22 lines.** `def _artifacts` is at **`devflow-validate.py:2214`**; :2236 is inside the docstring | P-21 |
| Nine fixes are backed by the measured patch in `full.diff` | **Partly FALSE.** `full.diff` covers **6** of the 9 (V22 oracle, V22 message, V23, `_artifacts`, V8 casing, V8 root message). **V5, V2, V6 and the dependency are NOT in it** and carry **0** of the 29 measured arms | P-23, P-24 |
| §2 / `state.json`: the imaging dependency is undeclared | **FALSE as framed.** It is declared — as an ad-hoc `pip install pillow` at `.github/workflows/tui-ci.yml:42`, with a comment already ruling it *"dev tooling, not a runtime dep"*. The defect is the **home**, not the absence | P-18 |
| `state.json` `batch_objective`: V24 keys on *"ARCHITECTURE.md section 4"* | **FALSE — superseded** by the ARQ decision: key on the `Frozen?` column header, never a section number. The objective string still carries the refuted wording | P-19 |
| The V23 fix in `full.diff` is the fix to adopt | **REJECTED by the operator, and the rejection is measurable.** The diff's grammar accepts a token whose batch segment carries extra hyphens (`…-batch-88-audit-closures`; prefix elided per the C-56 note above M-3's constructed-token table), conflating a version with a hyphenated batch dir | P-11 |

---

## 2.7 · Premise evaluation (C-43) — one row per premise

> Every row's evidence is a command this session RAN or a `file:line` this session READ. A premise
> whose only support is `PLAN.md`, `full.diff` or the prior analysis agent's transcript is
> **UNDECIDABLE, not TRUE** — each was re-executed. **Tiering:** `axiom` = already validated and
> verified, re-openable on an executed counterexample; `hypothesis` = anything this batch or a
> prior *design* record introduces (written down is not verified); `premise` = a claim about the
> world, executed against disk.

| # | Premise, as a truth-apt proposition | Tier | Verdict | Executed evidence | Disposition |
|---|---|---|---|---|---|
| **P-1** | The shipped selftest passes at **192** arms, exit 0 | premise | ✅ TRUE | M-1: `devflow-validate.py --selftest` → `SELFTEST PASSED`, exit 0; arm lines counted **192** | the floor LLR-88.4 raises from |
| **P-2** | The gate line at this station is `14 block · 284 notice · 14 not applicable`, and **0** blocks are attributable to batch-88's content | premise | ✅ TRUE **and it expired while this document was being written** | M-2: full run tail `14 block · 284 notice · 14 not applicable`; all 14 are `[x] V4` findings, every one naming an `LLR` id declared in `.dev-flow/2026-05-05-batch-01/01-requirements.md`. M-10, same command after this file existed: **`4 block · 293 notice · 13 n/a`**, **0** naming that document | AT-B88-01's RED arm, on the live tree — **and its live positive control** (M-10). HLR-88.2's threshold is restated over the absent-artifact state, because that is the state the authoring act does not reach |
| **P-3** | `_artifacts` prefills every basename first-wins across all batch dirs, then overrides only basenames the active batch already holds | premise | ✅ TRUE | M-2: `devflow-validate.py:2214-2246` read — `art.setdefault(...)` over `os.walk(.dev-flow)`, then `art[fn] = ...` over `_active_batch_dir` only | LLR-88.5's positive authoring constraint |
| **P-4** | The measured patch takes the selftest to **221** arms and s19_app to **0 block**, and moves the census **276 → 280** | hypothesis | ✅ TRUE | M-4: `patched.py --selftest` → `SELFTEST PASSED`, **221** arm lines, exit 0. `patched.py .` → `0 block · 284 notice · 16 not applicable`; V22 line reads `280 of 544` where the shipped rule reads `276 of 544` | the re-baselines LLR-88.8 records |
| **P-5** | V5 reports *"no ledger expression found"* for **57 of 61** validation records, and prints the **identical sentence** for a correct ledger and for a file with none | premise | ✅ TRUE | M-5: 61 files named `04-validation.md` swept; the rule's regex parses **4** (batches 09, 10, 11, 74) and fails **57**. `v5_ledger` executed on batch-87's real record → `SKIP: no ledger expression found`; on `"# nada\n"` → **byte-identical output** | LLR-88.1; the largest instance of the family this batch closes |
| **P-6** | V2 tests **substring** membership against one concatenated `tests/` text, so a short acceptance id resolves because a longer one sharing its prefix exists | premise | ✅ TRUE | M-6: `v2_at_without_node` read at `devflow-validate.py:230-238` (`i not in tests` over `_tree_text`). Executed: a bare **one-digit** acceptance id → `substring_in_tests=True`, `token_in_tests=False`; **989** distinct acceptance/test tokens on disk | LLR-88.2 |
| **P-7** | V2's fix has **no live instance in this repository** — the rule SKIPs here | premise | ✅ TRUE | M-2: `[-] V2 01-requirements.md: no AT ids declared`. Cause read: V2's pattern requires digits immediately after the prefix, and this project's acceptance ids are letter-initial | **LLR-88.2's arms are necessarily synthetic, and the acceptance says so.** A criterion claiming a live pre-state here would be unfalsifiable |
| **P-8** | V6 requires the statement marker and the modal on the **same physical line**, so a wrapped statement **false-passes** | premise | ✅ TRUE | M-6: `v6_modal_in_statement` executed on two synthetic records differing only in a line wrap — same line → `BLOCK`; modal on the continuation line → **no finding at all** | LLR-88.3 |
| **P-9** | The live design-review citation corpus is **21 citations / 8 distinct tokens**, and every one carries a decision suffix | premise | ✅ TRUE WHEN MEASURED · ❌ **REFUTED BY LATER EXECUTION — the superseded figure is kept beside the correction, not overwritten** | M-3: V23's own harvest reproduced (walk + `_V23_TOKEN` + the rule's `rstrip`) → 21 citations, 8 distinct, every one a decision token. **Re-executed 2026-08-25, same harvest: 34 citations · 16 distinct · 9 NON-conforming.** The +13 is **this document**: the constructed-token table in M-3 minted 9 of them against its own record. The cause is a C-56 hygiene failure, not the grammar — see `PDR-2026-08-25-batch-88#D2` | the regression population LLR-88.9 must preserve, **re-stated over the measured corpus rather than the expired one** |
| **P-10** | The **strict** grammar preserves all 21 live citations while expressing a record, a version and a decision distinctly | hypothesis | ✅ TRUE | M-3: strict pattern → **21/21 conforming**, identical to the shipped rule's 21/21; and it accepts the bare record, the `.md` filename, `-v2`, and `-v2#D1`, each of which the shipped rule **rejects** | LLR-88.9 |
| **P-11** | The grammar proposed in `full.diff` conflates a **version** with a **hyphenated batch dir** | hypothesis | ❌ **FALSE as a fix — the permissive form is defective** | M-3: the diff's pattern matches `…-batch-88-audit-closures` and `…-audit-closures#D1` (prefixes elided per the C-56 note above M-3's table) → **accepted**. The strict form → **rejected**. Both forms accept all 21 live citations, so the live corpus **cannot** discriminate them | **BLOCKS the diff's V23 half.** Corrected in LLR-88.9 to the strict form; the discriminating arm is mandatory because the live corpus is blind to the difference |
| **P-12** | The map the validator opens (`docs/ARCHITECTURE.md`, `devflow-validate.py:245`) is tracked in git as `docs/architecture.md` | premise | ✅ TRUE | M-7: `git ls-files \| grep -i architecture` → `docs/architecture.md` (lowercase), and `devflow-validate.py:245` reads `os.path.join(root, "docs", "ARCHITECTURE.md")` | LLR-88.10 |
| **P-13** | The consequence is **latent, not active**: no CI job and no git hook runs the validator | premise | ✅ TRUE | M-7: `grep -r "devflow-validate" .github/workflows/` → **0**; non-`.sample` entries in `.git/hooks/` → **0**; workflow files are `snapshot-regen.yml` and `tui-ci.yml` | **confirms `PLAN.md`'s own self-correction.** The fix is specified at its true severity — latent — and its arm asserts `_resolve_ci`'s return value, which is platform-independent |
| **P-14** | A repository-root file is unclassifiable by the staleness check by construction | premise | ✅ TRUE | M-7: `devflow-validate.py:251-259` — prefixes are matched as `f.startswith(p + "/")`, and `setup.py` has no directory segment. Live: `[!] V8 setup.py: under no declared module — the map is stale, ARQ fires` | LLR-88.11 |
| **P-15** | Neither `V24` nor `V25` nor any "Layer C" text exists in the flow repo today | premise | ✅ TRUE | M-7: `grep -c '"V24"'` → **0**; `grep -c '"V25"'` → **0**; `grep -rl "Layer C"` over `skills/dev-flow/` and `docs/tools/` → **no files** | V25 is genuinely new; AT-B88-06's RED arm is the absence |
| **P-16** | `phase-checklists.md` has **no** RC-2 row, and RC-1 is INTAKE row 4 of 8 | premise | ✅ TRUE | M-7: `grep -c "RC-2"` → **0**; line 29 reads `\| 4 \| RC-1: origin/main tip fetched and recorded in PLAN.md before deriving \|`; the INTAKE table holds rows 1–8 | LLR-88.15 places RC-2 as **row 5**, shifting nothing |
| **P-17** | The selftest's synthetic-exemption list contains exactly **one** rule, V8 | premise | ✅ TRUE | M-1, selftest tail: *"V8 is the ONLY rule this selftest does not prove synthetically … the exemption list shrank by measurement rather than by assertion"* | **the reason acceptance criterion #1 is insufficient alone** — and V25 must not join a one-member list |
| **P-18** | The imaging dependency is **undeclared** | premise | ❌ **FALSE** | M-7: `.github/workflows/tui-ci.yml:38-43` — `python -m pip install pillow`, preceded by the comment *"Test-only dependency: tests/test_examples_pilot_gifs.py imports PIL to render the example GIFs (dev tooling, not a runtime dep)"*. `pyproject.toml` declares it in neither `[project].dependencies` nor the `dev` extra; `requirements.txt` holds only `rich` and `textual`; PIL 12.2.0 is installed locally | **the defect is the HOME, not the absence.** LLR-88.12 declares it in the `dev` extra — the tier the file's own comment already assigns — and **leaves the workflow line alone**, because `tui-ci.yml:43` installs `pip install -e .` **without** `[dev]`, so removing the ad-hoc line would break CI's push job. That removal is D-88-3 |
| **P-19** | `state.json` `batch_objective` correctly describes the rule Story A will write | hypothesis | ❌ **FALSE** | M-8: the objective string reads *"every interface marked Frozen? in ARCHITECTURE.md **section 4**"*; `docs/architecture.md:226` puts the `Frozen?` table at **§11**, and §242 of that file instructs *"key it on this table's shape … never on a section number"* | Story A defers to batch-89 (§0.5); **the objective string still carries the refuted wording** and is corrected as part of D-88-4 |
| **P-20** | The remote is reachable and recently written | premise | ✅ TRUE | M-9: `git ls-remote --exit-code --heads origin` → **exit 0, 9 refs**; newest remote-tracking commit age **0.003 days** | US-88-2's GREEN pre-state; the RED arms are synthetic (LLR-88.14) |
| **P-21** | `_artifacts` is at `devflow-validate.py:2236` | premise | ❌ **FALSE** | M-2: `grep -n "def _artifacts"` → **2214**. Line 2236 falls inside the docstring | a citation correction, not a scope change; `PLAN.md` §12's mechanism is otherwise confirmed line by line |
| **P-22** | `full.diff` covers all nine declared fixes | hypothesis | ❌ **FALSE** | M-1: the diff contains hunks for V8 casing, V8 root message, V22 message, V22 oracle, V23 grammar and `_artifacts` — **6**. It contains **no** hunk for V5, V2, V6 or `pyproject.toml` | **4 of the 9 are UNMEASURED input.** LLR-88.1, LLR-88.2, LLR-88.3 and LLR-88.12 carry their own pre-state probes (M-5, M-6, M-7) rather than inheriting the diff's evidence |
| **P-23** | The 29 measured arms cover the new rule and the four unmeasured fixes | hypothesis | ❌ **FALSE** | M-1: the 29 arms decompose as V8 casing **5**, V8 root **4**, `_artifacts` **4**, V22 **4**, V23 **12**. Arms for V5, V2, V6 and V25: **0** | LLR-88.4 states the arm-count floor as an **enumeration per rule**, never as a total — a total is satisfiable by adding arms to a rule that already had them |
| **P-24** | The three declared Part B interfaces are frozen for **this** batch, giving a Layer C rule real consumers | premise | ✅ TRUE **and it expires** | M-8: `docs/architecture.md:226-236` — three rows, `Frozen?` = *"✅ frozen for `2026-08-24-batch-88`"* on all three | **the freeze names batch-88 by string.** If Story A lands in 89, the declaration must be re-stamped or V24 has zero real consumers — D-88-2 |
| **P-25** | Story B's guard rule has an allocated number | hypothesis | ❓ **UNDECIDABLE** | The declared scope allocates `V24` and `V25` and nine fixes; no third number appears in `PLAN.md`, in `state.json`, or in the operator's scope line. `grep -c '"V26"'` over the validator → **0** | **BLOCKS Story B.** Dispositioned: **Story B is out of scope for batch-88** (§0.4) and the number is owed by the operator — D-88-1 |
| **P-26** | Letter-initial acceptance ids of the form used here are ungoverned by the id registry, so no reservation is owed | premise | ✅ TRUE | M-7: over `AT-TC-REGISTRY.jsonl` (1,373 lines), occurrences of the batch-85, batch-86, batch-87 and batch-88 letter-initial prefixes are **0, 0, 0, 0**; `"next_free": {"AT": 282, "TC": 613}` — untouched by three consecutive batches | precedent consistent; this document uses the same form, and P-7 records the cost (V2 cannot see them) rather than hiding it |

**Verdict counts: 19 TRUE · 6 FALSE · 1 UNDECIDABLE.**

> ⚠ **This line was wrong on its first writing and is corrected beside the error, not over it.** The
> first draft summed *"20 TRUE · 5 FALSE"* — it counted P-21 (the loader's line citation, refuted at
> `2214` against the claimed `2236`) among the TRUE rows while its own cell reads ❌. Caught by
> tallying the table's verdict column mechanically instead of by re-reading it. **A summary that
> disagrees with the rows it summarises is the same family as every defect this batch closes**, and
> it appeared in the document arguing that family, which is worth recording rather than tidying.

**Gate rule.** ❓ P-25 **blocks Story B** and is dispositioned in writing: deferred to batch-89,
number owed by the operator (D-88-1). Of the six ❌: **P-11 blocks the V23 half of `full.diff`**
and is corrected in LLR-88.9 with a mandatory discriminating arm; **P-18 relocates** the dependency
fix and forbids a workflow edit; **P-22 and P-23 withdraw** `full.diff` as sufficient evidence for
four of the nine fixes and force each to carry its own probe; **P-19** leaves the declared objective
asserting a refuted anchor and is handed to the operator as D-88-4; **P-21** is a citation
correction, carried in the §2.6 corrections table. No ❌ is left standing without a landing place.

---

## 2.8 · Probe transcripts (C-39 / C-56)

> Every figure below was produced by a command run this session against the live tree at
> `C:\Users\jjgh8\Github\s19_app` and the live flow repo at `C:\Users\jjgh8\.claude`. Working files
> are under `C:\Users\jjgh8\.claude\jobs\6650e29d\tmp\`. **No probe wrote to either repo.**
> **Two probes operate on tokens this document deliberately does not reproduce** (M-6): writing the
> token would add an occurrence to the very census the probe measures.

### M-1 — the selftest floor, the measured patch, and what the patch does NOT contain

- `python ~/.claude/docs/tools/devflow-validate.py --selftest` → `SELFTEST PASSED`, **exit 0**,
  **192** arm result lines.
- `python <tmp>/patched.py --selftest` → `SELFTEST PASSED`, **exit 0**, **221** arm result lines.
  **Delta +29**, matching the prior agent's claim.
- Arm attribution, counted from the diff's own print statements: V8 casing **5** · V8 root **4** ·
  `_artifacts` **4** · V22 **4** · V23 **12** = **29**. Arms for **V5, V2, V6: 0**. Arms for a new
  RC-2 rule: **0**.
- Hunks present in `full.diff`: `_resolve_ci`/`_read_ci` + `v8_module_map` · the V22 notice string ·
  the V22 `canon_ids` token set · `_V23_BATCH`/`_V23_OK` · `_artifacts` · the five selftest blocks.
  **Absent:** any change to `v5_ledger`, `v2_at_without_node`, `v6_modal_in_statement`, or
  `pyproject.toml`.
- Selftest tail, verbatim in substance: *"V8 is the ONLY rule this selftest does not prove
  synthetically — it needs a project map on disk … the exemption list shrank by measurement rather
  than by assertion. Said out loud rather than counted as covered."*

### M-2 — the live gate line, and the loader that produces it

- `python ~/.claude/docs/tools/devflow-validate.py .` → tail
  **`14 block · 284 notice · 14 not applicable`**, exit 1.
- All **14** BLOCK lines are `[x] V4 01-requirements.md · ### LLR-…: test/analysis label without
  executed verification`. Every id named is declared in
  `.dev-flow/2026-05-05-batch-01/01-requirements.md` — the directory `os.walk` reaches first.
- `grep -n "def _artifacts"` → **2214** (P-21 correction). Body read at `:2236-2246`:
  `art.setdefault(fn, …)` over the whole `.dev-flow` walk, **then** `art[fn] = …` over
  `_active_batch_dir(root)` only. When the active batch holds no file of that basename, the
  first-wins value survives — the window `PLAN.md` §12 describes, confirmed line by line.
- `[-] V5 04-validation.md: no ledger expression found` and
  `[-] V2 01-requirements.md: no AT ids declared` both appear in the same run — both are the SKIP
  branch, and both are indistinguishable from the state in which the rule genuinely had nothing to
  say.

### M-3 — the design-review grammar, three patterns over one corpus

Harvest reproduced exactly as `v23_design_review_citations` does it (`REQUIREMENTS.md` plus a
sorted walk of `.dev-flow/` excluding `_derived/` and `__pycache__`, `_V23_TOKEN.finditer`, then the
rule's own `rstrip(".,;:)]}` + quotes)`):

| corpus as harvested at P0 (2026-08-24) | citations | distinct | conforming |
|---|---|---|---|
| shipped grammar | 21 | 8 | **21 / 21** |
| `full.diff`'s permissive grammar | 21 | 8 | **21 / 21** |
| the **strict** grammar specified in LLR-88.9 | 21 | 8 | **21 / 21** |

**⚠ Re-executed 2026-08-25 — the figures above expired, and THIS DOCUMENT is why.** The same
harvest returned **34 citations · 16 distinct · 9 NON-conforming**. All nine were minted by the
constructed-token table below, which pasted literal non-conforming tokens into a file V23's own
walk reads. The de-minting is recorded as `PDR-2026-08-25-batch-88#D2`; the superseded figures
stay above rather than being overwritten.

| corpus after de-minting (2026-08-25, re-executed) | citations | distinct | conforming |
|---|---|---|---|
| shipped grammar | 26 | 10 | **26 / 26** |
| the **strict** grammar specified in LLR-88.9 | 26 | 10 | **26 / 26** |

**The live corpus cannot tell the candidate grammars apart — and after de-minting it cannot tell
them apart at ZERO notices.** The discrimination is visible only on constructed tokens, which is
exactly why the table below must **describe** them rather than paste them:

> **C-56 elision, applied at every site in the table below.** This file lives under
> `.dev-flow/**/*.md`, so V23's walk reads it and any literal design-review token written here
> becomes a live citation judged by the very rule under specification. Tokens below are written
> with the prefix elided as `…`; the elided text is the record prefix and the date `2026-08-24`
> in every row. Pasting them cost this document **9 self-inflicted notices** before the elision
> was applied — the same discipline `01b-qa-validation-plan.md` already applies to itself at its
> lines 15–19.

| token | shipped | permissive (`full.diff`) | **strict (specified)** |
|---|---|---|---|
| a decision inside a record — `…-batch-88#D3` | ✅ | ✅ | ✅ |
| a `DDR` decision — `DDR-2026-08-21-batch-85#D1` | ✅ | ✅ | ✅ |
| an alphanumeric batch segment — `…-batch-n8#D1` | ✅ | ✅ | ✅ |
| **the record itself** — `…-batch-88`, no decision suffix | ❌ | ✅ | ✅ |
| **the record's filename** — `…-batch-88.md` | ❌ | ✅ | ✅ |
| **a VERSION of the record** — `…-batch-88-v2` | ❌ | ✅ | ✅ |
| **a decision inside a version** — `…-batch-88-v2#D1` | ❌ | ✅ | ✅ |
| **a hyphenated batch DIR** — `…-batch-88-audit-closures` | ❌ | **✅ — the defect** | **❌ — correct** |
| the same, with a decision — `…-audit-closures#D1` | ❌ | **✅ — the defect** | **❌ — correct** |
| no date — the prefix followed straight by `85#D3` | ❌ | ❌ | ❌ |
| a template placeholder | ❌ | ❌ | ❌ |
| a bare hash — `…-batch-88#D` | ❌ | ❌ | ❌ |
| a path-prefixed filename | ❌ | ❌ | ❌ |

### M-4 — the patched validator over the real repository

`python <tmp>/patched.py .` → tail **`0 block · 284 notice · 16 not applicable`**, exit 0.

| Line | shipped | patched |
|---|---|---|
| gate | `14 block · 284 notice · 14 n/a` | **`0 block · 284 notice · 16 n/a`** |
| V22 canon census | `276 of 544 batch-declared ids …` | **`280 of 544 ids DECLARED AS HEADINGS in the .dev-flow/**/01-requirements.md corpus …`** with the `ATLAS-ORPHANS.md` scope sentence appended |
| V8 root file | `setup.py: under no declared module — the map is stale, ARQ fires` | `setup.py: at the repository root — no path/** prefix can match a file with no directory, so this is the rule unable to EXPRESS this file, not a stale map` |
| V5 | `04-validation.md: no ledger expression found` | `-: no 04-validation.md` — **the loader fix alone changes which of V5's two no-op sentences prints, and neither is a pass** |
| V23 | `21 design-review citation(s), every one conforming` | unchanged, `21` |

**Notice count is net zero (284 → 284)** across the whole patch: the V8 sentence changed without
changing the count, exactly as ARQ item 2 predicted.

### M-5 — the ledger sweep, over the whole history

- Files named `04-validation.md` under `.dev-flow/`: **61**.
- The rule's regex `(\d+)\s*=\s*(\d+)\s*[-−]\s*(\d+)\s*\+\s*(\d+)` parses a ledger in **4** —
  `2026-06-11-batch-09`, `2026-06-13-batch-10`, `2026-06-14-batch-11`, `2026-07-31-batch-74`.
- It reports *"no ledger expression found"* in **57**.
- Files whose prose contains the word "ledger": **38**.
- `v5_ledger` executed directly on `.dev-flow/2026-08-24-batch-87/04-validation.md` →
  `('SKIP', 'no ledger expression found')`. On a body of `"# nada\n"` →
  `('SKIP', 'no ledger expression found')`. **Byte-identical.**

### M-6 — the two false-pass mechanisms, executed

- **Acceptance-id resolution.** `_tree_text("tests")` concatenated; **989** distinct acceptance/test
  tokens recovered by the truncation-guarded tokenizer already present in the same file at
  `devflow-validate.py:1574`. For a **bare one-digit** acceptance id whose two-digit extension
  exists on disk: `substring_in_tests = True`, `token_in_tests = False`. *(Token not reproduced —
  writing it here would register it as a declared id.)*
- **Wrapped statement.** `v6_modal_in_statement` run on two synthetic requirement bodies identical
  except for one line break. Marker and modal on one line → `[('BLOCK', 'modal should/debería
  inside a requirement st…')]`. Modal on the continuation line → **`[]`** — no finding of any
  severity.

### M-7 — the structural facts, each read at its line

| Probe | Output |
|---|---|
| `grep -n "def v5_ledger"` | **148** |
| `grep -n "def _artifacts"` | **2214** |
| `grep -n "ARCHITECTURE.md"` | **245** (the open), 247, 250, 259, 260 |
| `grep -c '"V24"'` / `grep -c '"V25"'` | **0** / **0** |
| `grep -rl "Layer C"` over `skills/dev-flow/`, `docs/tools/` | *(no files)* |
| `grep -c "RC-2"` in `phase-checklists.md` | **0**; RC-1 is INTAKE row 4, table rows 1–8 |
| `grep -ci "independence\|assurance limit"` in `close-template.md` | **0** |
| `git ls-files \| grep -i architecture` | includes **`docs/architecture.md`** — lowercase |
| `grep -r "devflow-validate" .github/workflows/` | **0** hits across both workflow files |
| non-`.sample` entries in `.git/hooks/` | **0** |
| `pyproject.toml` grep for `pillow`/`PIL` | **0** hits; `dev` extra holds `pytest-textual-snapshot==1.1.0`, `textual==8.2.8` |
| `.github/workflows/tui-ci.yml:38-43` | `pip install -r requirements.txt` · `pip install pytest` · comment *"Test-only dependency … dev tooling, not a runtime dep"* · **`pip install pillow`** · `pip install -e .` **(no `[dev]`)** |
| `grep -n "importorskip\|skipif"` in `tests/test_examples_pilot_gifs.py` | **0** — the PIL import is inside a function, the test is `@pytest.mark.slow`, and CI's **push** job runs the full `pytest -q` |
| `AT-TC-REGISTRY.jsonl` | 1,373 lines; occurrences of the batch-85/86/87/88 letter-initial prefixes = **0/0/0/0**; `"next_free": {"AT": 282, "TC": 613}` |

### M-8 — the interface table, read at its own lines

`docs/architecture.md` carries **§10 Composition (by path)** at line 184 and **§11 Interfaces** at
line 216. The table header is at **line 226**: `| Interface | Owner module | Consumers | Shape |
Frozen? |`. Three rows, `Frozen?` = *"✅ frozen for `2026-08-24-batch-88`"* on all three. Line 242
of the same file instructs: *"Key it on **this table's shape** — the column header `Frozen?` — and
never on a section number."* The `state.json` `batch_objective` string still reads *"section 4"*.

### M-9 — RC-2 pre-state

- `git remote -v` → `origin  https://github.com/jav201/s19_app.git` (fetch and push).
- `git ls-remote --exit-code --heads origin` → **exit 0**, **9** refs listed.
- Newest remote-tracking commit (`git for-each-ref --sort=-committerdate refs/remotes/origin`) →
  **0.003 days** old. `origin/main` tip `4131a384…` dated `2026-08-24 21:26:37 -0600`, identical to
  the RC-1 value `PLAN.md` §1 recorded.
- **This is the GREEN side only.** Both RED sides — unreachable remote, and a push older than the
  threshold — have **no live instance** and their arms are synthetic by necessity (LLR-88.14).

### M-10 — the discriminating mutation, executed by AUTHORING this file

**The act of writing this document was itself the mutation `PLAN.md` §12 described, and the gate
moved exactly as the mechanism predicts.** Same command, same tree, minutes apart, the only change
being that `.dev-flow/2026-08-24-batch-88/` now holds a file named `01-requirements.md`:

| State of the active batch directory | `devflow-validate.py .` |
|---|---|
| `PLAN.md` only (M-2, before this file existed) | **`14 block · 284 notice · 14 n/a`**, all 14 naming a May 2026 document |
| this file present (now) | **`4 block · 293 notice · 13 n/a`**, **0** naming a May 2026 document |

The 4 surviving BLOCKs are all derived-Atlas drift — this document's ids are new to the corpus —
and are discharged by the regeneration every batch performs. Three further lines are worth pasting,
because each confirms a premise this document asserts about itself:

- `[-] V4 01-requirements.md: every test/analysis LLR carries both fields` — **this record's own
  requirements pass the executed-verification-and-threshold rule**, measured rather than claimed.
- `[-] V2 01-requirements.md: no AT ids declared` — **exactly as P-7 predicted.** The rule cannot
  see this document's letter-initial acceptance ids, which is why LLR-88.2's arms are synthetic.
- The modal rule prints **0** findings over this document, which is the pre-condition LLR-88.3's
  scope-note claimed.

**What this measurement is worth.** It is the live positive control for AT-B88-01, taken on the
real tree rather than on a fixture, and it was obtained by doing the batch's ordinary work rather
than by constructing a case. It also bounds the loader fix's remaining value honestly: **10 of the
14 blocks disappear the moment any batch authors its record**, and the loader fix's real subject is
the **window** before that happens — which every batch passes through, and in which the gate is
judging someone else's document.

### M-11 — the citation harvester, defeated by ordinary Markdown

**Found by tripping it.** While authoring §0.3 this session wrote a **conforming** citation as
``**… per `<cite>`.**`` and the run returned a V23 NOTICE against it. The token was valid; the
prose around it was not survivable. Probed with the harvester reproduced exactly — `_V23_TOKEN`'s
`\S+` plus the rule's own `rstrip(".,;:)]}` + quotes")` — over one conforming citation wrapped in
twelve ordinary Markdown contexts:

| Markdown context | harvested token | verdict |
|---|---|---|
| bare, code span, code span + period, parenthesised | the citation, clean | ✅ ok (4 of 12) |
| **bold, citation inside** — ``**`<cite>`**`` | citation + `` ` `` + `**` | ❌ **NOTICE** |
| **bold sentence, citation at the end** | citation + `` ` `` + `.` + `**` | ❌ **NOTICE** |
| italic · strikethrough · trailing `?` · trailing `!` · trailing `*` | citation + `` ` `` + the delimiter | ❌ **NOTICE** |
| table cell with no space before the pipe | citation + `\|` | ❌ **NOTICE** |

**8 of 12 ordinary Markdown contexts turn a conforming citation into a notice.**

**⚠ The mechanism is not "the strip set is missing `*`".** `str.rstrip` consumes from the right and
**halts at the first character not in its set**, so a single unlisted character **shields every
listed character behind it**. That is why the backtick survives in every failing row above — the
backtick **is** in the shipped set, and is reached only after the `*` that stops the scan.
Widening the set by one character fixes one context and leaves the shape intact.

**Why this is expensive here, specifically.** These records are written in emphasis-heavy prose,
and ``**`<cite>`**`` is the single most common way this project writes a sealed-record citation.
Worse, the strikethrough row collides head-on with the discipline this very batch adopted —
*superseded figures are struck beside the correction, never removed* — so **striking an
outdated citation mints a notice against the record for obeying the rule.**

**The defect is in the HARVESTER, not the grammar**, which is why no grammar repair reaches it:
the shipped, the permissive and the strict grammars all reject the same mangled string, because
they never see the clean one. Entered into `LLR-88.9` by operator ruling 2026-08-25.

**Recurrence, measured rather than predicted: this session tripped it THREE times, every one
while documenting it.** Once in §0.3 — which is how it was found; again in the `PLAN.md`
supersession banner written *after* this probe existed; and a third time in the very table row
that CLOSES the ruling on a different defect. All three are the same shape — a bold sentence
ending in a citation — the shape catalogued in the table above. **The count is the evidence:
three trips, by an author who had already written the catalogue, inside the document that
specifies the repair.** **A defect that catches the author who is
writing its own specification is not avoided by care.** That is the same argument the handoff
makes about the filename trap, and it is not a coincidence:

**⚠ The filename trap and the emphasis trap are ONE defect wearing two faces.** The prior
session hit the filename form three times — writing a sealed record's path raises a notice,
because the harvester swallows the path and the grammar then demands a decision suffix a
filename cannot carry. Both faces have the same root: **`_V23_TOKEN` matches `\S+`, so it grabs
everything up to the next space and the `rstrip` is the ONLY thing narrowing it back.** Every
authoring convention that puts a non-space character against a citation — emphasis, a path
separator, a table pipe, terminal punctuation — is a fresh instance. Fixing the grammar alone
leaves the family intact, which is why `LLR-88.9` specifies the harvester's discard set as a
character CLASS and not as a longer list of delimiters.

### M-12 — the dependency topology, executed before re-authoring against the ruling

The operator ruled 2026-08-25 for a **new `evidence` extra with CI's install line routed through
it**, against `LLR-88.12`'s `dev`-extra specification. Re-authoring on that ruling requires
knowing which install line, in which job — read today rather than assumed:

| Surface | What it declares | Read at |
|---|---|---|
| `pyproject.toml` `[project].dependencies` | **3** entries: `rich>=13.0`, `textual>=8.0.2`, `markdown-it-py>=3` | manifest |
| `pyproject.toml` `dev` extra | **2** entries: `pytest-textual-snapshot==1.1.0`, `textual==8.2.8` | manifest |
| **`requirements.txt`** — **a THIRD surface no record has named** | `rich>=13.0` and a **floorless** `textual`; **omits `markdown-it-py` entirely** | repo root |

**And `tui-ci.yml` has TWO install blocks, not one:**

| Job | Install line | Test step | Needs the imaging library? |
|---|---|---|---|
| **`tui-ci`** (`:24`) | `:43` — `pip install -e .`, **no extra**, preceded by the ad-hoc `pip install pillow` at `:42` | `:51` — `pytest -q` on push, **the full suite, slow included** | **✅ yes — this is the job** |
| `snapshot` (`:60`) | `:76` — `pip install -e ".[dev]"` | `:79` — `tests/test_tui_snapshot.py -m snapshot` only | ❌ no |

**The measurement confirms the ruling rather than merely obeying it.** The `dev` extra is already
consumed by the two snapshot jobs, which *want* `textual==8.2.8` because SVG baselines are
renderer-sensitive. The `tui-ci` job installs bare `-e .` **on purpose**, so routing it through
`dev` to obtain the imaging library would pin the renderer under the **2735** tests `pytest -q`
collects — contradicting `pyproject.toml`'s own comment at `:38-43`, which scopes the pin to
*"dev / snapshot env only; runtime stays >=8.0.2"*. A third extra is the only option that
declares the dependency without moving the runtime.

**⚠ Backlog, not this batch:** `requirements.txt` is a third declaration surface that disagrees
with the manifest in two ways (no `textual` floor, no `markdown-it-py`). CI installs it *before*
`pip install -e .`, so the manifest wins by ordering and nothing is currently broken — which is
exactly the shape of a defect that waits. Recorded as **R-88-6**; **not** repaired here, because
it is a fourth surface beyond the ruling's scope.

---

## 3 · Acceptance (black-box) — the user-verified outcomes

> Layer B. The "user" is the flow operator standing at a gate; the shipped surface is
> `devflow-validate.py` and the INTAKE checklist, read as the operator reads them (trigger B4: the
> acceptances run the REAL consumer over the REAL records, never a hand-fed copy). `AT-B88-*` ids
> are letter-initial ⇒ ungoverned by the registry (P-26), provisional-until-Phase-3 per V-5.
> **No new pytest files**; these acceptances are operator-local validator runs plus the shipped
> `--selftest`, and saying so is the honest form.

### AT-B88-01 — the gate stops counting a document from May

- **Observable outcome:** at a station where the active batch has authored `01-requirements.md`,
  the operator observes a gate line in which **0** findings name an id declared under
  `.dev-flow/2026-05-05-batch-01/`; and in the state where the active batch has **not** authored
  it, the operator observes a V4 line reading `no 01-requirements.md` — **not** a BLOCK over a
  foreign batch's content.
- **Shipped surface:** `python ~/.claude/docs/tools/devflow-validate.py .`
- **Deliverable + observation:** the post-state run pasted into `04-validation.md`, compared
  against M-2's pre-state.
- **Negative control, in the same probe:** a synthetic tree whose **declared** batch holds the
  defective record still BLOCKs (`OWN-still-judged`). A fix that made V4 stop blocking on
  *everything* would satisfy a count threshold and fail this control.
- **⚠ Live positive control, obtained by authoring this file (M-10).** `14 block → 4 block` on the
  same tree, minutes apart, with **0** surviving findings naming a May 2026 document. This bounds
  the requirement honestly: **10 of the 14 blocks are cleared by any batch authoring its record**,
  so the loader fix's real subject is the **window before that happens** — and the threshold in
  HLR-88.2 is therefore stated over the *absent-artifact* state, which is the only state the
  authoring act does not reach.
- **Boundary catalog:** ☑ empty — M-2's live `14 block` over a May document is the RED arm ·
  ☑ boundary — the two states (active batch with the file / without it) are both exercised, and
  the discriminator is the **key's presence in the artifact map**, not a count · ☑ invalid — a
  tree with no `state.json` must keep the walk fallback, else every single-batch project loses its
  rules · ☑ error — an unreadable batch dir resolves to the fallback, never to a crash.

### AT-B88-02 — the census re-baselines, and names the population it counted

- **Observable outcome:** the V22 canon line reports **280** where it reported **276**, and its
  sentence names the population — heading declarations in the `01-requirements.md` corpus — and
  distinguishes it from the larger mention-population the orphan census counts.
- **Shipped surface:** the same validator run.
- **Deliverable + observation:** `state.json` `batch_objective` and `PLAN.md` both restate the
  baseline as **280** *before* any canon-seeding tranche exists on disk (LLR-88.8). The observation
  that the re-baseline happened **first** is a git-order observation: the commit carrying the
  re-baseline precedes any commit touching `REQUIREMENTS.md`.
- **Boundary catalog:** ☑ empty — M-4 measured both numbers on the same tree · ☑ boundary — the
  discriminating id class is a story id present in the canon **only inside a letter-suffixed
  variant of itself**; substring says reflected, token says missing · ☑ invalid — an id that is
  genuinely absent must stay missing under both oracles (the `PLAIN-missing` direction) · ☑ error —
  a canon file that cannot be read yields the rule's existing SKIP, unchanged.

### AT-B88-03 — a rule that could not check no longer sounds like a rule that checked

- **Observable outcome:** for a validation record containing a reconciling ledger the operator
  observes a sentence **stating the reconciliation**; for a record containing none, a **different**
  sentence saying so. The two are not interchangeable.
- **Shipped surface:** the validator run, plus `--selftest`.
- **Deliverable + observation:** the two sentences pasted side by side in `04-validation.md`,
  against M-5's pre-state in which they are byte-identical.
- **Boundary catalog:** ☑ empty — M-5's byte-identical pair is the RED arm, taken on a **real**
  record (batch-87's) and not a fixture · ☑ boundary — the form the project actually writes
  (a word on the left of the equals sign) and the form the rule already parsed (digits both sides)
  are both exercised, and the 4 historically-parsing records must **still** parse · ☑ invalid — a
  ledger that does not reconcile must still BLOCK · ☑ error — a record with two ledger expressions
  yields one finding per expression, not one per file.

### AT-B88-04 — the grammar can say record, version and decision, and refuses to say batch-dir

- **Observable outcome, in two halves that carry unequal weight** (restated per
  `PDR-2026-08-25-batch-88#D2`; the prior phrasing *"all 21 live citations still conform"* was
  both **stale** and **unfalsifiable**).
  - **(a) Regression floor — it cannot fail, and is labelled so rather than counted as evidence.**
    The **non-conforming set is ∅**, and stays ∅. Measured 2026-08-25 after the C-56 de-minting:
    **26 citations · 10 distinct · 26/26 conforming**, under the shipped **and** the strict grammar
    alike. **The threshold is the SET, not the count** — this document mints a conforming citation
    every time it cites a design record, so any fixed population figure expires by the act of
    writing it. `26` is therefore recorded as a **floor with its measurement date**, never as a
    target to hit.
  - **(b) The load-bearing half — synthetic, because nothing else can be.** The grammar accepts a
    bare record, a `.md` record filename, a `-v<n>` version, and a decision inside a version —
    while **rejecting** a hyphenated batch-directory name in the record position.
- **Shipped surface:** the validator run, plus the `--selftest` grammar arms.
- **Deliverable + observation:** the V23 line in the post-state run, plus the arm block's printed
  labels, compared against M-3's three-pattern table.
- **⚠ This acceptance exists because the live corpus is blind — and de-minting made it BLINDER.**
  M-3 measured all three candidate grammars at 21/21 at P0; re-executed 2026-08-25 they measure
  **26/26**. A criterion phrased as *"the live citations still conform"* is **GREEN under the
  grammar the operator rejected** — it cannot fail. And the de-minting removed the last
  non-conforming tokens from the repository, so there is now **no live token anywhere that
  discriminates the strict grammar from the permissive one**. The discriminating arm on the
  hyphenated-dir token is therefore **not optional decoration; it is the only thing this
  acceptance actually tests** — and after `#D2` it is the only thing that CAN test it.
- **Boundary catalog:** ☑ empty — the shipped rule rejects 4 of the 7 legitimate forms, measured ·
  ☑ boundary — the version segment is exercised with and without a trailing decision · ☑ invalid —
  the hyphenated-dir token, the path-prefixed filename, the bare hash and the undated form must all
  be rejected · ☑ error — an empty citation set keeps the rule's existing SKIP, which already says
  *"the id glue is unused, which is a fact about this project, not a pass"* · **☑ harvest — the same
  conforming citation is recovered from each of M-11's 8 failing Markdown contexts (emphasis,
  strikethrough, terminal punctuation, unspaced table pipe), and the bare-hash form still fails
  after the widening**, which is the arm that stops an over-greedy discard set from repairing a
  token the grammar must reject.

### AT-B88-05 — the map is found through a casing difference, and a root file is not called stale

- **Observable outcome:** the map resolver returns the **on-disk casing** for a name differing only
  in case, returns the exact name when it exists, and returns nothing for an absent file and an
  absent directory; and the root-file notice and the real-orphan notice are **two different
  sentences**.
- **Shipped surface:** the `--selftest` arms plus the V8 line of a live run.
- **Deliverable + observation:** the post-state V8 line (M-4 shows it), and the four resolver arms.
- **⚠ Severity stated honestly, per `PLAN.md`'s own correction.** P-13 measured that **no CI job
  and no git hook runs the validator**, so the casing defect is **latent, not active**. The arm
  therefore asserts the **resolver's return value** — which `os.listdir` makes identical on every
  platform — and **not** a claimed CI verdict. A criterion asserting a CI behaviour would be a
  claim about a run that never happens.
- **Boundary catalog:** ☑ empty — the live V8 line calling `setup.py` stale is the RED arm ·
  ☑ boundary — exact-name-wins is exercised separately from case-folded resolution, so a resolver
  that always case-folds is caught · ☑ invalid — a real sub-directory orphan must **still** read as
  a stale map; a root sentinel in the map must classify the root **without** classifying everything
  · ☑ error — a missing `docs/` directory returns nothing rather than raising.

### AT-B88-06 — RC-2 is recorded at INTAKE with a command behind it

- **Observable outcome:** the INTAKE checklist shows an **RC-2** row at position 5, and the
  validator prints a V25 line naming the **measured age in days** — not merely the absence of a
  block.
- **Shipped surface:** `phase-checklists.md` as the operator fills it, and the validator run.
- **Deliverable + observation:** the checklist's evidence cell carries the executed
  `git ls-remote` exit status and the age; the V25 line is pasted in `04-validation.md`.
- **Negative control:** M-9's GREEN is live (exit 0, 0.003 days). Both RED directions —
  unreachable, and stale — are **synthetic by necessity** and the acceptance says so rather than
  implying a live failure was observed.
- **Boundary catalog:** ☑ empty — P-15 measured **0** occurrences of the rule token, so absence is
  the RED arm · ☑ boundary — the threshold is exercised at **30 days exactly** (pass) and at
  **31 days** (fail); a rule tested only at 0 and 365 days cannot see an off-by-one ·
  ☑ invalid — a repository with **no remote configured** is a third state and prints a third
  sentence, never the pass sentence · ☑ error — a network failure is distinguished from a remote
  that answers and reports no refs.

### AT-B88-07 — no rule this batch touches can pass without being able to fail

- **Observable outcome:** for **every** rule this batch creates or modifies, the operator observes
  in the selftest output (a) a **GREEN arm that asserts the rule's PASS SENTENCE**, and (b) a
  **`PASS != NOOP` arm** proving the pass sentence and the could-not-check sentence are different
  strings. The arm count rises from **192** to a floor stated **per rule**, never as a total.
- **Shipped surface:** `devflow-validate.py --selftest`.
- **Deliverable + observation:** the arm block printed per rule, enumerated in `04-validation.md`
  one rule per line.
- **⚠ This is the batch's own anti-vacuity acceptance, and it exists because criterion #1 was not
  enough.** P-17 measured the synthetic-exemption list at exactly **one** member. P-5 proved a rule
  can sit off that list, carry arms, pass them, and still be inert for 61 batches — because its arm
  asserted *"not red"* over a branch that is never red. **Absence from the exemption list is
  necessary and not sufficient.**
- **Boundary catalog:** ☑ empty — V5's shipped `CLEAN` arm is the worked example of an arm that
  cannot fail · ☑ boundary — the floor is asserted per rule, because a total is satisfiable by
  piling arms onto a rule that already had them (P-23) · ☑ invalid — an arm asserting only a
  severity is explicitly insufficient where two branches share a severity (both V8 notices are
  NOTICE; the **sentences** are the arm) · ☑ error — a kill mutation is named for every new arm
  block, and reverting the fix must turn that named arm red.

### AT-B88-08 (PIN) — the shipped application is untouched

- **Observable outcome:**
  `git diff --stat $(git merge-base HEAD origin/main) -- s19_app/ tests/ tools/` reports 0 files at
  every gate. **TWO files are excluded from this pin's scope, both named rather than hidden by a
  narrower diff command:** `pyproject.toml` and `.github/workflows/tui-ci.yml`, both covered by
  `LLR-88.12`. ~~one file~~ superseded — the operator's `evidence`-extra ruling makes the
  workflow edit **necessary**, because a declaration the installing job never reads declares
  nothing. The pin's three source paths are unchanged and neither exclusion is under them.
- **Shipped surface:** git.
- **Negative control:** the same command with `.dev-flow/` added returns non-empty — proof the diff
  command is not silently a no-op.
- **Boundary catalog:** ☑ empty — trivially observable · ☑ boundary — the scope names three source
  paths explicitly, in merge-base form, and names the one exclusion · ☐ invalid / ☐ error — N/A: a
  one-command structural check.
- **Labelled a PIN, not a gate:** green before any work and invariant under every edit specified.

### AT-B88-09 — the job that needs the imaging library gets it, and does not get a pinned renderer

Minted because the station-4 review found `HLR-88.6` pointing at `AT-B88-05`, which is the map
resolver's acceptance and tests nothing about a dependency (`PDR-2026-08-25-batch-88#D7`).

- **Observable outcome:** installing the project the way the `tui-ci` job installs it —
  `pip install -e ".[evidence]"` — yields an environment in which the example-GIF test's imaging
  import **succeeds**, and in which `textual` resolves to a version **>= 8.0.2 that is not the
  `8.2.8` the `dev` extra pins**. Installing with `.[dev]` instead yields the pin — which is the
  contrast that makes this acceptance able to fail.
- **Shipped surface:** `pip`, and the workflow file as executed by CI.
- **Deliverable + observation:** the resolved version printed from the installed environment, and
  `git diff -- .github/` showing exactly one file and four lines.
- **Negative control — and this acceptance is worthless without it.** The same import against an
  environment installed with **bare `-e .`** must **FAIL**. Without that arm the outcome is green
  on any machine where the imaging library happens to be present already — and it **is** present
  on the authoring machine (M-7 measured PIL 12.2.0 installed locally), so a check run here
  without the control would pass before the fix exists.
- **Boundary catalog:** ☑ empty — bare `-e .` provides nothing, measured as the negative control ·
  ☑ boundary — the `evidence` and `dev` extras are exercised separately and their resolved
  `textual` versions compared, which is the only observation that distinguishes the operator's
  ruling from the specification it overturned · ☑ invalid — a `.[evidence]` install must **not**
  drag in `pytest-textual-snapshot`, or the extras are not disjoint · ☑ error — an unknown extra
  name fails loudly at install rather than silently installing the base project.
- **⚠ The GIF suite's own pass is NOT claimed here.** No passing
  `pytest -m slow tests/test_examples_pilot_gifs.py` transcript exists (prior record's condition
  4), and this acceptance observes the **environment**, not the suite. Claiming the suite would
  be asserting an unrun command.

### Behavioral traceability (US → AT → observed outcome)

| US | Observable outcome | Shipped surface | Acceptance test | Observed? |
|----|--------------------|-----------------|-----------------|-----------|
| US-88-1 | The gate names only the active batch's ids | validator | `AT-B88-01` | RED pre-state ✅ M-2 (14 findings, May document) |
| US-88-1 | The census reports 280 and names its population | validator | `AT-B88-02` | both numbers ✅ M-4 (276 vs 280, same tree) |
| US-88-1 | A could-not-check sentence differs from a checked one | validator + selftest | `AT-B88-03` | RED pre-state ✅ M-5 (byte-identical pair) |
| US-88-1 | Record, version and decision are each expressible; batch-dir is not | validator + selftest | `AT-B88-04` | RED pre-state ✅ M-3 (4 of 7 forms rejected today) |
| US-88-1 | The map resolves through casing; a root file is not called stale | selftest + validator | `AT-B88-05` | RED pre-state ✅ M-7, M-4 |
| US-88-1 | The imaging dependency is declared in the manifest **and reaches the job that needs it** | `pip` + `pyproject.toml` + the `tui-ci` install step | **`AT-B88-09`** (~~`AT-B88-05` boundary~~ — that is the map resolver's acceptance and tested nothing here) | pre-state ✅ M-12 (0 hits in the manifest; 1 ad-hoc install at `tui-ci.yml:42`; the job installs bare `-e .`) |
| US-88-2 | RC-2 is a checklist row with a command behind it | checklist + validator | `AT-B88-06` | RED pre-state ✅ M-7 (0 occurrences); GREEN side ✅ M-9 |
| US-88-1 + US-88-2 | No touched rule can pass without being able to fail | selftest | `AT-B88-07` | RED pre-state ✅ P-5, P-17, P-23 |
| US-88-1 + US-88-2 | The shipped app is untouched | git | `AT-B88-08` (pin) | measured at gate |

---

## 4 · Requirements (HLR / LLR)

> **Scope-rule convention (batch-85 defect #7, standing).** Every Statement names the population it
> quantifies over. No repo-wide predicate appears in this section.
> **Symbol-citation convention.** Every named symbol carries a grep-verified `file:line` measured
> this session, or is flagged `NEW — created in Phase 3`.
> **Provisional-identifier convention (V-5).** Selftest arm **labels**, checklist row numbers and
> acceptance-test ids are provisional-until-Phase-3 and reconciled from the real tree at Phase 4.

### HLR-88.1 — no rule this batch creates or modifies is able to pass without being able to fail

- **Traceability:** US-88-1, US-88-2
- **Statement:** When `devflow-validate.py --selftest` runs, the system shall report, for each rule this batch creates or modifies, at least one arm asserting that rule's pass SENTENCE and at least one arm asserting that its pass sentence differs from its could-not-check sentence — the quantified population being the **seven rules** `V2`, `V5`, `V6`, `V8`, `V22`, `V23`, `V25`, and no other rule. **`_artifacts` is deliberately NOT in this population** (`PDR-2026-08-25-batch-88#D6`): it is a LOADER, it returns a document map rather than a finding, and it therefore has no severity, no pass sentence and no could-not-check sentence for an arm to assert. Its analogous obligation is stated in `LLR-88.5` and is a different assertion — **which document it resolves to**, not what sentence it prints.
- **Rationale (informative):** P-5 measured a rule that carried arms, passed them, and was inert for 61 batches because its arm asserted only "not red" over a branch that is never red. P-17 measured the synthetic-exemption list at exactly one member, so absence from that list is necessary and not sufficient. This requirement is the widened form of the original acceptance criterion #1.
- **Validation:** `test`
- **Executed verification:** `python ~/.claude/docs/tools/devflow-validate.py --selftest`, arm labels enumerated one rule per line; pre-state M-1 (192 arms, exit 0; **0** arms for V2, V5, V6, V25).
- **Numeric pass threshold:** exit code **0**; arm-result lines **≥ 240**; and, enumerated per rule, **≥ 1** pass-sentence arm and **≥ 1** `PASS != NOOP` arm for each of the **7** members of the population — **7 of 7**, never a total. ~~8 of 8~~ superseded: the loader `_artifacts` left the population because it has no sentence to assert (`PDR-2026-08-25-batch-88#D6`), and a population containing a member the mandate cannot be satisfied for is an unsatisfiable requirement, not a demanding one. **Separately, ≥ 1 arm shall assert `_artifacts` resolves `01-requirements.md` to the ACTIVE batch's copy when the active directory holds one, and ≥ 1 that it does not silently substitute another batch's copy when it does not** — the loader's `PASS != NOOP` analogue, stated over its return value.
- **Priority:** high
- **Acceptance (black-box) — the user-verified outcome:**
  - **Observable outcome:** the operator reads the selftest block and finds, for every rule this batch touched, a named arm asserting what the rule SAYS when it passes.
  - **Shipped surface:** `devflow-validate.py --selftest`.
  - **Deliverable + observation:** the arm block pasted into `04-validation.md`, enumerated one rule per line, with the kill mutation named for each.
  - **Acceptance test(s):** `AT-B88-07`.
  - **Boundary catalog:** ☑ empty — V5's shipped `CLEAN` arm is the worked example · ☑ boundary — per-rule enumeration, never a total (P-23) · ☑ invalid — a severity-only assertion is insufficient where two branches share a severity · ☑ error — each new arm block names the mutation that reverts it.

### HLR-88.2 — the rules that judge one document judge the declared one, or say they cannot

- **Traceability:** US-88-1
- **Statement:** When the validator runs in a repository whose `.dev-flow/state.json` declares an active batch, the system shall supply rules `V1` through `V9` with only the artifacts present in that batch's own directory, and shall report an absent artifact as absent — the quantified population being the basename-keyed artifact map `_artifacts` returns (`devflow-validate.py:2214`).
- **Rationale (informative):** *Absent* and *some other batch's copy* are different states, and the loader conflates them. Every batch passes through the window between pointing `state.json` at a new directory and Phase 1 authoring its record, and in that window the gate judges a document from May 2026.
- **Validation:** `test`
- **Executed verification:** `--selftest` arms over synthetic two-batch trees (declared batch without the file / declared batch with it / no `state.json`), plus `python ~/.claude/docs/tools/devflow-validate.py .` at a station where the active batch has authored the record; pre-state M-2.
- **Numeric pass threshold:** in the **absent-artifact state** — an active batch directory holding no `01-requirements.md`, which is the only state the authoring act does not reach (M-10) — **0** findings naming an id declared under `.dev-flow/2026-05-05-batch-01/`, against a pre-state of **14** measured in exactly that state (M-2); and **≥ 4** arms, of which one asserts the artifact KEY is absent rather than foreign, and one asserts the walk fallback survives when no `state.json` exists.
- **Priority:** high
- **Acceptance (black-box):** `AT-B88-01`.

### HLR-88.3 — the canon census counts what its sentence says it counts, and its baseline moves before anything measures it

- **Traceability:** US-88-1
- **Statement:** When the validator reports the living-canon census, the system shall resolve each declared id by TOKEN membership over the canon's harvested id set and shall name, in the same sentence, the population it counted and the distinct larger population the orphan census counts — the quantified population being the single `V22` notice whose subject is `REQUIREMENTS.md`.
- **Rationale (informative):** the same tool reported two numbers for one property — 276 by substring and 280 by token over an identical population. The operator has ruled this fix lands first, with the re-baseline recorded explicitly, because a story whose oracle is this number cannot be measured while the number moves.
- **Validation:** `test`
- **Executed verification:** `python ~/.claude/docs/tools/devflow-validate.py .`, the `V22 REQUIREMENTS.md` line; plus the `--selftest` oracle arms; pre-state M-4 (`276 of 544` shipped, `280 of 544` patched, same tree).
- **Numeric pass threshold:** the census reports **280** of **544**; the notice text contains the population name and the orphan-census disambiguation; **≥ 4** arms covering substring-false-positive, exact-match, plain-absence and the scope sentence.
- **Priority:** high
- **Acceptance (black-box):** `AT-B88-02`.

### HLR-88.4 — the design-review grammar expresses a record, a version of it, and a decision inside it, and each is distinguishable

- **Traceability:** US-88-1
- **Statement:** When the validator parses design-review citations, the system shall accept exactly four citation forms — the bare record, the record's `.md` filename, a version of the record, and a decision within either the record or a version of it — and shall reject a batch segment carrying any hyphenated component other than a version marker; the quantified population being every token `_V23_TOKEN` (`devflow-validate.py:1556`) harvests from `REQUIREMENTS.md` and the `.dev-flow/` walk.
- **Rationale (informative):** the seal rule requires a changed record to be a new version with a new id, and the shipped grammar cannot express a version at all. The permissive repair proposed in the measured patch buys that expressiveness by admitting arbitrary hyphens, which makes a batch-directory name parse as a record — the operator has rejected it, and M-3 shows the live corpus cannot tell the two repairs apart.
- **Validation:** `test`
- **Executed verification:** `python ~/.claude/docs/tools/devflow-validate.py .`, the `V23` line; plus the `--selftest` grammar arms over the constructed token table of M-3. Pre-state re-executed **2026-08-25**: **26 citations, 26/26 conforming** under all three candidate grammars — the live corpus is blind, and after the C-56 de-minting (`PDR-2026-08-25-batch-88#D2`) it holds no discriminating token at all. ~~21/21~~ superseded.
- **Numeric pass threshold** (restated per `PDR-2026-08-25-batch-88#D2` — the prior form thresholded on a population this document changes by citing anything): the **non-conforming citation set is ∅**, over a conforming population of **≥ 26** (the floor measured 2026-08-25, not a target); **≥ 11** grammar arms, of which **≥ 2** assert REJECTION of a hyphenated batch-directory token, **≥ 2** assert ACCEPTANCE of a version token (with and without a trailing decision), and **≥ 4** preserve the shipped rejections. **The arm count is the load-bearing half; the set being ∅ is a floor that is green under the rejected grammar too.**
- **Priority:** high
- **Acceptance (black-box):** `AT-B88-04`.

### HLR-88.5 — the module map is readable by the rule that reads it, and an unexpressible file is not called stale

- **Traceability:** US-88-1
- **Statement:** When the module-map rule opens the project map, the system shall resolve the map's filename case-insensitively over its parent directory, and shall report a repository-root source file as a file the rule cannot express rather than as evidence that the map is stale — the quantified population being the map at `docs/architecture.md` and the tracked source files the rule classifies.
- **Rationale (informative):** the map is tracked in one casing and opened in another; on a case-sensitive filesystem the rule would report "map not adopted here", and a skipped rule raises nothing at all. P-13 measured that no CI job and no git hook runs the validator, so the condition is latent rather than active, and this requirement is specified at that severity.
- **Validation:** `test`
- **Executed verification:** `--selftest` resolver arms (exact-name-wins · case-folded resolution · absent file · absent directory · map read through a casing difference) plus the two-sentence arms; and the `V8` line of `python ~/.claude/docs/tools/devflow-validate.py .`; pre-state M-7 and M-4.
- **Numeric pass threshold:** **≥ 9** arms; the root-file notice and the sub-directory-orphan notice are **2 distinct strings**; total notice count **284**, unchanged (a text change with a count change would mean a classification change, which is not what this asks for).
- **Priority:** medium
- **Acceptance (black-box):** `AT-B88-05`.

### HLR-88.6 — the imaging dependency is declared in the project manifest at the tier the project's own convention assigns it

- **Traceability:** US-88-1
- **Statement:** When the continuous-integration job that runs the full test suite installs the project, the system shall provide the imaging library the example-GIF test imports **from a declared `evidence` optional-dependency table of `pyproject.toml`**, shall leave the shipped runtime dependency list unchanged, and **shall not pin the renderer for that job** — the quantified population being the `[project].dependencies`, `[project.optional-dependencies].dev` and `[project.optional-dependencies].evidence` tables of `pyproject.toml` together with the install step of the `tui-ci` job.
- **Rationale (informative):** the dependency is not undeclared; it is declared in a CI workflow with a comment already ruling it dev tooling, which means exactly one of the several ways this project is installed provides it. The manifest's own comment forbids adding a dev-only package to the runtime list, so the tier is settled by the file rather than by preference. **The `dev` extra is equally forbidden, and for a measured reason** (M-12): it pins `textual==8.2.8` for the snapshot environment, so routing the full-suite job through it would silently pin the renderer under the **2735** tests that job collects. A **third** extra is the only tier that declares the dependency without moving the runtime. Operator ruling 2026-08-25, `PDR-2026-08-25-batch-88#D7`.
- **Validation:** `inspection`
- **Executed verification:** read of `pyproject.toml` and `.github/workflows/tui-ci.yml` after the edit against the **M-12** pre-state (0 occurrences in the manifest; 1 ad-hoc install at `.github/workflows/tui-ci.yml:42`; `dev` extra holds 2 entries; the `tui-ci` job installs bare `-e .` at `:43`); plus a resolved-environment check that the imaging library is importable and `textual` resolves **>= 8.0.2 and not pinned to 8.2.8** in that job.
- **Numeric pass threshold:** `[project].dependencies` holds exactly **3** entries, **byte-identical**; the `dev` extra holds exactly **2** entries, **unchanged** — the pin does not travel; a new `evidence` extra holds exactly **1** entry; `.github/workflows/tui-ci.yml` is modified in exactly **4** lines — `:43` rerouted to `.[evidence]` and the three ad-hoc lines `:40-42` removed — and in **no other line**; the `snapshot` job's `:76` `.[dev]` install is **byte-identical**. ~~`dev` extra holds 3 entries; tui-ci.yml modified in 0 lines~~ superseded by the operator ruling.
- **Priority:** medium
- **Acceptance (black-box):** **`AT-B88-09`**, which observes this requirement's own outcome through the shipped surface. ~~the `AT-B88-05` boundary row~~ superseded: `AT-B88-05` is the **map resolver's** acceptance and tests nothing about a dependency — the station-4 review found this requirement pointing at an acceptance that does not test it (`PDR-2026-08-25-batch-88#D7`). `AT-B88-08`'s pin scope EXCLUDES `pyproject.toml` and now also `.github/workflows/tui-ci.yml`, and says so.

### HLR-88.7 — the batch records verifiable backup at INTAKE, and a rule states what it measured

- **Traceability:** US-88-2
- **Statement:** When the validator runs, the system shall report whether the repository's configured remote answered and how many days have passed since the newest commit on any remote-tracking ref, and the INTAKE checklist shall carry a backup row beside the upstream-tip row — the quantified population being the repository's `origin` remote and the eight-row INTAKE table of `phase-checklists.md`.
- **Rationale (informative):** a single operator carries more backup exposure than a team, because no colleague's clone is a second copy. The value of this rule is that it names a measured age, not that it stays green.
- **Validation:** `test`
- **Executed verification:** `--selftest` arms for the rule over synthetic repositories (reachable and fresh · reachable and stale · no remote configured · remote unreachable), plus the `V25` line of `python ~/.claude/docs/tools/devflow-validate.py .`; pre-state M-9 (exit 0, 9 refs, 0.003 days) and M-7 (0 occurrences of the rule token).
- **Numeric pass threshold:** the rule's pass line states the age in days with **≥ 1** digit; the threshold is exercised at **30** days (pass) and **31** days (fail); **≥ 6** arms including **≥ 1** `PASS != NOOP`; **4** distinct sentences across the four states.
- **Priority:** high
- **Acceptance (black-box):** `AT-B88-06`.

### HLR-88.8 — after every edit, the flow's identity is what its manifest declares

- **Traceability:** US-88-1, US-88-2
- **Statement:** When the validator runs after the last increment, the system shall report the flow revision as current and the packaged bundle as identical to the manifest table in both directions — the quantified population being the `V7` and `V15` findings of one validator run.
- **Rationale (informative):** editing the validator and the templates without bumping and re-hashing leaves the manifest asserting an identity the files no longer have, and the bundle is a build output that drifts silently if `--sync-bundle` is not re-run.
- **Validation:** `test`
- **Executed verification:** `python ~/.claude/docs/tools/devflow-validate.py .`, the `V7` and `V15` lines; pre-state M-2 (`[-] V7 FLOW-VERSION.md: flow current (9c1449ed815d267c)`).
- **Numeric pass threshold:** `V7` reports current against the bumped revision; `V15` reports **0** unpaired entries in either direction; **0** derived-file drift blocks.
- **Priority:** high
- **Acceptance (black-box):** covered by `AT-B88-07`'s selftest run and the closing validator run; there is no separate acceptance, and saying so is the honest form.

---

### LLR-88.1 — the ledger rule's two outcomes stop sharing one sentence

- **Traceability:** HLR-88.1
- **Statement:** The ledger rule (`v5_ledger`, `devflow-validate.py:148`) shall parse a ledger expression whose left operand is a word as well as one whose left operand is a digit sequence, shall emit a distinct message for the state in which no ledger expression exists, and shall emit a message naming the reconciled figures for the state in which one exists and reconciles — the quantified population being the return values of that function over the 61 files named `04-validation.md` under `.dev-flow/`.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the V5 arm block; plus a re-run of M-5's 61-file sweep against the modified rule.
- **Numeric pass threshold:** the 4 historically-parsing records (batches 09, 10, 11, 74) still parse — **4 of 4**, a regression floor; the count of records for which the rule reports a reconciliation rises from **4** to **≥ 34**; the no-expression message and the reconciled message are **2 distinct strings**; **≥ 4** arms including **≥ 1** `PASS != NOOP`.
- **Acceptance criteria (informative):**
  - The RED arm is a **real record** — `.dev-flow/2026-08-24-batch-87/04-validation.md`, which exists (measured, M-5) — paired against a body of `"# nada\n"`, and today the two outputs are byte-identical.
  - A threshold of *"V5 no longer SKIPs"* would be satisfied by deleting the SKIP branch, which is why the threshold is stated as a **count of records reconciled** with the 4-record regression floor beneath it.
  - The rule must **still BLOCK** a non-reconciling ledger; a fix that made it stop blocking would raise this rule's coverage and lower its value.

### LLR-88.2 — acceptance-id resolution becomes token membership

- **Traceability:** HLR-88.1
- **Statement:** The acceptance-node rule (`v2_at_without_node`, `devflow-validate.py:230`) shall resolve each declared acceptance id against the set of acceptance and test tokens harvested from the `tests/` tree by the truncation-guarded tokenizer `_ATLAS_ID_ATTC` (`devflow-validate.py:1574`), rather than by substring containment against the concatenated tree text — the quantified population being the acceptance ids the rule harvests from the active batch's requirements document.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the V2 arm block, over synthetic trees.
- **Numeric pass threshold:** **≥ 4** arms, of which **≥ 1** asserts that a short id whose longer extension exists on disk resolves as MISSING, **≥ 1** that the exact id resolves as PRESENT, **≥ 1** that a plainly absent id stays MISSING, and **≥ 1** is a `PASS != NOOP` arm distinguishing "no ids declared" from "all ids resolved". Pre-state: **989** distinct tokens on disk; the short-id case measured `substring=True, token=False`.
- **Acceptance criteria (informative):**
  - **The arms are necessarily SYNTHETIC and this document says so rather than implying otherwise.** P-7 measured that this repository's acceptance ids are letter-initial and the rule's own pattern requires digits immediately after the prefix, so the live line is `no AT ids declared` — the rule **SKIPs here and will keep SKIPping after the fix**.
  - The discriminating token is described by position and operation in M-6 and is **not reproduced** anywhere in this document, because writing it would register it as a declared id.
  - The tokenizer this LLR names already exists in the same file and is not used here — the fix is a call-site change, not a new parser.

### LLR-88.3 — the modal check becomes statement-scoped

- **Traceability:** HLR-88.1
- **Statement:** The modal rule (`v6_modal_in_statement`, `devflow-validate.py:162`) shall evaluate the modal against the whole statement block — the statement marker's line together with its indented continuation lines, terminated by the next requirement heading or the next unindented list item — rather than against a single physical line; **and shall emit a pass SENTENCE naming the number of statement blocks it scanned when it finds no modal, distinct from its could-not-check sentence when no requirements document is present**; the quantified population being the statement blocks of the active batch's requirements document.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the V6 arm block, over synthetic records differing only in a line break.
- **Numeric pass threshold:** **≥ 6** arms; a wrapped statement carrying the modal on a continuation line yields **1** BLOCK where the shipped rule yields **0** findings (pre-state measured, M-6); a modal in the rationale beneath the statement yields **0** BLOCKs; **≥ 1** `PASS != NOOP` arm; and **≥ 2** arms over the new pass sentence — one asserting the sentence itself with its block count, one asserting it differs from the no-document sentence. **Pre-state measured 2026-08-25: V6 emits NOTHING on success** — it returns an empty finding list, so the rule prints no line at all, which is why `HLR-88.1`'s pass-sentence mandate was unsatisfiable for it as written (`PDR-2026-08-25-batch-88#D6`). **This is the LLR that makes it satisfiable**, and without it `HLR-88.1` names a population it cannot be met over.
- **Acceptance criteria (informative):**
  - The negative arm is the load-bearing one. Widening the scope past the statement's own block would make every rationale paragraph a finding — and the same file's `v4_method_without_verification` docstring (`devflow-validate.py:246`) already argues that a line-scoped version of a block-scoped rule *"false-fails a perfectly correct requirement — which is as expensive as passing a wrong one."* This LLR applies that argument to its neighbour.
  - **Scope note for this batch:** once the loader fix (LLR-88.5) lands, this rule judges **this document**. The statements in §4 were authored with the modal absent from every statement block, checked by the same probe (M-6).

### LLR-88.4 — the arm floor is stated per rule, with a named kill mutation each

- **Traceability:** HLR-88.1
- **Statement:** The selftest shall print, for each of the eight members of HLR-88.1's population, at least one arm whose assertion is the rule's pass SENTENCE and at least one arm whose assertion is that the pass sentence and the could-not-check sentence are unequal strings, and each new arm block shall name in a comment the mutation that turns it red — the quantified population being the arm blocks of `devflow-validate.py`'s `--selftest` path.
- **Validation:** `inspection`
- **Executed verification:** read of the selftest source after the edit, one rule at a time; the printed labels of `devflow-validate.py --selftest` enumerated one rule per line into `04-validation.md`.
- **Numeric pass threshold:** **8 of 8** rules carry both arm kinds; **8 of 8** new arm blocks carry a named kill mutation; total arm lines **≥ 240** against a pre-state of **192**.
- **Acceptance criteria (informative):**
  - **The total is a floor, never the criterion.** P-23 measured the 29 arms already written and found **0** of them touch four of the nine fixes; a total threshold is satisfiable by piling arms onto a rule that already had them.
  - Where two branches of one rule share a severity — both module-map notices are NOTICE — a severity assertion cannot see the defect and the **sentences** are the arm. That is the shape the shipped `V18 CAUSES-differ` and `V20 PASS!=NOOP-msg` arms already use.
  - **Absence from the synthetic-exemption list is necessary and not sufficient**, and the criterion is written that way rather than as the original proposal's criterion #1.

### LLR-88.5 — the artifact loader prefers the declared batch and reports absence as absence

- **Traceability:** HLR-88.2
- **Statement:** The artifact loader (`_artifacts`, `devflow-validate.py:2214`) shall, when `.dev-flow/state.json` declares an active batch, populate its map from that batch's directory alone, shall omit the key entirely for a basename that directory does not contain, and shall fall back to the whole-tree walk only when no active batch is declared — the quantified population being the basename-keyed map the function returns.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the loader arm block, over synthetic two-batch trees; plus `python ~/.claude/docs/tools/devflow-validate.py .` at this station.
- **Numeric pass threshold:** **≥ 4** arms — a declared batch lacking the file yields **0** BLOCKs and a message naming the file as absent; a declared batch holding a defective copy still yields **≥ 1** BLOCK; the map key is absent rather than foreign; the walk fallback survives with no `state.json`. Live: BLOCK count **14 → 0** (measured both sides, M-2 and M-4).
- **Acceptance criteria (informative):**
  - `_active_batch_dir` is at `devflow-validate.py:2206` and is **unchanged**; this LLR changes only which map the walk builds.
  - The second arm is the control. A loader that simply stopped judging anything would take the gate to 0 blocks and satisfy a count threshold — it is caught only by asserting that the declared batch's **own** defective document still blocks.
  - **This change makes this document judgeable.** Stated as a consequence, not hidden: after it lands, the rules this batch is fixing judge the record that specifies the fixes.

### LLR-88.6 — the census oracle becomes token membership

- **Traceability:** HLR-88.3
- **Statement:** The canon-mirror rule (`v22_canon_mirror`, `devflow-validate.py:1461`) shall test each declared id for membership in the set `_ATLAS_ID_REQ` (`devflow-validate.py:1575`) harvests from the canon text, rather than for substring containment in that text — the quantified population being the declared-id set the rule iterates.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the census oracle arms; plus the `V22 REQUIREMENTS.md` line of a live run.
- **Numeric pass threshold:** the live census reports **280** of **544** against a pre-state of **276** of **544** (both measured on one tree, M-4); **≥ 3** oracle arms — substring-false-positive, exact-match, plain-absence.
- **Acceptance criteria (informative):**
  - The discriminating id class is a story id present in the canon **only inside a letter-suffixed variant of itself**. The token is described here and **not reproduced**, for the same reason as LLR-88.2.
  - **The delta of exactly 4 is the whole error**, and it is the difference between two numbers this tool already prints for one property.

### LLR-88.7 — the census names the population it counted

- **Traceability:** HLR-88.3
- **Statement:** The canon-mirror notice shall name the population it counted — identifiers declared as headings in the `01-requirements.md` corpus — and shall state that the orphan census counts a larger mention-population, a different question rather than a disagreement — the quantified population being the single notice whose subject is `REQUIREMENTS.md`.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the notice-scope arm; plus the live `V22` line.
- **Numeric pass threshold:** the notice text contains the population name and the orphan-census file name — **2 of 2** required substrings; **≥ 1** arm asserting both are present.
- **Acceptance criteria (informative):** a reader who maps a heading-scoped figure onto a mention-scoped one concludes the tool disagrees with itself. The two numbers are both right about different questions, and the sentence is where that gets said.

### LLR-88.8 — the re-baseline is recorded before any evidence is measured against it

- **Traceability:** HLR-88.3
- **Statement:** The batch objective in `.dev-flow/state.json` and the scope section of `PLAN.md` shall state the canon-census baseline as **306 of 570 measured 2026-08-25**, shall carry that measurement DATE as part of the figure, and shall record the supersession of both previously declared values — **276 of 544** and **280 of 544** — and the commit carrying that restatement shall precede in commit order every commit touching `REQUIREMENTS.md` in this batch and the next — the quantified population being the baseline figures of those two documents.
- **Validation:** `inspection`
- **Executed verification:** read of both documents after the edit; `git log --oneline --reverse` over the batch's commits, comparing the position of the restatement commit against any commit whose diff touches `REQUIREMENTS.md`.
- **Numeric pass threshold** (re-derived per `PDR-2026-08-25-batch-88#D4`): **0** occurrences of `276 of 544` or `280 of 544` as a **declared baseline** in either document — occurrences inside a supersession note are required, not forbidden, and are counted separately; **2** documents restating **306 of 570** with its measurement date; the restatement commit's ordinal is **strictly less** than that of every `REQUIREMENTS.md`-touching commit, and this batch touches `REQUIREMENTS.md` in **0** commits.
- **⚠ The baseline carries a date because the DENOMINATOR moves.** Re-executed 2026-08-25: shipped **302 of 570**, patched **306 of 570**, delta **+4** — `HLR-053`, `HLR-056`, `US-064`, `US-068`. ~~276 → 280 of 544~~ was not wrong when measured; it expired because **batch-88 declared 26 ids of its own** and every batch does the same. **The +4 delta survives every re-measurement; the baseline survives none.** A bare figure with no date is therefore not a re-baseline, it is a future refutation, and this requirement forbids writing one.
- **Acceptance criteria (informative):**
  - The same `state.json` string also carries the refuted "section 4" wording for the deferred Layer C rule (P-19). Correcting the baseline without correcting that phrase would leave one document asserting two superseded things; the correction is **D-88-4** and is an operator decision because it edits a declared objective.
  - This LLR owns no transform. It constrains ordering, and it is expected to appear as an unowned-identifier notice — which is a question per id, never a gate.

### LLR-88.9 — the design-review grammar, in its strict form

- **Traceability:** HLR-88.4
- **Statement:** The design-review grammar (`_V23_OK`, `devflow-validate.py:1557`) shall admit a batch segment consisting of a date, the literal batch marker and a single alphanumeric component, followed optionally by a version marker of the form `-v` plus a digit sequence, followed optionally by either a decision suffix of the form `#D` plus a digit sequence or the literal Markdown extension — and shall admit no other hyphenated component in the batch segment; **and the citation harvester shall discard every trailing character that no citation can end in, the discard set being derived from the id grammar's own character class rather than enumerated from Markdown delimiters** — the quantified population being every token the citation harvester collects from `REQUIREMENTS.md` and the `.dev-flow/` walk.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the grammar arm block over M-3's constructed token table, **and the harvester arm block over M-11's twelve Markdown contexts**; plus the `V23` line of a live run. Pre-state M-3, re-executed 2026-08-25: **26/26** conforming under the shipped grammar, the permissive candidate AND this strict one (~~21/21~~ superseded). Pre-state M-11: **8 of 12** ordinary Markdown contexts mangle a conforming citation into a notice.
- **Numeric pass threshold** (restated per `PDR-2026-08-25-batch-88#D2`, and widened by the operator ruling of 2026-08-25): the **non-conforming citation set is ∅** over a conforming population of **≥ 26** — a floor with its measurement date, never a target, because this document mints a conforming citation every time it cites a record. **Grammar arms: ≥ 11**; **≥ 2** assert REJECTION of a hyphenated batch-directory token (with and without a trailing decision); **≥ 2** assert ACCEPTANCE of a version token (with and without a trailing decision); **≥ 4** preserve the shipped rejections (undated, placeholder, bare hash, path-prefixed). **Harvester arms: ≥ 8**, one per failing Markdown context of M-11, each asserting the CLEAN citation is recovered — and **≥ 1 `PASS != NOOP` arm** proving the bare and code-span contexts, which pass today, are not what the new arms measure. **≥ 1 arm must assert that a bare-hash form still FAILS after the widening**, because a discard set that is too greedy would repair the token the grammar is required to reject.
- **Acceptance criteria (informative):**
  - **The measured patch's grammar is REJECTED here and the rejection is not a matter of taste.** Its batch segment admits any number of alphanumeric hyphenated components, so this batch's own directory name parses as a record. Measured, both directions (M-3).
  - **The live corpus cannot discriminate the two candidates** — all three grammars score 21/21. Without the rejection arms, this rule's acceptance is green under the grammar the operator rejected, which is the vacuous-check shape at the level of the *criterion* rather than of the rule.
  - The Markdown-extension form is admitted because the flow's own initialisation command prescribes exactly that filename shape for a sealed record; a grammar forbidding the convention its own command prescribes is the rule disagreeing with the flow.
  - **The harvester half was found by tripping it while authoring §0.3 (M-11), and it is a SHIELDING defect, not a missing-character defect.** `rstrip` halts at the first character outside its set, so one unlisted delimiter protects every listed one behind it — the shipped set already contains the backtick and still cannot reach it through a `*`. **The repair is therefore specified as a character CLASS derived from the grammar, not as a longer list of delimiters**, because an enumeration of Markdown punctuation has no closing condition and would owe a new entry at every new authoring convention.
  - **This half moves 0 live verdicts today** — the de-minting of `PDR-2026-08-25-batch-88#D2` left no mangled citation on disk — so its evidence is necessarily synthetic, exactly as the grammar half's is, and the acceptance says so rather than claiming a live pre-state.

### LLR-88.10 — the map resolver, kept pure and kept local to its rule

- **Traceability:** HLR-88.5
- **Statement:** The module-map rule shall obtain the map's contents through a resolver that returns the on-disk filename matching the requested name case-insensitively within its parent directory, preferring an exact match, and returning nothing when the directory or the name is absent; and the general file reader used by the revision-identity and bundle-pairing rules shall remain exact — the quantified population being the map read at `devflow-validate.py:245` and no other read in the file.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the resolver arm block over a temporary directory holding a lowercase map.
- **Numeric pass threshold:** **≥ 5** arms — case-folded resolution returns the on-disk casing; an exact name returns itself; an absent name returns nothing; an absent directory returns nothing; the map is read through a casing difference. **0** call sites other than the module-map rule use the resolver.
- **Symbol citations:** `_read` `devflow-validate.py:47` (EXISTING, unchanged) · the module-map rule's map read `devflow-validate.py:245` (EXISTING) · the resolver and the resolving reader are **NEW — created in Phase 3**.
- **Acceptance criteria (informative):**
  - **The arm asserts the resolver's return value, not a platform verdict.** The directory listing returns on-disk casing on every platform, so the arm asserts the same thing on Windows and on Linux — which matters because P-13 measured that **no CI job and no git hook runs this validator**, so a claim about CI behaviour would be a claim about a run that does not happen.
  - The exclusion in the second half is load-bearing: the revision-identity and bundle-pairing rules hash what the general reader returns, and a case-folded resolution there would move the flow's own identity.
  - Two exits existed — rename the tracked file, or resolve in the reader. The second touches one call site and retargets **0** existing citations of `docs/architecture.md`; the first would retarget every one. The same reasoning kept the interface table at its existing section number.

### LLR-88.11 — a root file reads as unexpressible, and a declared root sentinel classifies it

- **Traceability:** HLR-88.5
- **Statement:** The module-map rule shall emit, for a tracked source file at the repository root, a message stating that no path-prefix declaration can match a file with no directory component, and shall emit for a sub-directory file under no declared prefix the existing staleness message; and where the map declares a bare recursive sentinel, the rule shall classify root files without classifying files under undeclared sub-directories — the quantified population being the orphan list the rule builds from `git ls-files`.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the two-sentence arm block over a synthetic repository holding one root orphan and one sub-directory orphan; plus the `V8` line of a live run.
- **Numeric pass threshold:** the two messages are **2 distinct strings**; the root message contains the word signalling inexpressibility and does **not** contain the staleness phrase; the sub-directory message **does** contain it; with a sentinel declared, the root file is classified and the sub-directory orphan is **not**; total notice count **284**, unchanged (M-4).
- **Acceptance criteria (informative):**
  - **Both messages are the same severity**, so a severity assertion cannot see this defect. The sentences are the arm.
  - The defect is not the notice; it is that the notice says the wrong thing. *"The map is stale"* and *"the rule cannot express this file"* are different conditions with different remedies, and the map's own §10 already names `setup.py` as the bounded case (M-8).

### LLR-88.12 — the imaging dependency lands in its own `evidence` extra, and CI is routed through it

- **Traceability:** HLR-88.6
- **Statement:** A new `evidence` optional-dependency table of `pyproject.toml` shall declare the imaging library the example-GIF test imports; the `[project].dependencies` and `dev` tables shall remain byte-identical; the `tui-ci` job's project install shall be routed through that extra and its ad-hoc imaging install removed; and the `snapshot` job's install shall remain byte-identical — the quantified population being those three tables and the two install steps of `.github/workflows/tui-ci.yml`.
- **Validation:** `inspection`
- **Executed verification:** read of `pyproject.toml` and `git diff -- .github/` after the edit, against the **M-12** pre-state; plus an executed `pip install -e ".[evidence]"` into a clean environment, importing the library and printing the resolved `textual` version.
- **Numeric pass threshold:** `[project].dependencies` holds exactly **3** entries and the `dev` extra exactly **2**, both byte-identical to M-12; the `evidence` extra holds exactly **1** entry; `.github/` shows exactly **1** changed file with exactly **4** changed lines; the resolved `textual` in the `evidence` environment satisfies **>= 8.0.2** and is **not** `8.2.8`-pinned. ~~`.github/` shows 0 changed files~~ superseded — **and the supersession is the point**: the old threshold forbade the only edit that makes the declaration reach the job that needs it.
- **Acceptance criteria (informative):**
  - **The tier is settled by the file, not by preference.** The manifest's own comment states that the snapshot dependency *"is NEVER added to `[project]` dependencies — it does not affect the s19tui runtime footprint"*, and the workflow comment already rules the imaging library *"dev tooling, not a runtime dep"*.
  - **The workflow line is deliberately left in place, and removing it is a trap this LLR forbids.** Measured at `.github/workflows/tui-ci.yml:43`: CI installs `pip install -e .` **without** the `dev` extra. Deleting the ad-hoc install while the declaration lives only in `dev` would leave CI's **push** job — which runs the full suite including the slow example-GIF test, with **0** import-skip guards (M-7) — without the library. Removing it correctly requires a CI change; that is **D-88-3**.
  - The declared version bound is `assumed — verify in Phase 3`. The locally installed version is 12.2.0 (measured); whether to floor at that, or leave it unbounded as `rich` is, is an implementation choice with no measurement behind it yet.

### LLR-88.13 — the backup rule's two checks, and its four distinguishable outcomes

- **Traceability:** HLR-88.7
- **Statement:** The backup rule shall report the repository's configured remote as answering or not answering, shall report the number of days since the newest commit on any remote-tracking reference, and shall emit four mutually distinct messages for the states *fresh*, *stale*, *no remote configured* and *remote did not answer* — the quantified population being the findings that rule returns for one repository.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, the backup rule's arm block over synthetic repositories; plus the rule's line in `python ~/.claude/docs/tools/devflow-validate.py .`. Pre-state M-9: reachable (exit 0, 9 refs), newest ref **0.003 days**; M-7: **0** occurrences of the rule token in the validator today.
- **Numeric pass threshold:** the pass message contains the measured age with **≥ 1** digit; the threshold passes at **30** days and fails at **31**; the **4** state messages are **4** distinct strings; the rule's severity for a stale or unreachable remote is NOTICE, never BLOCK, and the shipped rule count rises by exactly **1**.
- **Symbol citations:** the rule function, its outcome core and its coverage-map entry are all **NEW — created in Phase 3**. The finding constructor `F` `devflow-validate.py:33` and the rule registry list `devflow-validate.py:1904-1914` are EXISTING.
- **Acceptance criteria (informative):**
  - **The rule's value is the number it states, not the colour it prints.** A backup rule that only ever says "ok" tells the operator nothing they did not already assume, which is the condition the whole batch exists to remove.
  - **Boundary at the threshold, not at the extremes.** Arms at 0 days and 365 days cannot see an off-by-one; 30 and 31 can.
  - *No remote configured* is a **third** state, not a variant of unreachable — a repository with no `origin` has no backup and no network problem, and collapsing the two would print a network diagnosis for a configuration fact.
  - The rule is a NOTICE because a stale backup is a fact about the operator's week, not a defect in the batch's content; a BLOCK here would make the gate a hostage to connectivity.

### LLR-88.14 — the backup rule's arms, and the honest labelling of what is synthetic

- **Traceability:** HLR-88.7
- **Statement:** The selftest shall exercise the backup rule with at least one arm asserting its pass SENTENCE, at least one arm asserting that its pass sentence differs from each of its three non-pass sentences, and one arm per boundary side of the age threshold — the quantified population being that rule's arm block.
- **Validation:** `test (unit)`
- **Executed verification:** `devflow-validate.py --selftest`, arm labels enumerated into `04-validation.md`.
- **Numeric pass threshold:** **≥ 6** arms; **≥ 1** `PASS != NOOP`; **2** threshold-boundary arms at 30 and 31 days; and the rule does not appear on the **DERIVED** synthetic-exemption set specified in the next bullet, whose membership is **1** (`V8`) today and must remain **1**.
- **The exemption set shall be COMPUTED, not printed** (`PDR-2026-08-25-batch-88#D3`). The selftest shall derive it as the set difference between the rule names declared in `CHECKS` and the rule names appearing in its own emitted arm labels, shall print the derived set, and shall drive its `ok` flag from a comparison against a declared expected set — so that a rule shipping with no arms makes the selftest **FAIL** rather than making a sentence inaccurate.
- **The derivation owes its own `PASS != NOOP` arm, or the vacuity has only moved.** **≥ 1** arm shall feed the derivation a corpus in which a rule has no arms and assert the derived set is **non-empty**; **≥ 1** shall assert that over the real arm set it is exactly `{V8}`. Without the first, a derivation that always returned the empty set would pass every check written over it.
- **Acceptance criteria (informative):**
  - **Both RED sides are synthetic and this document says so.** M-9 measured only the GREEN side live; the repository is reachable and was written 0.003 days ago. A criterion implying a live failure was observed would be false.
  - **Membership of the exemption list is asserted as a count that must not rise.** P-17 measured it at exactly one rule, and the selftest's own tail says the list *"shrank by measurement rather than by assertion"* — a new rule joining it would reverse that, silently.
  - The age is computed from remote-tracking references, which is a **bound on staleness rather than a proof of currency** — the same honest limitation the revision-currency rule already declares about itself in `FLOW-VERSION.md`.

### LLR-88.15 — the INTAKE checklist carries the backup row

- **Traceability:** HLR-88.7
- **Statement:** The INTAKE table of `phase-checklists.md` shall carry a row numbered 5 requiring the remote's reachability and the age of its newest commit to be recorded with executed evidence before deriving, and the rows presently numbered 5 through 8 shall be renumbered 6 through 9 with their text unchanged — the quantified population being the eight rows of that table.
- **Validation:** `inspection`
- **Executed verification:** read of `phase-checklists.md` after the edit against M-7's pre-state (0 occurrences of the row's marker; the upstream-tip row is number 4; the table holds rows 1 through 8).
- **Numeric pass threshold:** the table holds **9** rows; the backup row is at position **5**, immediately after the upstream-tip row; the text of the **4** renumbered rows is byte-identical to their pre-state text; occurrences of the row's marker rise from **0** to **1**.
- **Acceptance criteria (informative):** placing it beside the upstream-tip row is the point — both are recorded-before-deriving conditions about the state of the remote, and separating them would let one be filled while the other is forgotten.

### LLR-88.16 — the flow's identity is re-declared after the last edit

- **Traceability:** HLR-88.8
- **Statement:** After the final source edit of this batch, the revision string and the aggregate hash in `FLOW-VERSION.md` shall be updated and the packaged bundle regenerated from the manifest table, and the revision-identity and bundle-pairing rules shall report current and paired — the quantified population being the manifest table of `FLOW-VERSION.md` and the derived bundle under `skills/dev-flow/`.
- **Validation:** `test`
- **Executed verification:** `python ~/.claude/docs/tools/devflow-validate.py .`, the revision-identity and bundle-pairing lines; the bundle regenerated by the validator's own sync command. Pre-state M-2: `[-] V7 FLOW-VERSION.md: flow current (9c1449ed815d267c)`.
- **Numeric pass threshold:** **0** identity findings; **0** unpaired bundle entries in either direction; **0** derived-file drift blocks. The revision string differs from `2026.08.24-rev46` and the hash differs from `9c1449ed815d267c`.
- **Acceptance criteria (informative):**
  - `PLAN.md` §5 budgets this: the previous revision bump raised four derived-drift blocks in the consuming repository, and the same is expected here. **The four-step procedure including the re-hash is the whole content of this increment**, and skipping step 4 is what produced the drift the batch opened with.
  - This LLR runs **last** by construction. Running it earlier means running it twice.

### Functional traceability (US → HLR → LLR → TC)

| Requirement | Method | Test case | Notes |
|---|---|---|---|
| HLR-88.1 | test | `TC-B88-01` — per-rule arm enumeration, 8 of 8 | the anti-vacuity criterion |
| HLR-88.2 | test | `TC-B88-02` — loader arms + live gate-line delta | 14 → 0, both sides measured |
| HLR-88.3 | test | `TC-B88-03` — census oracle arms + the live 276/280 pair | |
| HLR-88.4 | test | `TC-B88-04` — grammar arms over the constructed token table | the live corpus is blind (M-3) |
| HLR-88.5 | test | `TC-B88-05` — resolver arms + two-sentence arms | severity-blind, sentence-keyed |
| HLR-88.6 | inspection | `TC-B88-06` — manifest table read + empty CI diff | |
| HLR-88.7 | test | `TC-B88-07` — backup-rule arms, four states, two boundary sides | |
| HLR-88.8 | test | `TC-B88-08` — identity + pairing lines after the bump | |
| LLR-88.1 | test (unit) | `TC-B88-09` — ledger sweep re-run, 4-record regression floor | |
| LLR-88.2 | test (unit) | `TC-B88-10` — token-vs-substring arms | **synthetic by necessity** (P-7) |
| LLR-88.3 | test (unit) | `TC-B88-11` — wrapped-statement arms, both directions | |
| LLR-88.4 | inspection | `TC-B88-01` | shared with HLR-88.1 |
| LLR-88.5 | test (unit) | `TC-B88-02` | shared with HLR-88.2 |
| LLR-88.6 | test (unit) | `TC-B88-03` | shared with HLR-88.3 |
| LLR-88.7 | test (unit) | `TC-B88-03` | the notice-scope arm |
| LLR-88.8 | inspection | `TC-B88-12` — commit-order check on the re-baseline | |
| LLR-88.9 | test (unit) | `TC-B88-04` | shared with HLR-88.4 |
| LLR-88.10 | test (unit) | `TC-B88-05` | the resolver half |
| LLR-88.11 | test (unit) | `TC-B88-05` | the two-sentence half |
| LLR-88.12 | inspection | `TC-B88-06` | shared with HLR-88.6 |
| LLR-88.13 | test (unit) | `TC-B88-07` | the rule's own arms |
| LLR-88.14 | test (unit) | `TC-B88-07` | the arm-discipline half |
| LLR-88.15 | inspection | `TC-B88-13` — checklist table read, 9 rows, 4 byte-identical | |
| LLR-88.16 | test | `TC-B88-08` | shared with HLR-88.8 |

`TC-B88-*` and `AT-B88-*` are letter-initial ⇒ ungoverned by the id registry (P-26, measured
this session: **0** occurrences of three consecutive batches' prefixes over 1,373 registry lines,
`next_free` untouched). All are operator-local selftest arms, validator runs and file inspections —
**0 pytest files added**, and saying so is the honest form.

---

## 5 · Validation strategy

### 5.1 Methods

- **Layer A — white-box (`TC-B88-*`).** `test (unit)` = a named selftest arm block; `test` = a
  validator run over the live tree; `inspection` = a named file read with an observable condition.
  Every `test`/`analysis` requirement above carries both its executed verification and its numeric
  pass threshold.
- **Layer B — black-box (`AT-B88-*`).** The operator drives the shipped surfaces — the validator
  and the INTAKE checklist — and observes the sentences and numbers they print. No acceptance
  references a private symbol. `AT-B88-08` is labelled a **pin**, not a gate.
- **`demo` is used nowhere in this batch**, and no acceptance is discharged by observation alone.

### 5.2 Dual-traceability

Both chains are present and both are complete: the behavioral chain is §3's table
(9 rows, every story covered, every row carrying its measured pre-state), and the functional chain
is §4's table (24 rows: 8 HLRs and 16 LLRs, each with a method and a test case).

### 5.3 Batch acceptance criteria

1. **Every rule this batch creates or modifies carries a `PASS != NOOP` arm, and its GREEN arm
   asserts the PASS SENTENCE rather than the absence of a BLOCK.** **8 of 8**, enumerated per rule
   in `04-validation.md`, never claimed as a total. *(This is the widened form of the original
   criterion #1. The original — "no new rule on the selftest's synthetic-exemption list" — is
   retained below as criterion 2 and is **insufficient on its own**: P-5 measured a rule that sat
   off that list, carried arms, passed them, and was inert across 61 batches because its arm
   asserted "not red" over a branch that is never red.)*
2. **No rule this batch creates joins the synthetic-exemption set, which shall be DERIVED from
   `CHECKS` and the emitted arm labels rather than asserted in prose.** Membership **1** (`V8`)
   today and **1** at close, and the derivation carries a `PASS != NOOP` arm (`LLR-88.14`).
   **⚠ CORRECTION, and it corrects this document twice over.** As shipped there is **no list**:
   `devflow-validate.py` prints a hardcoded English sentence, nothing computes membership, and
   the selftest's `ok` flag is never influenced by it — so the criterion as originally written
   **could not fail**. Executed proof it was already false: the patched validator emits **9**
   `V8` arm lines where the shipped emits **0**, and *both* still print *"V8 is the ONLY rule
   this selftest does not prove synthetically"*, and both print `SELFTEST PASSED`. This session
   twice told the operator that criterion 2 was *"a real, checkable constraint, not a
   formality"*. **It was a formality.** `PDR-2026-08-25-batch-88#D3`. The remedy is the
   derivation above; criterion 1's `PASS != NOOP` mandate stands on its own merits and never
   depended on this one.
3. **0 BLOCK attributable to this batch's content**, stated **per run** and with its scope. The
   gate line's absolute value **will move inside this batch** — measured, `14 block → 0 block` for
   the loader increment alone (M-2, M-4) — so a single batch-wide number would be a splice of
   mutually exclusive states. Each increment records its own pre-state and post-state.
4. **The canon census reads 306 of 570 as measured 2026-08-25** (shipped reads 302 of 570; the
   **+4 delta** is the oracle move, and it is the half that survives re-measurement), and the
   restatement commit precedes every `REQUIREMENTS.md`-touching commit in this batch and the
   next. This batch touches `REQUIREMENTS.md` in **0** commits. ~~*reads 280 of 544*~~ superseded
   per `PDR-2026-08-25-batch-88#D4` — **the figure expired because this batch declared its own
   ids while the criterion was being written**, which is the denominator hazard biting the very
   criterion that measures it. **The date is part of the figure.**
5. **The non-conforming design-review citation set is ∅** — stated as a SET, because a count
   expires the moment this document cites another record — over a conforming population of
   **≥ 26** (floor measured 2026-08-25, M-3 re-executed); **AND** the grammar arms reject a
   hyphenated batch-directory token. ~~*All 21 live design-review citations still conform*~~ is
   superseded on both halves: the figure was stale, and **the first half is green under the
   grammar the operator rejected**, so it is a regression floor and is not accepted alone. After
   the C-56 de-minting (`PDR-2026-08-25-batch-88#D2`) **no live token discriminates the two
   grammars**, which makes the arms the only evidence this criterion has.
6. **The 4 historically-parsing validation records still parse** under the modified ledger rule —
   a regression floor beneath the improvement.
7. **Selftest exit 0, arm lines ≥ 240** against the measured floor of 192.
8. **0 files changed under `s19_app/`, `tests/`, `tools/`.** The product-repo edits are **two**,
   both declared as exclusions in `AT-B88-08`'s scope rather than hidden by a narrower diff
   command: `pyproject.toml` (the new `evidence` extra) and `.github/workflows/tui-ci.yml`
   (exactly **4** lines — the install rerouted, the three ad-hoc lines removed).
   ~~*and `.github/`*~~ **struck by operator ruling** (`PDR-2026-08-25-batch-88#D7`): the old
   criterion forbade the very edit that makes the declaration reach the installing job, so it
   was a criterion that could only be met by leaving the defect in place.
9. **Every user story has ≥ 1 passing acceptance test observing its outcome through the shipped
   surface, with boundary and negative evidence.** 2 of 2.
10. **Expected unowned-identifier notices, enumerated in advance:** `LLR-88.8` and `LLR-88.4` from
    this document (both constrain without transforming), plus the six inherited from batches 85, 86
    and 87 that the live run already names (M-2). Threshold = **0 UNEXPECTED**, never "0 notices".

---

## 6 · Open findings and decisions owed

### 6.1 Findings registered by this batch, not fixed here

| id | Finding | Evidence | Disposition |
|---|---|---|---|
| **R-88-1** | The validation-ledger rule was inert across 61 batches while 38 records' prose invoked it. The *history* is not repaired by fixing the rule — those batches remain human-asserted | M-5 | The fix lands (LLR-88.1); the **historical claims are not retro-verified** and this record says so rather than implying the fix reaches backwards |
| **R-88-2** | The acceptance-node rule has **no live instance in this repository** and will still SKIP after its fix, because this project's acceptance ids are letter-initial | P-7, M-2 | Registered. The rule is correct after the fix and unexercised here; the cost of the letter-initial convention is now measured on a second axis (batch-87 measured the first: id collisions inside a batch are caught by review or not at all) |
| **R-88-3** | The map-resolution defect is **latent**: no CI job and no git hook runs this validator | P-13, M-7 | Fixed at latent severity (LLR-88.10). The day the validator enters CI is the day it matters, and a batch closing audit gaps is exactly the batch that would put it there |
| **R-88-4** | The example-GIF test imports an imaging library with **0** import-skip guards and is exercised by CI's full-suite push job | M-7 | Not fixed here. Declaring the dependency (LLR-88.12) is the minimum; adding a skip guard is a `tests/` edit this batch's pin forbids |
| **R-88-5** | The requirement tokenizer harvests range notation and prose compounds as ids — **129** of 1,004 census rows, per `PLAN.md`'s seventh finding | inherited, **not re-measured this session** | Registered as **inherited and unverified**. It is Story E's territory and re-baselines with it in batch-89 |
| **R-88-6** | **`requirements.txt` is a third dependency-declaration surface, and it disagrees with the manifest twice**: it declares a **floorless** `textual` where `pyproject.toml` declares `>=8.0.2`, and it **omits `markdown-it-py` entirely** | **measured this session**, M-12 | **Not repaired here** — a fourth surface, beyond the operator's ruling. Currently harmless **by ordering alone**: CI installs it *before* `pip install -e .`, so the manifest wins. That is the shape of a defect that waits for someone to reorder two lines. Batch-89 candidate |

### 6.2 Decisions owed to the operator — I could not specify these without you

| id | Decision owed | Why Phase 1 cannot take it | What I did instead |
|---|---|---|---|
| **D-88-1** | **A rule number for Story B's review-marker guard.** The scope allocates `V24` and `V25`; the review-debt ledger's guard is a **third** new rule with no allocated number (P-25, ❓ UNDECIDABLE) | Minting a rule id the operator has not allocated is the same class of act as minting a phantom requirement id, and this session already caught one of those | **Story B is deferred to batch-89** (§0.4). Its design is captured in §7 **without ids**, so nothing is lost and nothing is minted |
| **D-88-2** | **Whether the three Part B interfaces are re-declared frozen for batch-89.** `docs/architecture.md:226-236` freezes all three **for `2026-08-24-batch-88` by string** (P-24, M-8) | If Story A lands in 89 against a freeze naming 88, the Layer C rule has **zero real consumers** and lands on a synthetic fixture — the exact shape acceptance criterion #1 forbids | Recorded as the precondition of Story A in §7. Not assumed either way |
| **D-88-3** | ~~Whether CI's ad-hoc imaging install is removed, and how~~ — **RULED BY THE OPERATOR 2026-08-25**, per `PDR-2026-08-25-batch-88#D7` | The trap as written down was real: removing the ad-hoc line while the declaration lived only in `dev` **would have broken CI's push job**, and the first proposed fix did exactly that. The ruling dissolves it — a **new `evidence` extra**, CI's install rerouted through it, the ad-hoc line removed. `dev` is excluded for a measured reason (M-12): it pins `textual==8.2.8` | **CLOSED.** `LLR-88.12` and `HLR-88.6` re-authored; criterion 8 and `AT-B88-08` widened to admit the `.github/` edit; `AT-B88-09` minted to observe it |
| **D-88-4** | **Whether the batch objective string in `state.json` is corrected.** It declares the superseded baseline **276** (P-4) **and** the refuted *"section 4"* anchor for the Layer C rule (P-19) | It is a declared objective; editing it is an operator act, not a Phase-1 derivation | LLR-88.8 specifies the baseline half as a requirement **conditional on this decision**; the "section 4" half is recorded as refuted and left standing |
| **D-88-5** | **Whether the ruling in §0 is accepted.** If Story A is kept in batch-88, §0.6 states its minimal safe form and its price: +3 increments, +2 source files, and V24's debut inside four oracle moves | The operator scoped the batch; a Phase-1 record can argue the cut and must not take it unilaterally | §0 argues it from measured counts and names what would change the recommendation. **This document specifies the ruled scope.** If the ruling is overturned, Story A's requirements are additive — continuing from the next free HLR number — and no id below is invalidated. **The successor number is deliberately NOT written here:** a forward reference to an id no heading declares is minted by the Atlas scanner as a citation-only id, which is the largest class in the frozen baseline census (426 of 1004). Caught by the id census at this station, +27 against 26 declared, and fixed at the SOURCE |

---

## 7 · Deferred to batch-89 — design captured, **no ids minted**

> **Deliberately id-free.** Writing `US-`, `HLR-` or `LLR-` prefixed identifiers for work that is
> not in this batch would register them in the census as declared-and-unreflected, which is the
> phantom-minting failure this session already caught once. The design below is prose; batch-89's
> Phase 1 authors the ids.

**Story A — Layer C component verification.** Rule **V24**, keyed on the **`Frozen?` column
header** and never on a section number (ARQ decision; the anchor lives at line 226 of the map while
the template calls the viewpoint §4 — measured, M-8). Its consumers are the three declared Part B
contracts, currently frozen **for batch-88 by string** — see D-88-2. Its hard precondition,
`_read_ci`, lands in this batch as LLR-88.10. Its arms owe the same `PASS != NOOP` discipline as
every rule above.

**Story B — declared limits of assurance, plus the review-debt ledger.** Two parts, and the second
is what the sealed design record has not seen (§0.4):

1. **Declared limits of assurance** — a sibling to the existing user-evaluation declaration in the
   close template (measured: **0** occurrences of independence or assurance-limit language there
   today, M-7). Three fixed lines, as the design proposal specified.
2. **The human-review marker, designed as a REVIEW-DEBT LEDGER rather than a boolean.** Every batch
   proceeds autonomously and **accrues** review debt; the marker **discharges** it. Recorded
   fields: the reviewer's name (`Javier Granados`), an optional `reviewer-id` reserved for
   organisational context, the **date**, the **phase** at which review happened — which may be
   early or immediately before the final commit — and `authored-by` **alongside** `reviewed-by`, so
   a one-sided sign-off is visible at a glance rather than inferable. Anything the review turns up
   goes to the backlog, never silently into the batch.
   **The guard rule must not claim to verify the review's QUALITY.** Its whole assertion is that a
   review **occurred, by whom, and when** — and its sentence must say that, because a rule whose
   description overreaches its behaviour is the exact family of defect this batch exists to close.
   That rule needs a number: **D-88-1**.

**Story E — canon seeding.** Re-baselines at **280 of 544** (measured, M-4), not 276, and its
tranche oracle is the census delta printed by the fixed rule. Deferring it was the operator's own
ruling and this batch discharges its precondition: the oracle moves **here**, once, recorded.

---

## 8 · Appendices

### 8.1 Phase-1 reconciliation log

No reconciliation event has occurred: this is the first authoring of this record and no locked
requirement has been amended. Should a threshold or statement change at the reconciliation gate, a
per-decision audit table lands here with the columns `Decision ID | What changed | Parent HLR
re-read? | Body edit landed?`, and the §4 body edit is written **before** the row that points at it.

### 8.2 Requirement amendments (Before / After · Deleted / New)

None yet. The two amendments this document *carries* are amendments to **inputs**, not to locked
requirements, and both are recorded in place rather than applied silently:

- **`full.diff`'s design-review grammar → REJECTED and replaced** by the strict form (P-11,
  LLR-88.9). *Before:* a batch segment admitting any hyphenated alphanumeric component. *After:* a
  single alphanumeric component plus an optional explicit version marker. *New tokens:* the
  rejection arms on a hyphenated batch-directory citation, which are the only part of that
  requirement the live corpus can be tested against.
- **`PLAN.md`'s framing of the imaging dependency → CORRECTED** (P-18, LLR-88.12). *Before:*
  "undeclared". *After:* declared in a CI workflow, undeclared in the manifest; the defect is the
  home, and removing the workflow declaration is forbidden until D-88-3 is taken.

### 8.3 Evidence checklist

| Item | ✓/✗ | Evidence |
|---|---|---|
| Constraints stated explicitly | ✓ | §0.1–§0.2: source-file budget, family-E threshold, both measured against the scope |
| ≥2 alternatives considered | ✓ | §0.5 (proposed vs ruled scope, tabulated); LLR-88.9 (three candidate grammars, M-3); LLR-88.10 (rename vs resolve, with the citation cost of each) |
| Recommendation tied to constraints | ✓ | §0.3 — four oracle moves against the operator's own already-taken sequencing ruling |
| Risks listed | ✓ | §6.1 (five findings), §6.2 (five decisions owed), LLR-88.12's CI trap |
| Cost estimated | ✓ | §0.5 table: 6→4 files, 10→7 increments, 3→2 max per increment |
| Diagram | ✗ | N/A — the flow is a linear increment sequence; §0.5's table carries it without a diagram earning its place |
| What would change the recommendation | ✓ | §0.6, three named conditions |
| Two-layer requirements | ✓ | §3 (8 acceptances, each with a first-class Acceptance block and a boundary catalog) + §4's two traceability tables; both chains complete |
| Every probe EXECUTED at draft time with pre-state | ✓ | §2.8, M-1 through M-10 — 10 transcripts, all run this session against the live tree; M-10 is the live positive control obtained by authoring this file (`14 block → 4 block`, same tree, minutes apart) |
| Premise table, one row per premise, tiered, executed | ✓ | §2.7 — 26 rows, **19 TRUE · 6 FALSE · 1 UNDECIDABLE**, every verdict carrying a command output or a `file:line`; the count itself was re-derived mechanically from the verdict column after a miscount, and the miscount is recorded beneath the table |

### 8.4 Working-file note (C-44) — foreign work observed, not touched

`.dev-flow/2026-08-24-batch-88/01b-qa-validation-plan.md` (48,782 bytes, mtime `2026-08-24 23:38`)
appeared in the active batch directory **during this authoring session** and was written by neither
this session nor its brief — the directory held only `PLAN.md` when this session opened it. Per C-44
it is **reported, not swept**: it has not been read into this record, edited, committed or removed.
Its `AT-B88-*` id space may overlap this document's, which is the fork batch-87 §3 had to reconcile
by mapping; **that reconciliation is owed before the PDR station** and is not attempted here,
because reconciling against a document this session did not author would mean silently adopting it.
