# Phase 2 — Cross-agent review — batch-46

> Triple review (architect · qa-reviewer · security-reviewer) of `01-requirements.md` + `01b-qa-strategy-and-verification.md`, run 2026-07-15. **Outcome: ITERATE-TO-REFINE → FOLD (autonomous) → re-gate APPROVE.** No blockers; the two load-bearing mechanisms (D1 CSS-reuse, app-side reparent-safety) are independently VERIFIED. 7 MAJOR + 9 MINOR/hardening, all in-spec fixes. C-21 re-reconciliation triggered (AT-064c added → increment cut re-derived).

## Verified sound (no action — affirmed by review)
- **D1 CSS-reuse mechanism (architect).** `#patch_editor_panel` is composed inside `_compose_screen_patch()`, a direct child of the `#workspace_body` Container (`app.py:1288-1291`). The descendant combinator `#workspace_body.width-narrow #patch_editor_panel …` fires at any depth → zero Python needed. **Mechanism real.**
- **App-side reparent-safety (architect).** `app.py` has **0** hits for `patch_pane_*` / `patch_doc_file_row` / `patch_paste_row`. Every leaf id the app queries is in the must-preserve list. **No missed app-wired id.**
- **§6.5 A-1 stale-text reconciliation accurate** (live `grid-size: 2 4` / `1fr 2fr 2fr auto`, `styles.tcss:806-808`); SUPERSEDE/AMEND/NOTE dispositions correct; no orphaned LLR.
- **No `shall`/`should` misuse** (only the convention line uses `should`, informative).
- **`_fully_visible` rejects 0-area regions** (`r.area == 0 → False`) → the reachability side has no vacuous pass (qa).
- **Snapshot drift = exactly 2 patch cells**, per-cell reasoned, `xfail(strict=False)`; **C-28 correctly not-triggered** (patch bindings `show=False`, `app.py:801/807/824/825`) — affirmed by all three.
- **Security: layout-only, no new external surface** (F5 clean); window titles are CONSTANTS (F3); `security_required: TRUE` only because the reparent could regress an existing C-17 guard — covered.

## MAJOR findings + dispositions (folded into the spec)

| # | Source | Finding | Disposition (FOLD) |
|---|--------|---------|--------------------|
| **M-A2** | architect | Increment cut can't hold a green ≤5-file boundary — retiring pane ids in the source increment reds the other census tests (atomic unit ~7 files). | **DESIGN CHANGE — preserve, don't retire.** Keep `patch_pane_entries`, `patch_pane_changefile`, `patch_pane_variant`, `patch_doc_file_row` as **non-scrolling grouping sub-containers** inside the 3 new windows. `test_tui_patch_variant.py` + `test_tui_directionb.py` then stay GREEN unchanged. Atomic unit = 5 files (see re-cut below). No >5-file exception. |
| **M-A1** | architect | TC-035.2 asserts `#patch_pane_variant` children == `[patch_variant_row, patch_execute_row]`; retire → `NoMatches`. `patch_variant_row`/`patch_execute_row` unclassified. | Resolved by M-A2's preserve: `patch_pane_variant` survives with its 2 children → **TC-035.2 passes unchanged**. Classify `patch_variant_row` (:2488) + `patch_execute_row` (:2502) **MUST-PRESERVE**. |
| **M-A3** | architect | LLR-063.2 `w ≤ host//3` hard-locks equal thirds; wide ratio is NOT operator-locked (only the narrow STACK is D1); forces relocating variant/execute. | **Relax LLR-063.2:** "each window within its measured content need, sum ≤ host, non-overlapping, **≥ MIN_USABLE_W**." Allow asymmetric `grid-columns: 2fr 1fr 1fr` (keeps variant/execute local in PATCH SCRIPT). Ratio = a Phase-3 pilot-measured design call, recorded then. |
| **M-Q1** | qa | Geometry oracle has no **minimum-usable-size floor** → a window starved to width ~3 passes distinct-x + upper-budget; R-1 undetectable. | **Add `MIN_USABLE_W`/`MIN_USABLE_H`** per-window floor to AT-063a/063b (`assert r.width >= MIN_USABLE_W and r.height >= MIN_USABLE_H`), floor **pilot-measured at both regimes** (C-23). Closes the 0-width-passes-distinct-x hole too. |
| **M-Q2** | qa | AT-063c routes observably only for PATCH SCRIPT + CHECKS; JSON + variant/execute are **presence-only** (violates the doc's own C-10). | **Strengthen AT-063c:** JSON window → seed `#patch_paste_text` valid change-set, `request_action("parse_pasted")`, assert an **observable effect** (entries table populates / status changes); variant/execute → assert an observable outcome, not `resolves`. |
| **M-Q3** | qa | R-TUI-046 preserved outcome (paste first line in-viewport @scroll 0) **lost its verifier** — A-3 and 01b §6 name different, non-asserting destinations. | **Add `#patch_paste_text` to the `_fully_visible` named set** (predicate is widget-generic) AND a dedicated in-viewport assertion at scroll 0. **Reconcile A-3 ↔ 01b §6** to the SAME target (JSON-window in-viewport TC). |
| **M-Q4** | qa | Revealed docked save-back / before-after buttons are only an **optional, non-gate-blocking** sub-check → a regression trapping the revealed row below a fold ships green (the exact B2 class). | **Add gate-blocking AT-064c:** reveal `#patch_saveback_row` + `#patch_before_after_row`, pause, assert `_fully_visible` on `patch_saveback_confirm/decline/width_button` + `patch_before_after_button` at 80×24. (C-21: new AT → re-reconcile cut — done below.) |

## MINOR / hardening (folded)
- **F1 (security) — C-17 markup-safety across reparent.** Add to TC-46.3: `assert app.query_one("#patch_checks_status", Label)._markup is False` + same for `#patch_doc_issues`; and cite the backstops `test_tui_patch_editor_v2.py::AT-051e` (hostile `zz[red]boom[/red]` kind) + `test_loadfilescreen_input.py::test_ac2h` (unbalanced bracket) as the C-17 net. **Preservation note:** the Inc-2/id-set edit must NOT weaken AT-051e (queries leaf ids only → should be untouched; confirm at gate).
- **F3 (security) — window titles.** Keep titles CONSTANT ("PATCH SCRIPT / CHECKS / JSON EDIT"). If the implementer uses Textual `border_title` (markup-parsed in 8.2.8), safe ONLY because constant — never interpolate a filename/symbol into a title.
- **F4 (security) — CappedTextArea.** Add to TC-46.3: `assert isinstance(app.query_one("#patch_paste_text"), CappedTextArea)` (64 KiB paste cap must not downgrade to stock TextArea). Backstop `tests/test_capped_text_area.py` (static census, not in touched set) also guards it.
- **q-m1 (qa) — window-id mismatch.** Canonical = `patch_win_script/_checks/_json` (architect-final). Reconcile 01b prose (`patch_window_*`) → `patch_win_*`.
- **q-m2 (qa) — AT-063a/b RED capture.** On the current tree the new window ids are ABSENT, so the RED trips on cardinality-0 (`all(counts[w]==1)` fails), not "2 distinct x." **Key the RED-capture counterfactual to the pane-lead ids (`#patch_pane_*`)** for a true 2-vs-3 discriminator, OR document the actual RED path (id-absent). Note: with M-A2 preserving the pane ids, the counterfactual can assert the OLD grid geometry via the preserved pane ids directly.
- **q-m3 (qa) — TC-46.1 layout token.** Don't pin `layout.name == "grid"` while the wide token is open (M-A3 allows horizontal or grid). Make TC-46.1 **layout-agnostic** (assert the 3-window structure + width-narrow rule, not a specific `layout` name); the AT geometry oracle already is.
- **a-m1 (architect) — 14 vs ~46 ids.** State that the **14 are the wiring-critical subset** explicitly guarded by AT-063c/TC-46.3; the remaining census ids are covered by the increment gate (`pytest -q`).
- **a-m2 (architect) — unclassified intermediate containers.** Classify `patch_doc_entry_inputs` (:2372), `patch_doc_entry_buttons` (:2359), `patch_history_controls` (:2370), `patch_paste_controls` (:2529), `patch_saveback_buttons` (:2543), `patch_before_after_buttons` (:2563). CSS-referenced ones (`styles.tcss:997,1005,848,1006`) need **lockstep rename** inside the source increment or their rules go dead — preserve the ids (they carry no test-order pin; safest to KEEP).
- **a-m3 (architect) — duplicate `#patch_saveback_row`.** `:838` (`column-span: 2`) becomes DEAD once the grid is removed → **drop the `:838` rule**; the `:999`-block rule (width/height/padding under the grouped selector) **survives** (adjust as needed for the JSON window). Reconcile both CSS blocks (801-852 + 985-1009) in the source increment.

## Re-derived increment cut (C-21 — AT set changed: +AT-064c; design changed: preserve pane ids)
- **Inc-1 (RED-first, 5 files — the atomic structural green unit):**
  `screens_directionb.py` (compose → 3 windows `#patch_win_script/_checks/_json`, each = title Label + scrollable body + docked button-row sibling(s); **preserve `patch_pane_entries/changefile/variant` + `patch_doc_file_row` as non-scrolling sub-containers**; relocate `patch_checks_controls` into the CHECKS window; move `patch_paste_text`+saveback+before/after into the JSON window) + `styles.tcss` (3-window wide layout, `#workspace_body.width-narrow #patch_editor_panel` vertical reflow, docked-row rules, reconcile the duplicate `#patch_saveback_row` + both CSS blocks) + `test_tui_patch_layout.py` (supersede AT-033a/b + tc_pane_styles + tc319 + at058a → **AT-063a/b/c + AT-064a/b/c + new layout-agnostic TC-46.x**, min-usable floor, observable routing, RED keyed to preserved pane ids) + `test_tui_snapshot.py` (`_batch46_patch_drift_marks`, 2 cells `xfail(strict=False)`) + `test_tui_patch_editor_v2.py` (retarget 2× `scroll_end("#patch_pane_entries")` → PATCH-SCRIPT window body; markup-safety + CappedTextArea assertions in the census; confirm at057a/at058b green).
- **Inc-2 (docs, 2 files):** `REQUIREMENTS.md` (add R-TUI-063 / R-TUI-064; §6.5 amend R-PATCH-2X2-LAYOUT-001, R-PATCH-2X2-SNAPSHOT-001, R-TUI-046; NOTE R-PATCH-VARIANT-SELECT-001) + `01-requirements.md` traceability closeout.
- **DROPPED from the touched set (M-A2 preserve):** `test_tui_patch_variant.py`, `test_tui_directionb.py` — pass unchanged (their pane-id assertions hold). Confirm at the Inc-1 gate via the full-suite run.

## Axis verdict
- **Coverage** — was the gap: M-Q3 (lost paste verifier), M-Q4 (revealed buttons unguarded). FOLDED (added in-viewport TC + AT-064c). Now complete.
- **Certainty** — was the gap: M-Q1 (no min-usable floor → vacuous pass), M-Q2 (presence-only routing), q-m2 (wrong RED narrative). FOLDED. Every AT now non-vacuous with a real RED.
- **Evidence** — mechanisms verified with `file:line`; census dispositions concrete. Complete.

**Post-fold: 0 blockers, 0 unresolved MAJOR → re-gate APPROVE. Advance to Phase 3 with the re-cut plan.**
