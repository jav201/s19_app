# Increment 001 — R-TUI-114 · `Seed the living canon: screen_workspace IFC mirror`

| Field | Value |
|---|---|
| Batch | `2026-08-24-batch-86` |
| Increment | `001` (the batch's only authoring increment) |
| Lane (if the batch forked) | none — single lane |
| Requirement(s) | `R-TUI-114` (new) mirroring `US-86-1`, `HLR-86.1`, `HLR-86.2`, `HLR-86.3`, and `LLR-86.1` through `LLR-86.8` |
| Acceptance | gate G6 (live): per-id grep loop · `devflow-validate` V22 drop + `0 block` · source-scope pin |
| Agent | `software-dev` (supervised-incremental-development) |
| Date | `2026-08-24` |

---

## 1 · What changed

The living canon now reflects batch-86. A new section
`## screen_workspace information contract — batch-86 IFC surface #2 (R-TUI-114)` was inserted
into `REQUIREMENTS.md` immediately before `## Retired ids` (after the batch-85 R-TUI-113
mirror, its style baseline). It mirrors — never duplicates — the canonical record at
`.dev-flow/2026-08-24-batch-86/01-requirements.md` §5: the What (control C-54, surface #2,
`SURFACE:` value quoted verbatim, parent of the pilot's `loaded_panel`), one-line mirrors of
`US-86-1`, of `HLR-86.1`, `HLR-86.2`, `HLR-86.3`, and of `LLR-86.1` through `LLR-86.8`
taken from their actual headings, an honest Status
(record ACCEPTED at P2 after one iteration — 1 blocker + 6 major + 7 minor discharged, moved
figures re-measured per the C-39 rider; V12 `PARENT` balancing now CHECKED; `workspace_body`
recorded as the next undeclared PARENT; 30 outputs · 106 resolved consumers · M-10-guarded
census), and the R-TUI-114 monotonic allocation basis. No literal widget addresses (no `#...`
query strings) appear in the section, so it adds no V13 reacher pairs. The Atlas derived
files were regenerated at the gate (`--atlas --write`) because the canon edit changes
ATLAS-TRACE realm columns; V20 confirms `atlas current (4 files, census 2)`.

---

## 2 · Files modified

**The budget counts SOURCE files only. Tests are not capped. Product docs and `.dev-flow/**` are outside the count.**

| File | Kind | Change |
|---|---|---|
| `REQUIREMENTS.md` | doc | new R-TUI-114 canon-mirror section inserted before `## Retired ids` (+32 lines) |
| `.dev-flow/_derived/ATLAS-TRACE.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |
| `.dev-flow/_derived/ATLAS-ORPHANS.md` | doc (derived) | regenerated by the validator at the gate (V20 digest-guarded) |

| Count | Value |
|---|---|
| **SOURCE files** | **0 / 4** — REQUIREMENTS.md is a doc; no code touched |
| Test files | 0 (uncapped) — tests untouched |
| Doc files | 3 (outside the count) |

---

## 3 · How to test

```bash
cd "C:\Users\jjgh8\Github\s19_app\.claude\worktrees\dev-flow-68a67d"

# 1 — per-id grep loop: every batch-86 heading id present in the canon (>= 1 each)
for id in "US-86-1" "HLR-86.1" "HLR-86.2" "HLR-86.3" "LLR-86.1" "LLR-86.2" "LLR-86.3" \
          "LLR-86.4" "LLR-86.5" "LLR-86.6" "LLR-86.7" "LLR-86.8"; do
  printf "%-10s %s\n" "$id" "$(grep -cF "$id" REQUIREMENTS.md)"; done

# 2 — validator: V22 unreflected drops 288 -> 276, final line 0 block
#     (if V20 blocks on stale Atlas: regenerate with --atlas --write, then re-run)
python ~/.claude/docs/tools/devflow-validate.py "C:\Users\jjgh8\Github\s19_app\.claude\worktrees\dev-flow-68a67d"

# 3 — source-scope pin: the batch's source diff is empty
git diff --stat $(git merge-base HEAD origin/main) -- s19_app/ tests/ tools/ pyproject.toml
```

---

## 4 · Test results

**One complete run each; outputs pasted from those runs, not stitched.**

| Layer | Nodes | Result |
|---|---|---|
| **0 · unit** | none — doc-only increment, no code | n/a |
| **A · white-box** | none — no TC in this increment's scope | n/a |
| **B · black-box** (gate G6, live) | 12-id grep loop · validator · source pin | all three PASS (transcripts below) |

**Gate 1 — per-id grep loop (all 12 >= 1):**

```
US-86-1    1
HLR-86.1   1
HLR-86.2   1
HLR-86.3   1
LLR-86.1   1
LLR-86.2   1
LLR-86.3   1
LLR-86.4   1
LLR-86.5   1
LLR-86.6   1
LLR-86.7   1
LLR-86.8   1
```

**Gate 2 — validator.** V22 before: `288 of 531` (the P2-gate frozen figure; this session's
own runs are all post-edit — the delta is corroborated by arithmetic: 276 + the 12 ids seeded
here = 288). First post-edit run: `2 block` — both V20 Atlas-staleness rows tripped by this
canon edit, as anticipated, cleared by `--atlas --write`. Review F1 then found that writing
this packet re-staled the Atlas again (`.dev-flow/**` is corpus input — the tree read
`2 block · 254 notice · 14 not applicable` with the packet present), so after the F2/F3/F4
fixes the Atlas was regenerated over the fixed corpus and the validator re-run; the fresh
`ATLAS-TRACE.md` holds 0 hits for the RED-arm's corrupted token and the US/HLR/LLR family
is back to `1422 unique id(s)` (`ATLAS-TRACE.md:1383`). Final run, reproduced on the
shipped tree at this exact packet content:

```
  [-] V20  .dev-flow/_derived/: atlas current (4 files, census 2)
  [!] V22  REQUIREMENTS.md: 276 of 531 batch-declared ids are not reflected in the living canon (HLR 75 · LLR 193 · US 8; e.g. HLR-007a, LLR-001.6, US-B63-1) — the seeding backlog, retired one tranche at a time
0 block · 254 notice · 15 not applicable
```

**Gate 3 — source-scope pin:** `git diff --stat $(git merge-base HEAD origin/main) --
s19_app/ tests/ tools/ pyproject.toml` produced empty output, exit 0. `git status
--porcelain` (re-run 2026-08-24 at the review fix, per F4):

```
 M .dev-flow/2026-08-24-batch-86/PLAN.md
 M .dev-flow/_derived/ATLAS-ORPHANS.md
 M .dev-flow/_derived/ATLAS-TRACE.md
 M .dev-flow/state.json
 M REQUIREMENTS.md
?? .dev-flow/2026-08-24-batch-86/03-increments/
```

`state.json` and `PLAN.md` are orchestrator-owned batch state (P3 entry — legitimate, not
this increment's writes); the untracked directory is this packet itself. Nothing under
`s19_app/ tests/ tools/ pyproject.toml`.

### RED counterfactual — executed, not predicted

| Field | Value |
|---|---|
| Mutation applied | the `.` in the canon section's `LLR-86.7` replaced by an underscore (sed on REQUIREMENTS.md); the corrupted token is not reproduced here — it is scannable and the Atlas would adopt it as an id |
| Where it ran | my own worktree (`dev-flow-68a67d`), no other lane reading it |
| Transcript | `mutated: 6081: …` (the corrupted token, followed by) `the component's INPUTS` · `RED probe: grep -cF 'LLR-86.7' -> 0` · `exit=1` — the id-loop gate goes RED when an id is absent |
| Restore proven by | sha256 returned to pre-mutation value `177b9311a2b757d30c6ac8612c2196607ceac6fb817c6f1b5c9762a66d8860bd` (note: sed normalized CRLF→LF file-wide; endings re-added, byte-identity then confirmed by hash — `git status` alone would not have caught it) |
| Bytecode cache | n/a — no Python module touched or imported from a mutated state |
| Arms resolved at baseline | 12 — the loop prints exactly 12 rows; a missing row is visible in the pasted table |
| Verdict granularity | per id (one grep per id), never a single process exit code |
| Arms that stayed GREEN | the other 11 ids stayed 1 under the mutation, as they must — the mutation targeted only `LLR-86.7` |

### Load-bearing emptiness — what is this resting on that is only true today? (C-55)

| Field | Value |
|---|---|
| Does any claim here rest on the tree holding NO instance of some case? | yes: "no `#...` literal widget addresses in the new section" (the V13 reacher constraint) |
| If the result is an ABSENCE, what made the search wide enough | the validator's own V13 census over the merged corpus — the final run reports no new pair attributed to REQUIREMENTS.md beyond the pre-existing ones; verdict count unchanged at `0 block` |
| Guard labelled as protecting a CONCLUSION, not a behaviour | V13 itself (corpus rule, runs every validation) — no new local guard added |
| Conjunctive criteria: one mutation per conjunct | the G6 gate is 3 conjuncts; conjunct 1 (id presence) mutated RED above; conjuncts 2–3 are validator/git measurements whose RED arms live in the batch's selftest (189 arms, P2-verified), not re-mutated here |
| Synthetic instance of the absent case | not built — the absent case (`#` literal in the section) is prevented by authoring and detected by V13 on every future run |
| Positive control for the ABSENCE probe | V13 currently reports the batch record's own declared pairs (36 + pilot 4) on the same run that shows the canon section adding none — the probe demonstrably finds pairs where they exist |
| Second absence — the headline CHECKED claim | "no V12 row names `loaded_panel`" IS the claim that the pilot's `PARENT` balancing is CHECKED: V12's affirmative summary is suppressed when any other notice fires, so the claim is provable only by this absence. Width: V12 runs over the merged corpus. Positive control: the `screen_workspace` / `workspace_body` NOT-checked notice on the same run proves V12 still emits NOT-checked rows where they apply |

### Reverse census — trigger family B

| Probe | Command | Result |
|---|---|---|
| B1 symbols asserted by **other** tests | `grep -rl "R-TUI-114" tests/` | did NOT fire — 0 hits; the id is new |
| B2 file moved on disk | no file moved | did NOT fire |
| B3 byte-identical golden captures this source | no golden captures REQUIREMENTS.md | did NOT fire |
| B4 artifact produced here is consumed elsewhere | FIRED — REQUIREMENTS.md is read by `devflow-validate` (V13/V20/V22) and `tests/test_id_registry.py` (G4 anchors on `## Retired ids`, which this edit did not move or rename — the section landed before it, heading intact) | validator run above = the real consumer executed; source pin confirms tests untouched and the suite ledger position unchanged |

| A3 | interface consumed by another module changed | no code interface touched | did NOT fire |

### Signed-balance test ledger

`post = base − deleted + added` → post = base − 0 + 0 ✓ reconciles (no test nodes added or deleted; the suite is byte-identical to merge-base per the source-scope pin)

---

## 5 · Risks

- The Status bullet freezes P2-era figures (30 outputs · 106 consumers · 36-pair census). If
  a later batch re-measures and they move, this canon section must be re-pointed — same
  maintenance class as every other mirror; the canonical text stays in the batch record by
  design, which bounds the drift to one summary bullet.
- The "288 before" V22 figure was not re-measured pre-edit in this session (the edit landed
  first); it rests on the P2 gate record plus the exact-12 arithmetic. If the P2 figure were
  wrong, the drop size would be wrong, but the shipped state (`276`, `0 block`) is measured.
- sed-induced CRLF→LF normalization during the RED arm was caught and reverted by hash; a
  future editor using sed on this CRLF file will hit the same trap.

## 6 · Pending items / spec deviations

None. (The `workspace_body` undeclared-PARENT successor is recorded in the record and
mirrored in the canon section — it is batch-scope backlog, not this increment's.)

## 7 · Suggested next task

Phase 4 validation: execute the kill-mutation plan — M4, M5-copy, M7 — against the shipped
record + canon per `01b-qa-validation-plan.md`.

---

## Increment gate checklist

| # | Item | ✓/⚠/✗ | Evidence (node id · command output · file:line) |
|---|---|---|---|
| 1 | ≤4 source files, or reason declared | ✓ | 0 source files; §2 table |
| 2 | Tests written in this same increment | ⚠ | none — doc-only increment; the live gate G6 (grep loop + validator + pin) is the verification, executed in §4 |
| 3 | Layer 0 written where the criterion applies | ✓ | criterion does not apply — no function authored |
| 4 | RED counterfactual captured **and restored by hash** | ✓ | §4 RED table; sha256 `177b9311…` restored |
| 5 | Reverse census run on every touched symbol | ✓ | §4 B1–B4/A3 table; B4 fired and its consumer was executed |
| 6 | `code-reviewer` passed — a HIGH blocks | ⚠ | not run — no code diff; the canon text falls under the batch's P2 double-review + orchestrator re-gate |
| 7 | No file from another lane touched | ✓ | single lane; `git status --porcelain` in §4 Gate 3 — my writes are `REQUIREMENTS.md`, the two `_derived/` files, and this packet; `state.json`/`PLAN.md` are the orchestrator's |
| 8 | Frozen interfaces untouched (or returned to the trunk) | ✓ | source pin empty over `s19_app/ tests/ tools/ pyproject.toml` |
| 9 | Coverage claims verified **on disk**, not from intent | ✓ | all §4 outputs pasted from real runs on this tree |
| 10 | Load-bearing emptiness declared, with its synthetic instance (C-55) | ✓ | §4 C-55 table (absence guarded by V13, positive control cited) |
| 11 | Mutation verdicts recorded **per arm**, inert arms named (C-40 rider) | ✓ | §4: per-id granularity, 11 GREEN arms named as expected-GREEN |

**An item without a citation is not satisfied — it is asserted.**
