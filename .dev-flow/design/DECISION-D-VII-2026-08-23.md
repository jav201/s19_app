# D-VII RULED — the living human artifacts are TWO PLANES, not one answer

> **Ruled 2026-08-23 by the operator.** Question posed by
> `HANDOFF-atlas-ifc-2026-08-22.md` §4.0 / §7 D-VII: are the living human artifacts
> (requirements, TC, checklists, traceability) DERIVED by command, or AUTHORED and
> coherence-CHECKED? The handoff blocked all `--atlas` work on this ruling.

## The ruling

**Hybrid — the tension dissolves because D-I and the 2026-08-23 declaration rule over
DIFFERENT artifacts:**

| Plane | Artifact | Regime |
|---|---|---|
| **Index** | the Atlas (`.dev-flow/_derived/ATLAS-*.md`) | **DERIVED by command** (reading (a)) — D-I stands as ruled. Guarded by digest (`V20`, both directions), `DERIVED — DO NOT EDIT` banner |
| **Canon** | `REQUIREMENTS.md`, living TC/checklist/traceability documents | **AUTHORED by hand** (reading (b)) — as `REQUIREMENTS.md` already is at 6,074 lines. The machinery owed here is a **coherence rule** against the IFC, NOT a builder and NOT a digest guard |

## Consequences

1. **Handoff §4.2, §4.3, §5.2 stand as written** — they were provisional pending this
   ruling and they apply to the *index* plane only. The non-duplication rule (§4.3: the
   Atlas emits headings, ids, paths, URLs, computed numbers — never sentences) is what
   keeps the two planes from colliding.
2. **The coherence rule for the canon plane is NEW, SEPARATE scope** — not part of any
   Atlas increment. It verifies that hand-authored living documents agree with the IFC
   (and with the batch increments that feed them). To be designed after the Atlas ships;
   register in `BACKLOG-CODE.md` when scoped.
3. **The 2026-08-23 "living documents" declaration is implemented on the canon plane:**
   requirements/TC/checklists/traceability accumulate as standing hand-authored documents,
   batch records become their increments. "The consolidation IS the artifact" — the
   consolidated *canon* is authored; the consolidated *index* over it is derived.
4. **Unblocked:** handoff §9 action 1 — freeze the Atlas field set by execution
   (`tools/atlas_probe.py`, this same date). Still blocking Atlas *adoption*: D-III
   (third artifact-selection semantics) per §7.

## Provenance

- Question: `HANDOFF-atlas-ifc-2026-08-22.md` §4.0 (the two readings), §7 row D-VII.
- Ruling channel: operator answer to a structured question, session of 2026-08-23,
  option "Híbrido: dos planos".
