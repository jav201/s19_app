# s19_app — BACKLOG · LANE B: engineering process (controls · skills · rules)

## PRIORITIZED INDEX — THIS LANE'S OPEN WORK, DERIVED 2026-09-03

> **This index is a POINTER, not a record.** It carries a band, one line, and where to read the item — **never the item's evidence.** `~/.claude/docs/NEXT-SESSION.md:4-6` states why in the imperative: *"Do not duplicate it here — a summary beside the thing it summarises is the defect this project has now found six times."* Copy a figure up here and it goes stale against the item three feet below it. **If a line here disagrees with its item, the item wins.**
>
> **Priorities are cross-lane.** An operator choosing what to do next does not care which file an item is filed in, so the bands below are the same bands [`BACKLOG-CODE.md`](BACKLOG-CODE.md) uses — both files declare the identical legend (`P0` next · `P1` high · `P2` medium · `P3` low). **That lane's top items are cross-referenced at the end of this index** so the whole set can be read from either file.

### How this was ordered — stated so the next person can RE-DERIVE it rather than trust it

1. **Band first, as each item declares it:** `P0` → `P1` → (code lane only) `MAJOR / HIGH` → `P2` → `P3`.
2. **Within a band, four tiebreaks, applied in this order:** **(i) blocks the next action** — it stops the very next flow step, a gate or a sync or a close, rather than merely being undone; **(ii) time-bounded** — its evidence expires (a reflog window, a still-reachable SHA), so the cost of waiting is not zero; **(iii) falsifies a green signal** — it makes a passing check untrustworthy, which outranks undone work because a vacuous check corrupts every future measurement *including the measurement that would close the other items*; **(iv) bounded cost** — among equals, the one with a measured one-line fix goes first.
3. **The bands are NOT re-triaged.** Every band below is the band its own item declares. Only the items the 2026-09-03 pass touched were re-measured; the rest are indexed at the priority they were filed under, however old. **An index is not an audit and this one does not pretend to be one.**
4. **Scope of the listing:** `P0` and `P1` are listed **individually and completely**. `P2` and `P3` are **rolled up by count with a section pointer** — a 300-line index is the artifact nobody reads, which is the problem this index exists to solve.
5. **Pointers use each item's OWN identifier** where it has one (`R-89-6`, `R-88-17`, `S-4`, `F-8`…) and a `§section → "opening words"` address where it does not. **No new id scheme is minted here**; these files identify items by provenance tag and opening sentence, and this index does the same.

### ⚠ Read this before using the band order

**If the next action is CLOSING batch-89 — which it is — then two `P1`s outrank both lanes' `P0`s operationally, and the band order does not show it.** `PROC-P1 · sync-P6` and `PROC-P1 · batch-89-unclosed` are the close path itself: the sync a close requires **cannot pass** for a `core` batch, and the close artifacts do not exist. Everything else in either lane can be worked on with batch-89 still open; the close cannot. **Band-first is the declared order, so it is what the index prints — this paragraph is the tension named rather than silently resolved.**

### `P0` — this lane: **1**

| id / pointer | one line | where |
|---|---|---|
| **`R-89-6` + `R-89-7`** | `V2` has been vacuous on this project all along, on two orthogonal axes — the rule reports green over a population it never harvested. | §*Routed from batch-88 and batch-89* → *"`V2` HAS BEEN VACUOUS…"* |

*Tiebreak note: nothing competes with it in-band, but it would win tiebreak (iii) against most of the `P1`s below — a vacuous rule is the one defect that makes the other items' closure evidence unreadable.*

### `P1` — this lane: **13**

*Ordered by the four tiebreaks above. The `(i)`/`(ii)`/`(iii)`/`(iv)` tag on each row says which tiebreak placed it, so a disagreement can be argued at the criterion instead of at the ordering.*

| id / pointer | one line | tiebreak | where |
|---|---|---|---|
| **`sync-P6`** *(the other half of `S-4`)* | `/dev-flow-sync`'s ACTIVE branch demands a last station of `P6`; every `core` batch ends elsewhere, so the check is wrong for the only mode this project uses. | **(i)** | §*Opened 2026-09-03* → *"CANNOT PASS PRE-REQUISITE 2…"* |
| **`batch-89-unclosed`** | The batch is merged and unclosed; `V28` makes the hole permanent the instant batch-90's directory opens. | **(i)** + **(ii)** | §*Routed from batch-88 and batch-89* → *"BATCH-89 IS UNCLOSED…"* |
| **`R-88-19`** | A probe is a measurement, and a measurement in the record is not a constraint until something checks the implementation against it. | **(iii)** | §*Routed from batch-88 and batch-89* → *"A PROBE IS A MEASUREMENT…"* |
| **`R-88-12`** | The named-mutation mandate is unverifiable as written — it requires a comment, and nothing in the selftest can read a comment. | **(iii)** | §*Routed from batch-88 and batch-89* → *"THE NAMED-MUTATION MANDATE…"* |
| **`no-canonical-mutation-harness`** | Every increment writes a throwaway, so mutation results cannot be re-run or accumulated. | **(iii)** | §*Routed from batch-88 and batch-89* → *"THERE IS NO CANONICAL MUTATION HARNESS…"* |
| **`code-reviewer-absent`** | No increment of batch-89 had an independent `code-reviewer` pass — 6 of 6 packets say `ABSENT`, and skipping it changed no verdict. | **(iii)** | §*Opened 2026-09-03* → *"NOT ONE INCREMENT…"* |
| **`R-88-17`** | A correction has a population and nothing in this flow enumerates it — the most repeated defect of batch-88. | **(iii)** | §*Routed from batch-88 and batch-89* → *"A CORRECTION HAS A POPULATION…"* |
| **`F-8`** | A *fix* is where this project's dominant defect class reproduces most reliably, and no test can catch it. | **(iii)** | §*Routed from batch-79* → *"`F-8` — a FIX is where…"* |
| **`C-40`/`C-42`-fire** *(USER STORY, batch-64 §7.12)* | Measure whether `C-40` and `C-42` actually fire, or admit they are paragraphs. | **(iii)** | §*Deferred control-encodes / batch-64 carries* → *"MEASURE WHETHER C-40 AND C-42…"* |
| **`per-node-counterfactual`** | A counterfactual verdict must be reported per resolved node id, never per mutant. | **(iii)** + **(iv)** | §*Registered by batch-76's merge-gate closure* → *"a counterfactual verdict must be reported…"* |
| **`evidence-bytes-verbatim`** *(control candidate)* | Bytes committed as evidence must be stored verbatim and the store verified, not assumed. | **(iii)** | §*carries opened by batch-66* → *"bytes committed as EVIDENCE…"* |
| **`carry-input-side`** *(control candidate)* | A deferral born in a prototype, ADR or audit never reaches the queue, because carry-over fires only at batch close. | **(iii)** | §*carries opened by batch-66* → *"a deferral written in a PROTOTYPE…"* |
| **`tui-design-separability`** | The `/tui-design` skill has no control-separability guard, and user testing found the gap before the skill did. | **(iv)** | §*operator-flagged 2026-07-28* → *"no CONTROL-SEPARABILITY guard…"* |

### `MAJOR`, `P2` and `P3` — this lane: **2**, **19** and **12**, rolled up

**The two `MAJOR`s** are §*MAJOR — there is no AT/TC registry* → *"THERE IS NO AT/TC REGISTRY…"* (batch-63 `OB-2`, operator-identified) and §*flow-manifest drift* → *"batch-74 reached MERGE with…"*. They are placed between `P1` and `P2` by rule 1's note.

`P2` and `P3` are not listed individually, by rule 4 above. They live in: §*Routed from batch-88 and batch-89* (the newest, incl. `R-89-8` template fields, `R-89-3` the forensic-register preamble, and a four-item compact-carries bullet) · §*Routed from batch-79* · §*Control candidates raised and DECLINED* · §*Deferred control-encodes* · §*MAJOR — there is no AT/TC registry* · and the batch-64/66/74/75/76 carry sections. **The newest `P3` opened at this pass** is `ifc-set-repr` — three `--selftest` arms print an unsorted `set` repr, so the transcript is not byte-reproducible and any future arm that diffs it fails on noise (§*Opened 2026-09-03*).

### Cross-lane — [`BACKLOG-CODE.md`](BACKLOG-CODE.md)'s top, by id

*Cross-referenced, not copied; read them there. That lane also carries a legacy `MAJOR / HIGH` band the shared legend does not define — this index places it between `P1` and `P2`, which is a decision and not a reading.*

| band | id / pointer | one line |
|---|---|---|
| `P0` | **BATCH-79 CHARTER, Lane 1** | Command-bar deletion `Inc-6`…`Inc-12`, fully specified — execute, do not re-derive. |
| `P1` | **BATCH-82 CHARTER (Lane A half)** | Author the Information Flow Contract for s19's surfaces — the full retrofit. |
| `P1` | **`C-77-l`** | The aggregation path; load-bearing for ruling `R-10`. |
| `P1` | **AT/TC registry blindness** | The registry cannot see 106 batch-78/79 acceptance nodes. |
| `MAJOR` | **`R-TUI-098` residuals** (3) + **batch-75 `F4`** (3) | Report-producer bounding that is relocated, reduced or disclosed rather than removed. |
| `P2` | **`suite-gitignored-state`** *(opened 2026-09-03)* | The suite's failure count depends on gitignored files, so it cannot be trusted to reproduce. |
| `P3` | **`artifacts-capture-nondeterminism`** *(opened 2026-09-03)* | 3 of 15 pilot cases capture a different `ProgressBar` ETA field run to run. |

---


> **Canonical open-work queue for the PROCESS lane.** Split out of [`BACKLOG.md`](BACKLOG.md) on 2026-07-27 by operator ruling; that file is now the router and lineage archive. Scope: engineering controls, the global `/dev-flow` command (`~/.claude/commands/`), the project `docs/engineering-rules.md`, the `tui-design` skill, the TUI course, and the AT/TC id registry. Application code and its tests/CI live in [`BACKLOG-CODE.md`](BACKLOG-CODE.md) — do not add them here.
>
> **Control-encode rule:** every control needs its own AskUserQuestion before it is encoded (`feedback_devflow_control_encode_approval`). Global flows stay project-agnostic — stack-specific controls go to `docs/engineering-rules.md` (`feedback_devflow_general_flows_project_agnostic`). Note the global `/dev-flow` command lives OUTSIDE this repo, so that leg is not covered by the project PR flow and needs its own before/after record in the batch artifacts.

> **Last refresh: 2026-09-03 (PRIORITIZATION + CLOSURE PASS — no batch; the head of this file now carries a cross-lane prioritized index)**
> — flow **`2026.08.31-rev55`**, four revisions past the `rev51` that refresh cited and three past the `rev52` its header names. `origin/main` tip **unchanged at `a2a2534`**; the work rides `claude/batch-89-lean-contract`, **5 commits ahead and pushed** (`a679ee6` · `c4e6fe5` · `ed03b10` · `c118f6e` · `cf24e29`), **no PR open**. **batch-89 is STILL UNCLOSED** — no `04-validation.md`, no `05-close.md` — so this is again a hand refresh at no batch's close, the second in a row, which is itself the standing `P1` on that subject.
>
> **What this pass wrote here.** A **Prioritized index** at the head of the file, derived from the items rather than restating them — band, one line, pointer, and **no evidence copied upward**, because `NEXT-SESSION.md:4-6` forbids exactly that. The index states its own ordering criterion so it can be re-derived. It also **names the tension it could not resolve by rule**: band-first puts two `P0`s above the two `P1`s that are the batch-89 close path, and that paragraph says so instead of quietly reordering.
>
> **Closed here, each with the evidence that closed it, none of it taken from the changelog alone:** the `P1` environment item's **head** (`HLR-89.4` + `HLR-89.5` were built — `V30` plus three preflight lines, rev55, `cf24e29`; verified by executing `--selftest`: exit 0, **565 arms**, `SELFTEST PASSED`, 27 of 27 mutants killed) · its **limb (a)** (rev54, `c118f6e` — and rev54 found **three** defects in those four lines, not the one the limb named; live `text=True` call sites now **0**, with 5 prose mentions, which is the letter of the verify clause **unmet** and its substance met, stated as such) · its **limb (d)** (rev55's probe, closed against what the limb said would close it, with the two-element-domain clause **re-filed open** because `N7-case-probe-is-assumed` survived on a machine that folds case). rev53's PEP 701 parse failure is recorded there too, unasked-for and belonging to the same subject.
>
> ⚠ **Amended rather than closed, and the corrections are the point.** **limb (b)** re-verified **still open** — `FLOW-VERSION.md`'s verify route is still bash-only, `grep -cin 'powershell|git bash'` → **0**, across four revisions that closed its two sibling limbs. **limb (c)** still open **but its central measurement is FALSIFIED**: the 2026-08-30 *"`s19env` appears NOWHERE in the flow canon, 0 hits"* now returns **4**, put there by rev53 and rev55 — which is the routing violation the limb itself forbade, arriving from the direction it did not expect; the project-side half (`docs/engineering-rules.md`) is still empty. The mutation-harness `P1` re-measured at **0 files, unchanged**, now with **42 throwaway mutants across rev52/54/55** behind it. The batch-89-unclosed `P1` re-measured: **six** increment packets (was four), no batch-90 directory, so `V28`'s clock has **not** started.
>
> **Opened here — three items, none previously in either lane, each verified by execution at this pass:** `/dev-flow-sync`'s ACTIVE branch **cannot pass pre-requisite 2 for any `core` batch** (the hardcoded `P6` half of `S-4`, which rev51 left standing beside the key-name half it fixed — it has never fired because every sync so far took the SUPERSEDED branch, and batch-89's close is its first live subject) · **no increment of batch-89 had an independent `code-reviewer` pass**, 6 of 6 packets say `ABSENT` in their own words · **three `--selftest` arms print an unsorted `set` repr**, reproduced by two runs and a diff (**3 differing lines of 565**, no verdict moved).
>
> ⚠ **An incident inside this pass, recorded because C-44 says working-file damage is reported and not tidied away.** A write to this file failed mid-truncate on a lone-surrogate codepoint and left it at **0 bytes**. Restored from `HEAD` — back to exactly **137,003 bytes / 446 CRLF / 0 bare LF**, byte-identical to the pre-pass state — and the pass re-applied. **Every subsequent write in this pass encodes to a buffer FIRST and then `os.replace`s the target**, so a bad codepoint can no longer truncate a live queue. **Nothing was lost and nothing was committed.**
>
> **Last refresh: 2026-08-30 (batch-88 + batch-89 routed here BY HAND — neither batch closed, and that is exactly why this file was six days stale).** `origin/main` tip = **`a2a2534`** (2026-08-30). The flow is now **`2026.08.31-rev52 · bceb968c4975fe13`** (was `2026.08.30-rev51 · a8d3c4ce91f1b0c6` at this refresh; rev52 closed the `_active_batch_dir` item below) (`~/.claude/docs/FLOW-VERSION.md:19`) — **six revisions** past the `rev45` the previous refresh recorded. Before this refresh the file contained **zero** occurrences of `R-88-`, `R-89-`, `batch-88`, `batch-89`, `V25`, `V26`, `V27`, `V28` or `V29`.
>
> ⚠ **THE CAUSE IS STRUCTURAL, AND IT IS THE SAME DEFECT THESE TWO BATCHES SPENT THEMSELVES ON.** The routing rule that writes Lane-B carries into this file fires **at batch CLOSE** — `~/.claude/commands/dev-flow.md:544` (*"Backlog reconciliation (MANDATORY close step — the carry-over contract)"*), reinforced at `:548` (*"the batch is NOT closed until the canonical backlog reflects it"*) and `:565`. **batch-88 did not close until its `04-validation.md` and `05-close.md` were written retroactively on 2026-08-29**, a day after its merge (`0f40624`, PR #203, from station P3), and both say so at the top. **batch-89 has not closed at all** — it holds neither file today. So two batches produced their findings through a mechanism that only files them at a phase that never ran, and nothing noticed, because *the step that would have noticed is the step that did not happen.* **Nothing routed them; this refresh did, by hand.**
>
> **This is the input-side carry-over hole, measured a second time and in a NEW direction.** The entry below — *"NEW — 2026-07-31: the backlog-carry control has a hole on the INPUT side"* — covers deferrals born **before** a batch (a prototype verdict, an ADR, an audit). This is the mirror of its mirror: a batch that **produced** its carries and never reached the step that files them. `feedback_backlog_carryover_enforced` makes reconciliation a mandatory close step; it has no reading for *a batch that does not close*. **And that is not hypothetical here** — `V28` was shipped this fortnight for precisely the adjacent property, and measured that **9 of 72** batch-shaped directories under `.dev-flow/` hold neither or only one closing artifact (batches 25, 63, 66, 71, 73, 75, 78, 79, 85).
>
> **What this refresh writes:** one new section immediately below — *"Routed from batch-88 (2026-08-24) and batch-89 (2026-08-28)"* — carrying the **closures with the evidence that closed them** and the **open items**. Every item states what is wrong today, what closes it, and how you verify it closed, per this file's own admission rule; anything that could not state all three was left out and reported instead. **Nothing below is encoded as a control.** Every candidate still owes its own AskUserQuestion per the control-encode rule above, and unless an entry says otherwise its **C-45 landings read command ❌ · template ❌ · catalog ❌ · pushed ❌**. ⚠ Standing debt now spans **four** batches: 86 (1 candidate), 87 (4), 88 and 89 — and the flow shipped **five new rules** in the same fortnight without one of those candidates being encoded.
>
> **Earlier refresh: 2026-08-24 (batch-87 close — four Lane-B carries WRITTEN here per the routing rule).** `origin/main` tip = **`943e54a`** (unchanged; batch-87 rides `claude/batch-87-ifc-reauthor-surface-3`, 7 commits, **none pushed**). batch-87 (code lane: D-II re-author + IFC surface #3 `workspace_body`) surfaced **four** PROCESS items. **All four are registered and NONE is encoded** — C-45's four landings read **command ❌ · template ❌ · catalog ❌ · pushed ❌** for every one of them, stated here so a candidate cannot masquerade as a control. The flow stays at **`2026.08.24-rev45 · 09bea075fc183f8b`**, and `~/.claude` was measured **clean and fully pushed** at the batch-87 close (`git status --short` empty, `git log --branches --not --remotes` empty) — which is the evidence that nothing landed there. Each still needs its AskUserQuestion before encoding, per this lane's control-encode rule.
>
> **(1) FLOW-REPO DEFECT — `R-87-1`: a parent component declaring no OUTPUTS silently switches off the emits half of balancing for EVERY child it has.** Measured origin, batch-87 M-4 arm 5: with `workspace_body` declared carrying zero OUTPUTS, `screen_workspace`'s emits BLOCK **vanished entirely** — V12 printed no finding for it at all. Rule read at `devflow-validate.py:484`: `if child_out and parent_out:` — the emits half is skipped when **either** side is empty. The rule cannot distinguish *"this component emits nothing"* from *"this component did not say"* — the same `None`-versus-empty distinction `_ifc_consumers` was written to preserve for `consumers`, and which is **absent for `OUTPUTS`**. This is a **check that cannot fail** (C-55 shape), and declaring an output-less parent is the cheap form that buys a green gate for nothing; batch-87's `D-87-B` rejected it and paid 42 outputs instead. **Cheap fix sketched in the record:** treat an **omitted** `OUTPUTS` as *unknown* (NOTICE, balancing not checked) and an **explicit empty** one as a claim. **C-45 landings: command ❌ · template ❌ · catalog ❌ · pushed ❌.** Cite: `.dev-flow/2026-08-24-batch-87/01-requirements.md` §6 (`R-87-1`) and §2.8 (P-11).
>
> **(2) HARNESS RULE — never invoke the gate suite through `conda run`; call the environment's interpreter directly.** Measured origin, batch-87 Phase 4: `conda run` buffers the child's stdout and re-prints it through a `cp1252` writer. The suite emits `U+FFFD` in at least one assertion diff and the re-print dies with `UnicodeEncodeError` at position 29589; conda then offered to file a crash report and the run exited 1 with **no test summary at all** — 8 KB of conda diagnostics where a 40-minute suite result should have been. **The whole first gate run's evidence was destroyed and it had to be re-run from scratch.** The working invocation sets `PYTHONIOENCODING=utf-8` and calls `<env>/python.exe -m pytest` directly. This **supersedes** the command printed in batch-87's `01b-qa-validation-plan.md` §7, which is now kept as the refuted BEFORE text under an amendment block. **Two homes, and it needs both:** the project's `docs/engineering-rules.md` (the env-specific half — this is a Windows/`cp1252`/conda fact about **this** gate environment, and global flows stay project-agnostic) **and** the flow's **C-19 family** (the general half: *a wrapper that re-encodes a child's stdout can destroy the evidence the run exists to produce* — the same class as the tail-pipe and the ANSI-blind grep, both C-53 probe defects). **C-45 landings: command ❌ · template ❌ · catalog ❌ · pushed ❌.** Cite: `.dev-flow/2026-08-24-batch-87/04-validation.md`, probe-incident note and gap `G4-03`.
>
> **(3) PORTABLE CONTROL CANDIDATE — flaky-node disposition by CLASS MEMBERSHIP, with rates recorded as FIGURES.** Measured origin, batch-87 gate `G4-01`, and it is the strongest of the four because the machinery caught **its own prior batch's vacuous check**. The pre-registered rule batch-87 was handed was a **whitelist of six node ids** plus the diagnosis *"every one passes in isolation"*. Both halves were refuted by execution: **5 of the 6 failing nodes were off-list** (one of them a node the plan explicitly named and EXCLUDED), **5 of the 6 pre-registered nodes did not recur**, and re-sampled at **N = 10** the one on-list node that did recur **fails 4 of 10 alone**. A single isolated pass on a node that passes 60 % of the time is a coin landing heads, not a diagnosis. **The criterion adopted verbatim at that gate, and the portable form:** a FAILED node is *pre-existing* when **(i)** the source-scope diff against merge-base is **empty**, **and (ii)** the node is re-run **N ≥ 10** times in isolation on the same tree with its failure signature a member of the **recorded family**, **and (iii)** its isolated failure **rate is recorded as a figure**, never as the word *"passes"*. A rate of **0 of 10** is *order-dependent*; a **non-zero** rate is a *timing race* — a worse and **separately tracked** class. Two grounds for portability: **C-53** (the `n = 1` binary had just false-failed correct work) and *the figure over the narration*. ⚠ **A whitelist under-covers by construction** — the failing population resamples every run — so the portable rule must be **class membership**, not an id list. **C-45 landings: command ❌ · template ❌ · catalog ❌ · pushed ❌** (it lives today only in one batch's `01b-qa-validation-plan.md` §7 as a Before/After amendment, which is an instance discharge, not a landing). The measured node ids and rates are lane A — see [`BACKLOG-CODE.md`](BACKLOG-CODE.md) carry (1).
>
> **(4) C-56 TOKENIZER FOLLOW-UP — the scanner still mints bare stems from backtick-quoted family tokens.** batch-86 routed the *evidence-transcript-is-corpus-input* candidate here (item (1) of the batch-86 block below) and the flow's rev45 tokenizer fix addressed the suffixed-id shape. **A residual class survives, measured at batch-87:** a backtick-quoted family token — an id followed by a stray dot or a wildcard where the member number would be — is still reduced by the Atlas id regex (`devflow-validate.py:1575`) to a **bare stem with no allocation**, which the Atlas then adopts as an id row. Census at batch-87: **8 authored feeders across 3 distinct phantom stems**, all three already present in `ATLAS-TRACE.md` at `:1565`, `:2384`, `:2394`. The **flow-side** half is the scanner regex (this lane); the **corpus-side** half — the authored feeders, all in frozen record sections — is lane A carry (2). ⚠ The authoring rule that pairs with it: **a family should be named in prose, never as a quoted wildcard**, because writing it mints a phantom every time. **C-45 landings: command ❌ · template ❌ · catalog ❌ · pushed ❌.** Cite: `.dev-flow/2026-08-24-batch-87/03-increments/increment-001.md` §6.
>
> ⚠ **Standing debt this close makes visible:** batch-86 registered **1** control candidate unencoded; batch-87 registers **4** more, all unencoded, and batch-86's own is **still** unencoded. **Five candidates, zero landings, two batches.** Per the template's own warning the signal to watch is the reverse — several consecutive batches finding *none* — but a catalog that keeps **finding** and never **encoding** is the other failure, and it is the one this project has now.
>
> **Earlier refresh: 2026-08-24 (batch-86 close — two Lane-B carries WRITTEN here per the routing rule).** `origin/main` tip = **`a112eeb`**. batch-86 (code lane, IFC surface #2) surfaced two PROCESS items:
>
> **(1) NEW CONTROL CANDIDATE — an evidence transcript is corpus input.** Origin, measured: the Inc-1 increment packet quoted the RED-counterfactual's corrupted id verbatim and used dotted-range shorthand; the Atlas id-scanner adopted THREE phantom ids, and one `--atlas --write` away they were permanent in the committed derived plane. The C-40 restore obligation must extend to every scannable artifact that QUOTES the mutation (describe mutations by position/operation, never spell the token), and range shorthand is forbidden where an id-scanner reads. Owed: the four C-45 landings (command · template · catalog · push) at a future flow rev — batch-86 discharged only the instance. Needs its AskUserQuestion before encoding, per this lane's control-encode rule.
>
> **(2) P0-scaffold rule candidate:** a batch scaffolded WITHOUT its own `01-requirements.md` is judged on batch-01's frozen doc by `_artifacts()`'s walk fallback (measured at batch-86 open: 17 V4 blocks). The Phase-0 artifact (the §2.6 intake block) must be born with the batch — one sentence in the flow's Phase-0 map would close it.
>
> **Earlier refresh: 2026-08-01 (batch-77 Phase-3 handoff — a CODE-lane batch routing one Lane-B carry here).** `origin/main` tip = **`f8747b8`**. batch-77's own work is reconciled in [`BACKLOG-CODE.md`](BACKLOG-CODE.md); **one item belongs to this lane and is WRITTEN here rather than left as a pointer**, per the routing rule. See the new section immediately below.

## 🆕 Routed from batch-88 (2026-08-24) and batch-89 (2026-08-28) — routed by hand 2026-08-30, because neither batch closed

> **Sources, so every item below is actionable without reopening the batch:**
> `.dev-flow/2026-08-24-batch-88/01-requirements.md` §6.1 (findings `R-88-1` … `R-88-19`) and §6.2 ·
> `.dev-flow/2026-08-24-batch-88/04-validation.md` · `.dev-flow/2026-08-24-batch-88/05-close.md` §3–§6 ·
> `.dev-flow/2026-08-28-batch-89/01-requirements.md` §2 (premises `P-1` … `P-13`) and §6 (`R-89-1` … `R-89-10`) ·
> `.dev-flow/2026-08-28-batch-89/01-requirements-ledger.md` · that batch's four increment packets ·
> and `~/.claude/docs/FLOW-VERSION.md` rows **rev47–rev51**, used **only to date what CLOSED** — its
> 51-row changelog is the flow's own record and is deliberately not duplicated here.
>
> ⚠ **Line numbers into `~/.claude/docs/tools/devflow-validate.py` were re-measured on 2026-08-30 and
> several had moved since the batches cited them** — that file grew from 402 to 516 selftest arms across
> rev47–rev51. Where an item cites it, the **SYMBOL is the anchor** and the line is the address on that
> date, per `R-88-9`'s own standing lesson and batch-89's `LED-89.2`.

**▸ ✅ CLOSED by these two batches — with the evidence that closed each one.** *A backlog that only grows is a list, not a queue.*

  - **▸ ✅ (CLOSED, rev46 → rev47) NINE live validator rule defects repaired.** `05-close.md` §1: `V22`'s oracle (**substring → token membership**) and its scope-naming message, landing first by operator ruling; `V23`'s strict design-review grammar plus its harvester; `V5`'s ledger sentence; `V8`'s resolver and root message; `V2`'s token membership; `V6`'s statement blocks; and **the `_artifacts` loader made SCOPED rather than merely preferring.** The loader was the sharpest and is worth keeping in view: rev39 had made the declared batch win by *overriding* a whole-tree walk, **and an override is not a scope** — every basename the declared batch did not hold kept whichever copy `os.walk` reached first. Measured on s19_app 2026-08-27, `art["04-validation.md"]` was **byte-identical to `.dev-flow/2026-05-05-batch-01/04-validation.md`**, so **`V5` was judging a document from May 2026 at every gate that batch ran.** Now the map is built from the declared directory alone and a missing basename is **omitted entirely**, never handed back as an empty string — *a mapping is the only return type where ABSENCE CAN BE FORGED.* Read at `devflow-validate.py:3604-3620` (docstring of `_artifacts`).
  - **▸ ✅ (CLOSED) FIVE new rules shipped: `V25` · `V26` · `V27` · `V28` · `V29`.** `V25` (rev47, NOTICE, selector `S4`) — a reachable remote plus days since the newest commit on any remote-tracking ref, answering RC-2. `V26` (rev48) — the live-contract / append-only-ledger pairing, compared in **both** directions. `V27` and `V28` (rev50) — the `decisions_log` correspondence and currency, and *the batch a newer batch superseded holds its closing artifacts*. `V29` (rev51) — the log in the single active slot is the **active** batch's. Selftest arms **402 → 443 → 490 → 516**; registered rules **26 → 27**. Verify: `~/.claude/docs/FLOW-VERSION.md:19` reads `2026.08.31-rev52` (it read `2026.08.30-rev51` when this row was written; arms 516 → 524 at rev52).
  - **▸ ✅ (CLOSED, rev49) the `--selftest` cp1252 crash.** **Reproduced before a line of repair was written, on two surfaces** (`increment-002.md`; batch-89 `P-8`): with `PYTHONIOENCODING` unset and stdout redirected, exit 1 on `−` after **12** arm lines; under `PYTHONIOENCODING=ascii`, exit 1 on `·` — the separator in every arm line — after **0**. Both printed **no verdict line**, which is what a genuine failure also looks like, in the one mode carrying CI authority. **The inherited cause was wrong and the measurement corrected it:** not block buffering, the ENCODING. Fixed **at the stream** — `_harden_streams` sets `errors=backslashreplace` at `__main__` — and deliberately **not** by ASCII-ing the fixture, because the offending character is one instance of a class: measured over the manifest's 22 canon rows, **534 non-cp1252 characters of 28 kinds**, and against ASCII **2511 non-ASCII of 38**. batch-89 marks `R-89-4` **CLOSED 2026-08-29** in its own findings table.
  - **▸ ✅ (CLOSED, rev49) the `python3` interpreter trap, and `V17` now EXECUTES rather than stat-ing.** `python3` resolves to the Microsoft Store alias in `WindowsApps` — a real file, on `PATH` — which **exits 49 without starting Python**. `_guard_path_resolves` tested `os.path.isfile` and stopped, so `python3 ~/.claude/hooks/flow-guard.py` printed `SKIP: guard wired on UserPromptSubmit` **while every rule the guard enforces went unenforced.** Three shebangs now say `python`; `V17` executes the declared interpreter and reads back a marker. **`~/.claude/.github/workflows/flow-selftest.yml:40` KEEPS `python3` by ruling** (`LED-89.10`): that job is `ubuntu-latest`, the runner's provision of a bare `python` was **not measured**, and an unmeasured premise is no licence to change a working CI line — so the threshold became *agreement among the four LOCAL sites*, never *a name runs*. ⚠ **The lessons catalogue already carried this trap and the flow shipped `python3` anyway** — *learning a lesson and encoding it are different acts*, which is the argument for the C-45 landings this file tracks.
  - **▸ ✅ (CLOSED, rev50 + rev51) the `decisions_log` rollover — this closes `G5-05`, batch-88's own best-candidate gap, and `R-89-9`.** `V18` verified that `state.json` **declares** a batch and nothing checked that its log **corresponds** to anything, so *a log that exists and a log that is current were the same picture to every rule that shipped.* batch-88 paid twice in one batch: the log froze on 2026-08-27 with no entry for increments 3–7, the batch **found and repaired the identical freeze mid-flight** (`0e3445a`, finding F-D), **and it re-froze** before the merge. `V27` now checks correspondence in **both** directions and currency against the newest commit touching the batch directory (asked of git **with a pathspec**). rev51 adds `/dev-flow` **§Batch rollover** — a retire-or-carry table over every single-slot field, ruling that outgoing entries are **ARCHIVED into the closing batch's own record and MOVED, never copied** (`.dev-flow/<outgoing>/decisions-log.json`), then the slot resets to `[]`, written by **exactly one act**: the rollover, before `batch_id` changes. `V29` is what executes it. The live instance was repaired by hand 2026-08-30 (`increment-006.md` §5).
  - **▸ ✅ (CLOSED, rev51) `/dev-flow-sync` gains a batch-id argument, and the copy list becomes DERIVED — `S-1` … `S-4`.** ⚠ **Accounting correction, stated rather than smoothed over: batch-88's `05-close.md` §4 numbers only `S-1` and `S-2`.** `S-1` — step 4 named `.dev-flow/02-review.md` and `.dev-flow/05-postmortem.md` while this project writes `02-review-security.md` and `05-close.md`, so **two artifacts were dropped with no error and no warning**, and the `05` mismatch was live for **every** batch. `S-2` — `PLAN.md` and `01b-qa-validation-plan.md` were named in **no mode at all**. The third defect in §4 is prose, not a numbered id: `state.json` is single-slot, so the sync **could only ever sync the active batch** and batch-88 became unsyncable the instant 89 opened. **`S-3` and `S-4` are numbered only in `FLOW-VERSION.md` rev51:** `S-3` is the batch-id argument (`state.json` becomes the DEFAULT source, not the only one; the SUPERSEDED branch **writes nothing to `state.json`**, so the vault destination folder is that batch's flag); `S-4` is that **pre-requisite 2 tested `current_phase >= 6`, a key this project's schema does not have** — it uses `current_station` — so the condition **never failed, never passed and never ran**, for every batch here, and always had. The copy list is now *everything in the batch directory minus exclusions that are PRINCIPLES*, with a REPORT classified by role and every unfilled REQUIRED role a WARN. **No rule was minted, by ruling** — a derived list cannot drift, and the residual risk is an operator declining to run a command.
  - **▸ ✅ (CLOSED at rev51, and it leaves an open residual — see the harness item below) the mutation harness's SUBSTRING verdict, which produced a FALSE SURVIVOR SWEEP.** Its verdict parser searched the output for the substring `SELFTEST PASSED`, which **rev49's `ENC VERDICT-live` arm prints mid-run from a child process** — so the first pass scored **all twelve applicable mutants SURVIVED at exit=1**: *the exact shape mutation testing exists to refuse, inside the instrument that refuses it.* The verdict now comes from the **FINAL `SELFTEST` line**. **Dating, because it matters for which past results are suspect:** the defect is **latent at rev48** (no such mid-run child print existed yet), **live from rev49**, and **rev51's harness is immune by construction.** ⚠ Any mutation tally taken between rev49 and rev51 by a harness reading that substring is unsafe; batch-89 Increment 1 already records **two intermediate tallies that do not reconcile** (12 named + 12 review = 24 scored against a final battery of 19) with *"Reconciliation: ABSENT"* and **no harness transcript surviving in either repository**.
  - **▸ ✅ (CLOSED) `G5-03` — the source pin was measured after all.** The gap recorded it unmeasured *"never picked up by a later station because there was no later station."* The observation exists in the close itself: `git diff --name-status 4131a38 0f40624` returns **23 files, 0 of them `.py`** (`05-close.md` §1), and the batch's code changes landed in `~/.claude` and its nested `skills` checkout, not here. Closed on that evidence, **with the honest caveat that it was taken at the close and not at the gate** — which is `G5-02`'s complaint one file over.

**▸ STILL OPEN — routed here, newest evidence first.** *Nothing below is encoded; each candidate owes its AskUserQuestion.*

> **➕ Opened 2026-09-03 (flow `2026.08.31-rev55`) — three items, none of which existed in either backlog before this pass.** They sit at the top because this section declares *newest evidence first*; for the cross-lane priority order, read the **Prioritized index** at the head of this file. Each was verified by execution at this pass, not carried from a session note.

  - **▸ (P1, NEW — measured 2026-09-03) `/dev-flow-sync` CANNOT PASS PRE-REQUISITE 2 FOR ANY `core` BATCH, AND IT WILL FIRE THE MOMENT batch-89 CLOSES.** The ACTIVE branch of pre-requisite 2 requires the LAST station to be **`P6`, or `6`** (`~/.claude/commands/dev-flow-sync.md:37-40`, verbatim: *"require the LAST station — `P6`, or `6` — with `phase_status` in {`approved`, `awaiting-sync`}"*). **`.dev-flow/state.json` declares `"mode": "core"` with `"stations_active": ["P0", "ARQ", "P1", "PDR", "P3"]`** — so this batch's last station is **`P3`**, and `P6` is not in its station set at all. A `core` batch that closes correctly, at its own declared final station, fails the check.
    **This is the OTHER HALF of `S-4`, and rev51 repaired only the first half.** `S-4` found the condition testing `current_phase >= 6` while this project's schema uses `current_station`, and the command's own text records the consequence: the condition was *"unevaluable for every batch in that project and always had been — it never failed, never passed, and never ran."* rev51 fixed the **key name** and left the **hardcoded `P6`** standing beside it. So the repair converted a condition that never ran into a condition that runs and is **wrong for the only mode this project uses**.
    **Why it has never fired, stated so nobody concludes it is harmless:** every sync so far took the **SUPERSEDED** branch, which does not read `state.json` at all — it judges the directory for a `05-*` artifact. The ACTIVE branch has had no live subject. **batch-89 will be the first**, and `state.json` currently reads `"current_station": "P3"`, `"phase_status": "in_progress"`.
    **What closes it:** the ACTIVE branch derives the closing station from **`stations_active[-1]`** rather than comparing against a literal — the file already carries the authority, and `dev-flow.md:575` says so in as many words (*"`stations_active` is the authority on which exist"*). A literal `P6` is a second inventory of the station set, which is the defect class `FLOW-VERSION.md` records five times over. **Do not fix this by adding `P3` to the literal** — that mints a third mode-to-station mapping.
    **How to verify closed:** with `state.json` set to `mode: core`, `stations_active: […,"P3"]`, `current_station: "P3"` and `phase_status: approved`, `/dev-flow-sync` reaches pre-requisite 3 instead of stopping at 2; **and** a `full`-mode state ending at `P6` still passes; **and** a state whose `current_station` is NOT its `stations_active[-1]` is still refused. **Three cases, because a check that only admits is not a check** — and the middle one is the regression the fix can silently break.

  - **▸ (P1, NEW — a PROCESS observation, not a code defect; measured 2026-09-03) NOT ONE INCREMENT OF batch-89 HAD AN INDEPENDENT `code-reviewer` PASS — 6 of 6, and the packets say so themselves.** Every one of `.dev-flow/2026-08-28-batch-89/03-increments/increment-00{1..6}.md` carries an **`### Independent review`** section reading **ABSENT**. Quoted, because the packets are already honest about it and the finding is what the honesty reveals: increment-001 — *"No `code-reviewer` verdict on an increment packet exists, because no packet existed until today. batch-88 ran that lens at every increment; batch-89 ran it at none."* increment-003 — *"Both defects above were found by the increment's own arms — which is better than nothing and is **not** the independent lens batch-88 ran at every gate."* increment-004 — *"Every defect above was found by the increment's own sentinel and battery."*
    **What is wrong today is not the absence — it is that the absence cost nothing.** batch-88 ran the lens at every increment and its packets record the BLOCKs it returned. batch-89 ran it at none, shipped **five flow revisions** (rev51–rev55), and **passed every gate anyway**. So the reviewer pass is discretionary in practice: skipping it changes no verdict, produces no warning, and is visible only to a reader who opens six packets and reads a subsection. **A gate that is satisfied by the author's own sentinels is a gate the author owns on both sides.**
    ⚠ **And the self-found defects are the argument AGAINST comfort here, not for it.** The sentinels were unusually good this batch — they caught a survivor asserting the same constant on both sides, a probe that was an assumption, and `M9`'s lesson that an arm over the CORE is not an arm over the LOADER. **A lens that finds three defects of its own construction is evidence the construction is inspectable, not evidence a second reader would have found nothing.** The one thing no self-check produced this batch was the class of finding rev52 needed an outside measurement for: that the freeze it inherited protected nothing.
    **What closes it — and it is a CONTROL CANDIDATE, so it owes its own AskUserQuestion per this file's control-encode rule.** Either **(a)** the increment template's gate checklist gains an `Independent review` row that must read a verdict or an explicit, reasoned **WAIVED-BY-OPERATOR** — never blank and never `ABSENT` — or **(b)** an operator ruling that the lens is optional for flow-only increments, recorded in writing, so that `ABSENT` stops being a silent finding and becomes a declared policy. **`C-45` landings for this candidate read command ❌ · template ❌ · catalog ❌ · pushed ❌.**
    **How to verify closed:** the next batch's packets each carry a non-blank verdict in that row, **and** a packet with the row left blank or reading `ABSENT` fails its own gate checklist — demonstrated by trying it once, because a checklist row nobody has seen refuse anything is a paragraph.

  - **▸ (P3, NEW — mine, small, and reproduced twice at this pass 2026-09-03) THREE `--selftest` ARMS PRINT AN UNSORTED `set` REPR, SO THE SELFTEST'S OUTPUT IS NOT REPRODUCIBLE BYTE-FOR-BYTE UNDER DEFAULT `PYTHONHASHSEED`.** Measured by running `python ~/.claude/docs/tools/devflow-validate.py --selftest` twice into files and diffing: **exactly 3 differing lines of 565 arms**, both runs exit **0** with **`SELFTEST PASSED`**. The three, at their line numbers in that run's output: `IFC ids-declared` (`{'LLR-120.2', 'LLR-SENSE-2'}` vs `{'LLR-SENSE-2', 'LLR-120.2'}`), `IFC list-named` and `IFC list-multiline` (both `{'a', 'b'}` vs `{'b', 'a'}`).
    **NO VERDICT MOVES, and that is precisely why it is worth a line rather than a shrug.** rev55's own changelog row already records the fact — *"their output is **not** byte-identical and rev54 already recorded why"* — so it is a known, accepted property. **What is wrong today is downstream of it: this flow writes arms that diff output.** `ENC VERDICT-live` reads a CHILD `--selftest`'s output; `ART A9-freeze-noop` compares two `_artifacts` maps *values included*; rev55's two batteries were compared by *"verdicts read from the FINAL line beginning `SELFTEST `"* **specifically because a whole-output diff would not have held**. The next arm that diffs the selftest transcript rather than its final line **fails on noise**, and it will look like a real regression.
    **What closes it:** the three arms print a **deterministically ordered** rendering — `sorted(...)`, or a frozenset rendered through a sort — so a transcript diff is a usable instrument. **The set is the right data structure; the repr is the wrong presentation.** Cost is three lines and it removes the only known source of run-to-run variation in the transcript.
    **How to verify closed:** two consecutive `--selftest` runs under **default** `PYTHONHASHSEED` (not a pinned one — pinning hides it rather than fixing it) produce **byte-identical** output, and `diff` returns 0 lines. ⚠ **Do not verify with `PYTHONHASHSEED=0`**: that is the vacuous form of this check.

  - **▸ (P0, NEW — `R-89-6` + `R-89-7`) `V2` HAS BEEN VACUOUS ON THIS PROJECT ALL ALONG, and it is vacuous TWICE OVER on two orthogonal axes.** Routed as one item with two limbs because they share a subject and nothing else; **neither repair reaches the other**, and the record says so in its own words (`R-89-7`: *"it is orthogonal to `R-89-6`, and neither path considered for the grammar would have touched it"*).
    **Limb 1 — the GRAMMAR harvests nothing this project writes.** `_V2_DECLARED` is `\bAT-\d+[a-z]?\b`; batch-88 declared **nine** batch-scoped acceptance ids and the rule harvests **0** of them, printing *"no AT ids declared"* — **a true sentence about a grammar nobody uses.** ⚠ The grammar excludes the convention this repo's own `CLAUDE.md` tells authors to prefer (*"Prefer batch-scoped ids … they cannot collide across batches and stay outside the global pool"*), so the rule is blind **by policy**, not by accident.
    **Limb 2 — the CORPUS is hardcoded.** `V2` resolves an id against `<project>/tests/` **and nothing else**, so a batch whose acceptance surface is elsewhere cannot satisfy it with a correct id. batch-89's acceptance arms live in `devflow-validate.py --selftest`. **Measured, not argued:** an id in the numeric form, taking a number free across the whole corpus, was minted into that record experimentally and the gate answered that it *has no node on disk*; it was then withdrawn — **and withdrawing it meant DELETING the token, not quoting it**, because `V2` harvests its declared side **without `_strip_code`**, so an id written as guidance inside backticks is still declared. That is a fourth `V2` observation and it is registered nowhere else.
    **What closes it.** Limb 1: widen `_V2_DECLARED` to the batch-scoped form — **in its own increment**, because the constant is **FROZEN by the `WORDING-declared` arm** from batch-88 Increment 4, and that freeze was earned (rewording a constant to assert its opposite once left **288 arms green**). Limb 2: the node corpus becomes **declared** rather than hardcoded to `tests/`, so a project whose acceptance arms live in a selftest can name where they live. `R-89-7` calls limb 2 *"the real fix"* and a batch-90 candidate.
    **How you verify it closed.** Limb 1: run the gate over `.dev-flow/2026-08-24-batch-88/` — it names all nine ids instead of printing *"no AT ids declared"*. Limb 2: an acceptance id whose node is a `--selftest` arm **resolves**, and one with no node anywhere still **BLOCKs**. Each limb needs an executed RED arm; a widened grammar that resolves nothing is the same defect wearing a wider hat.
    ⚠ **This absorbs `G5-02` and that gap should not be re-registered separately.** `G5-02` records *"Layer B never executed — 0 of 9 acceptance ids run at a gate, and three appear in no increment at all."* That is not only a batch-88 discipline failure: **the rule that exists to notice it could not see the ids.** The two halves are one item.
  - **▸ ◑ (was P1, NEW — **HEAD CLOSED at rev55, limbs (a) and (d) closed, limbs (b) and (c) STILL OPEN**; re-measured 2026-09-03) The environment the gate ASSUMES is undeclared, unmeasured at runtime, and one rule violates the law its own file states.**
    **✅ THE HEAD IS CLOSED (rev55, 2026-08-31, commit `cf24e29`) — `HLR-89.4` and `HLR-89.5` were BUILT, and the thing they were owed for was the pair of them.** This item's own sentence was *"approved scope, specified, and NOT IMPLEMENTED"*; both are now shipped. **`HLR-89.4`** landed as **`V30`** (BLOCK, selector `S0` plus an EXECUTED import probe), which declares the contract as a canon row in `FLOW-VERSION.md` and **derives** every floor from the construct that binds it: **API 3.7** by an AST walk over the 3 Python files that table declares (two construct kinds, **15 catalogued hits** — `from __future__ import annotations` at `devflow-validate.py:41` plus `subprocess.run(capture_output=)` at 14 further sites), **git 2.28.0** by `git init -b` at 3 sites all inside `--selftest`, and **0 third-party imports** run under `python -S -E` in a child interpreter rather than looked up in a name list. **`HLR-89.5`** landed as **three PREFLIGHT lines `run()` now prints before any rule runs** — git and its floor, stdout's encoding, and whether this filesystem folds case.
    **Two decisions inside that closure are worth keeping, because each had an obvious wrong answer beside it.** (1) `sys.stdlib_module_names` was **refused** for the third-party-import check because it is a **3.10 API**, and using it would have raised this file's own floor by a construct the catalog cannot see — the rule would have violated the contract it declares. (2) **The second derivation is the one that cannot be done, and it is reported rather than faked:** an AST walk structurally cannot see a SYNTAX floor because it only ever sees what already parsed, and `ast.parse(feature_version=)` is **not** the substitute — measured, it returns the *running* interpreter's verdict at every requested version (3.11.15 refusing the rev53 PEP 701 construct **6 of 6**, 3.12.7 accepting it **6 of 6**). So `python-executed` is **3.11, a lower bound WITNESSED BY EXECUTION**, and `python-api` 3.7 carries the words *DOCUMENTARY, NEVER EXECUTED* inside its own finding.
    **Verified at this pass by execution, not by reading the changelog:** `python ~/.claude/docs/tools/devflow-validate.py --selftest` → exit **0**, **`SELFTEST PASSED`**, **565 arms** (was 530 at rev54, 516 at rev51). **Mutation at rev55: 27 applied, 27 killed, 0 survivors** — and **two sentinels survived their first battery and both were right to**, which is the reusable lesson: `SENTINEL-must-be-RED` over `V30` survived because the arm asserted `msg == _V30_NO_SECTION`, **the same constant on both sides**; and `N7-case-probe-is-assumed` reduced the case-folding probe to `return True` and survived **because this machine really does fold case**. *A probe is told from an assumption only by substituting the filesystem underneath it.*
    ⚠ **A closure this item did not ask for, recorded because it belongs to the same subject: rev53.** conda `s19env` is Python **3.11.15**, and `devflow-validate.py` carried a multi-line string literal *inside* an f-string replacement field — **PEP 701, which lands in 3.12** — so **the flow's own validator could not be PARSED by the environment the project's gate declares**: `SyntaxError` at the `INT FOUR-PLACES-agree` arm, exit 1. It passed on 3.12 and died on the interpreter that actually runs the gate. **That is the sharpest possible statement of this item's premise** — the assumed environment was not merely undeclared, it was wrong — and it was found only because rev53 went looking.
    **✅ (a) — CLOSED at rev54 (2026-08-31, commit `c118f6e`); see the block after this line, which records that the defect was WORSE than this limb states. `v8_module_map` passes `text=True`, violating the law stated by `_git`'s own docstring in the same file.** Re-measured 2026-08-30: the `git ls-files` call inside `v8_module_map` (`devflow-validate.py:618-619`) runs `capture_output=True, text=True`, while `_git` (`:1344-1345`) states *"Explicit encoding rather than `text=True`: the latter decodes with the locale codepage, which on this platform is not UTF-8 and raises on a byte it cannot map."* ⚠ **And the failure renders as a false sentence:** the call sits under a bare `except Exception` that sets `tracked = None`, which is the `_V8_NO_GIT` path — so a decode error on one tracked filename prints *"git could not be run, so NO file was enumerated"*, which is not what happened. **Closes:** the call takes `encoding="utf-8", errors="replace"` or routes through `_git`. **Verify:** `grep -n "text=True"` over the file returns the docstring line **and nothing else**, plus an arm over a tracked path carrying a byte cp1252 cannot map. ⚠ **This collides with `HLR-89.4` and the collision is `R-88-18`'s shape:** that requirement derives the **Python floor of 3.7** from *"`capture_output=` with `text=`"*. Remove the `text=` and the derivation's evidence moves — so the floor must be **re-derived**, not carried across. *A derivation that replaces a sentence inherits the sentence's constant, and the constant was true of the OLD predicate.*
    **✅ (a) CLOSED — and rev54 found THREE defects in those four lines, not the one this limb names.** (1) **The decode cause, as written above:** `text=True` decodes with the locale codepage and a `UnicodeDecodeError` surfaced through the bare `except Exception` as `_V8_NO_GIT` — *"git could not be run"* about a run in which git ran fine. **The rule did not fall silent; it testified incorrectly.** (2) **A FALSE PASS that was already present, not "one edit away" as the file's own comment claimed:** `subprocess.run` does not raise on a non-zero exit, so a root outside any repository gave `[]` and `V8` printed *"N modules, no orphan files"* — the exact verdict the rule exists to withhold. (3) **`core.quotepath`, a git config this flow does not own:** under git's default git ASCII-quotes non-ASCII paths to `"Ä.py"`, which does not end in `.py` and was **silently dropped from the check**, while whitespace `.split()` cut `two words.py` into a phantom orphan *and* a file never checked.
    **The repair, and it removed a dependency rather than documenting it:** `git ls-files -z` (`devflow-validate.py:644`), explicit `encoding="utf-8"`, a returncode branch carrying its own distinct sentence `_V8_GIT_FAILED` (`:579`, *"git RAN and exited non-zero, so NO file was enumerated"* — a different fact from absent-from-PATH, and this file has now paid three times for collapsing those states), and a narrowed `except`. **`QUOTEPATH-invariant` asserts the verdict does not move under EITHER setting of the config**, so the flow no longer depends on a git option it cannot control.
    **This limb's stated verify clause is met in substance and NOT in letter, and the difference is stated rather than smoothed over.** It asked that `grep -n "text=True"` return *"the docstring line and nothing else"*. Measured 2026-09-03 it returns **five** lines — `:626`, `:628`, `:1376`, `:3503`, `:5318` — and **every one is prose**: a comment or a docstring explaining the trap. **Live call sites passing `text=True`: 0.** The grep grew because the lesson was written down next to each site that must not repeat it, which is the opposite of the failure. **Reopen if** that count is ever non-zero.
    **And the collision this limb predicted DID have to be paid: `HLR-89.4`'s Python floor was RE-DERIVED, not carried.** Removing `text=` moved the derivation's evidence exactly as `R-88-18`'s shape says it must. The API floor stays **3.7**, now bound **independently** by `from __future__ import annotations` and by `capture_output=` at the **9 remaining** call sites — off the construct rev54 edits. **`--selftest` executed on 3.11.15 and 3.12.7: 530 arms, 0 FAIL, `SELFTEST PASSED` on both.** Nothing below 3.11 exists on this machine, so **3.7–3.10 is documentary and never executed**, and the declared *executed* floor is **3.11**. rev54 mutants: **9 applied, 7 killed, RED sentinel first, 2 survivors argued equivalent on NTFS.** See `LED-89.22`.
    **⚠ (b) STILL OPEN, re-verified 2026-09-03 — The manifest's canonical verify command is bash-only, on a PowerShell machine.** `~/.claude/docs/FLOW-VERSION.md:117-124` is a `bash` block using `sed -nE`, `sha256sum`, `tr -d '\r'`, `cut` and a `while read` loop. This operator's machine is Windows 11 / PowerShell 5.1. So *"prove in one command that the flow you are about to run is current"* — the sentence **C-45 exists to make mechanical** — **is not runnable in the shell the operator actually uses.** A check that cannot be run is indistinguishable from one that is never run. **Closes:** a declared PowerShell route, **or** an explicit line saying the command requires Git Bash and that `V7` is the supported route on Windows. ⚠ **Do not add a third inventory** — a PowerShell variant must derive from the same table by a second *implementation*, never a second *list*; this manifest has recorded that exact defect five times (rev15, rev16, rev19, rev20, and again in `V15`'s pairing config). **Verify:** the declared route runs in the operator's default shell and reproduces `bceb968c4975fe13` (the aggregate was `a8d3c4ce91f1b0c6` when this item was written; it moves with every bump, which is itself part of why a figure typed into an open item needs its date).
    **⚠ (b) UNCHANGED, and re-measured rather than assumed.** `~/.claude/docs/FLOW-VERSION.md:141-147` is still the only verify route and still a `bash` block — `sed -nE`, `sha256sum`, `tr -d '\r'`, `cut`, a `while read` loop. Measured at this pass: the file carries **three** fenced blocks, and **both of the two that hold commands are `bash`** (`:112`, `:141`; the third, `:18`, is the manifest header and not a command); `grep -cin 'powershell\|git bash'` over the whole file returns **0**. So neither of the two closures this limb offered has been taken: there is no declared PowerShell route, **and** there is no line saying the command requires Git Bash with `V7` as the supported Windows route. **Four flow revisions have shipped since this was written (rev52–rev55) and none touched it** — worth noting because the same four revisions closed the two OTHER limbs of this item, so the omission is selective rather than a backlog-wide stall. ⚠ The aggregate figure in the line above has moved again with those revisions; re-derive it, never carry it.
    **⚠ (c) STILL OPEN — but its central measurement is now FALSIFIED and the reason matters; see below. `s19env` — the conda environment the gate suite actually runs in — appears NOWHERE in the flow canon.** Measured 2026-08-30: a recursive grep over `~/.claude/docs/`, `commands/`, `templates/` and `hooks/` returns **0** hits. It is named only inside batch records (batch-87 §2.8 `M-6`, *"run under conda `s19env` through Textual's `run_test()`"*) and their mirrors under `~/.claude/jobs/`. Meanwhile **this file's own batch-87 refresh already carries an env-specific HARNESS RULE** — *never invoke the gate suite through `conda run`; call the environment's interpreter directly* — naming `docs/engineering-rules.md` as one of its two homes, and **that landing reads ❌**. **Closes:** the project's `docs/engineering-rules.md` names the environment and the invocation that works (`<env>/python.exe -m pytest` with `PYTHONIOENCODING=utf-8`). **Global flows stay project-agnostic, so this half does NOT go into `~/.claude`.** **Verify:** `grep -n s19env docs/engineering-rules.md` is non-empty and names the same invocation the batch artifacts paste.
    **⚠ (c) STILL OPEN, and its "0 hits" measurement no longer reproduces — corrected 2026-09-03 rather than left standing.** The recursive grep that returned **0** on 2026-08-30 now returns **4**: `~/.claude/docs/FLOW-VERSION.md:91` (the `python-executed` canon row, *"the whole file set parses, and `--selftest` passes, on `s19env` **3.11.15**"*), :244 (rev53), :245 (rev54), and `docs/tools/devflow-validate.py:7237`. **So the environment did reach the flow canon** — carried in by rev53's parse failure and rev55's contract row, both of which had to name the interpreter they measured.
    ⚠ **And that is a routing violation this item itself predicted, arriving from the direction it did not expect.** This limb's own text says *"Global flows stay project-agnostic, so this half does NOT go into `~/.claude`."* The project's environment name is now in four places inside `~/.claude`, put there by the flow's own revisions rather than by anyone routing it. **Two readings, both admissible, and the operator owns the choice:** either `s19env` is legitimate canon content because it names the machine a canon row was **executed** on (a measurement is allowed to name its instrument), or it is project detail that must be generalised to *"the interpreter the consuming project's gate declares"*. **Not decided here.**
    **What is UNCHANGED is the half this limb said closes it, and it is still not done:** `grep -n s19env docs/engineering-rules.md` → **empty** (the file exists, 23,655 bytes). The project's own rules file still does not name the environment or the invocation that works in it, and this file's batch-87 refresh still carries the env-specific harness rule — *never invoke the gate suite through `conda run`; call the environment's interpreter directly* — with `docs/engineering-rules.md` named as a home and that landing reading ❌. **How to verify closed (unchanged):** `grep -n s19env docs/engineering-rules.md` is non-empty and names the same invocation the batch artifacts paste.
    **✅ (d) CLOSED at rev55 as to the stated closure, with the stated VERIFY clause still unmet and re-filed below. Filesystem case-folding is relied on, and proven only at the SOURCE level.** `_read_resolved` (`devflow-validate.py:530-546`) exists because `V8` opened `docs/ARCHITECTURE.md` while git tracks `docs/architecture.md` — **invisible on NTFS**. Its own docstring states that installing the resolver inside `_read` would be *"a behavioural NO-OP on this host"*, so the prohibition *"is checked at the source level or not at all."* **Nothing has ever run these rules on a case-sensitive filesystem**, and `HLR-89.5`'s rationale concedes it: *"the selftest's own tail admits case-folding is unproven."* **Closes:** the `HLR-89.5` preflight reports the measured value, so the assumption is stated rather than assumed. **Verify:** the preflight prints it, and `--selftest` passes on a case-sensitive runner — which is the **second element of a two-element domain**, the instrument `V25` and `V26` both earned, applied to the one axis that still has one element.
    **✅ (d) CLOSED against what this limb said would close it, quoted from itself:** *"the `HLR-89.5` preflight reports the measured value, so the assumption is stated rather than assumed."* rev55 does exactly that — `PRE CASE-live-probe` plus the **third preflight line every `run()` now prints**, and **the probe NAMES THE VOLUME it ran on**, because it writes where it is allowed to write and that is not always the volume of the tree under check. This was the **second and last** of the two I/O steps the selftest's own tail had listed as unproven for seven revisions; rev54 closed the first (`V8`'s enumeration).
    ⚠ **RE-FILED, OPEN, and deliberately not folded into the tick above: the VERIFY clause this limb wrote is NOT met, and rev55 proved it by killing nothing.** The clause asked for *"`--selftest` passes on a case-sensitive runner — the **second element of a two-element domain**."* The domain still has **one element.** The proof is rev55's own sentinel: **`N7-case-probe-is-assumed` reduced the case-folding probe to `return True` and SURVIVED**, *because this machine really does fold case* — so an arm over the VERDICT is satisfied by a constant. **A probe is told from an assumption only by substituting the filesystem underneath it, and nothing here can do that.** **What closes it:** one `--selftest` run on a case-sensitive filesystem (a Linux runner, or an NTFS directory with per-directory case sensitivity enabled), with the preflight line's value read from that run. **How to verify closed:** the run exists, its preflight prints the case-folding value as FALSE, and `N7-case-probe-is-assumed` is **KILLED** there — the sentinel is the oracle, and until it dies somewhere the probe is a well-documented assumption.
  - **▸ (P1, NEW — `R-88-17`, the single most repeated defect of batch-88, in a batch about populations) A CORRECTION HAS A POPULATION, AND NOTHING IN THIS FLOW ENUMERATES IT.** **8+ instances, every one found by a lens and none by a rule.** Measured across seven increments, **three in Increment 6 alone**: a pip-warning refinement landed at **1 of 2** sites and left the one the gate reads; a five-EDITS disambiguation landed at **1 of 5**; and an operator-ruled guard line changed while **four documents went on quoting the old one**. Earlier: `LLR-88.4` left demanding `8 of 8` after its parent moved to seven; the functional-traceability table; `LLR-88.6`'s threshold; `LLR-88.7`'s; and a de-minting that **RELOCATED** ids into a second rule's population.
    **Why no rule reaches it:** the batch's own discipline names the artifacts a ruling touches and stops there, and **no rule can know which documents carry a claim** — the claim spreads further than the ruling does.
    **What closes it — two candidates, both recorded by the batch, both needing their AskUserQuestion:** **(a)** a **claim registry keyed by figure**, so a figure has an enumerable population; **(b)** a **gate step that greps a superseded figure across the whole record and BLOCKs on any occurrence outside a supersession marker.** **How you verify:** change a figure in one document, leave it standing in a second, and the gate names the second. ⚠ **(b) is the cheap one and it has a known collision measured this fortnight:** `V26`'s first strike-pattern BLOCKed a record merely for **showing** the canonical supersession marker, and batch-89's own record escaped only by the accident of a space inside its example — so a marker-aware grep needs `_strip_code` from its first line, not after it false-fails (C-53). **Until one of the two exists, *"corrections beside errors"* is a discipline with no population and therefore no threshold.**
  - **▸ (P1, NEW — `R-88-19`) A PROBE IS A MEASUREMENT, and a measurement in the record is NOT A CONSTRAINT unless something checks the implementation against it. The probe ledger is owed a re-run and the re-run has not happened.** **The worked case, and it is unusually sharp:** probe `M-9`, written 2026-08-24, ran `git for-each-ref --sort=-committerdate refs/remotes/origin` — **the right domain and the right date field, both of them, three days before any code existed.** The first cut of `V25` swept `refs/remotes/` — **every** remote — and produced the review's only HIGH: a demonstrated *"backup verified … 0.00 day(s) old"* over a repository whose `origin` was **400 days stale**. **The divergence was checkable against the record the whole time and nothing compared them.** batch-88 armed sentences, counts, cardinality, extent, bindings, absence and referents, and **never once asserted that a shipped oracle's command matches the probe that justified it.** `M-9` is one of **12** probes.
    **What closes it:** the probe ledger is **re-run at close**, and every probe whose result a shipped rule reproduces **names the rule**, so the two can be compared by something other than a reader's memory. Owed at batch-89 and **NOT DONE** — batch-89 has no `04-validation.md` and no close, which is the item below. **How you verify:** diff a shipped rule's argv against its probe's; `V25`'s `git ls-remote` / `for-each-ref` call against `M-9` is the worked case, **and an arm that reads `V25`'s actual argv already exists** (rev47), so the mechanism is proven — what is missing is the obligation.
    ⚠ **Do not read this as `V25`'s defect.** The code was fixed by the implementer. The open item is the **missing comparison**, and it generalises past `V25` to every rule the flow has shipped since rev11.
  - **▸ ✅ (CLOSED, rev52 — and the fork was resolved by a THIRD option neither branch offered) `_active_batch_dir` IS FROZEN BY ITS OWN ACCEPTANCE CRITERION, so its guard sits in ONE caller and a SECOND caller inherits the hole.** Re-measured 2026-08-30. `_active_batch_dir` (`devflow-validate.py:3595-3601`) **joins the declared `batch_id` onto `.dev-flow/` and inspects nothing.** So `batch_id: "."` names a directory that **exists**, passes `isdir`, is reported **`ok`** by `V18` — *"active batch declared and on disk"* — and **re-admits the entire tree**: measured 2026-08-27, **206 keys** on s19_app, and `".."` escaped `.dev-flow/` altogether at **271**. **That is strictly worse than the `ghost` case, which at least announces itself.**
    **The direct-child test therefore lives in the CALLER**, and the code says why in its own words: *"A batch directory is a DIRECT CHILD of `.dev-flow/` or it is not one, and that is checked here rather than in `_active_batch_dir`, **which `LLR-88.5` freezes**"* (`:3632-3634`, repeated in the selftest at `:5360-5364`).
    **The hole, measured: the function has THREE call sites and only ONE carries the guard.** `_artifacts` (`:3670`) guards. **`_v27_packets` (`:2944-2952`) does not** — it calls `_active_batch_dir` and immediately does `os.listdir(os.path.join(active, "03-increments"))`, so a `batch_id` of `".."` points the brand-new `V27` coverage check outside `.dev-flow/` with **no check at all**. The third site is a selftest arm (`:5443`). **A guard in one caller is not a guard on the function**, and `V27` shipped at rev50 — *after* the guard was written — inheriting the hole rather than being protected by it. That is the cost of the freeze made concrete.
    **What closes it, and it is a fork the operator owns:** **either** lift `LLR-88.5`'s freeze for one line and move the direct-child test into `_active_batch_dir` — an operator act, because that is a shipped acceptance criterion — **or** state plainly in the record that the function's contract is *"whatever `state.json` says, unvalidated"*, and then **audit every caller against that contract** and keep auditing at each new one. What is not available is leaving it implicit. **How you verify:** a fixture setting `batch_id` to `".."` and an arm asserting `_v27_packets` returns the empty set rather than a foreign batch's increment numbers — **an arm that does not exist today**.
    **▸ CLOSED at rev52 (2026-08-31) by `LLR-89.6.5` · `LED-89.20` · `LED-89.21`, and NEITHER branch of the fork was taken.** Both were measured first and both are insufficient, for one reason: **`_active_batch_dir` is not the leak's only mouth.** `_v27_newest_commit` reaches the declared id through `os.path.basename(path)` on **`_active_batch_state`**'s return value and never calls `_active_batch_dir` at all — so the "lift the freeze for one line" branch closes `_v27_packets` and leaves the currency half of the **same rule** running `git log -- .dev-flow/.` over the whole corpus and passing on it. The "state the contract and audit every caller" branch is `R-88-17` wearing a contract: it has no population and must be re-discharged at every future caller, forever. **The guard went into `_active_batch_state`** — the function that RESOLVES a declared id against the filesystem — as a fifth reason code, `notchild`. `_active_batch_dir`'s **body is unchanged**, so `LLR-88.5`'s freeze is honoured to the letter and seven call sites close at once instead of three. **The freeze protected nothing measurable, and that is armed, not asserted:** `ART A9-freeze-noop` reproduces rev51's caller-side guard and compares the two `_artifacts` maps over 7 declared ids, values included — identical everywhere; had it moved, the honest answer was to report the freeze protects something real and take the other branch. **Sharper than this item recorded:** with an `03-increments/` sitting beside `.dev-flow/`, `batch_id: ".."` did not fall silent on the `OSError` — `_v27_packets` returned **`{42}`** and `V27` **named a foreign increment as this batch's unlogged work**. Verify: `--selftest` prints `ART A9-dot/dotdot/nested/trailing-sep/freeze-noop`, `V18 notchild-SENTENCE`, `V18 TRAVERSAL-notice/up`, `V27 GUARD-traversal`, `V27 GUARD-currency`, all `ok` at 524 arms; 6 of 6 applicable mutants KILLED, 0 survivors, crash control reported CRASH.
  - **▸ (P1, NEW — `R-88-12`) THE NAMED-MUTATION MANDATE IS UNVERIFIABLE AS WRITTEN — `LLR-88.4` requires every arm to name, IN A COMMENT, the mutation that reddens it, and NOTHING IN THE SELFTEST CAN READ A COMMENT.** Measured 2026-08-27, **and the batch found it inside its own anti-vacuity machinery**: the `SUB-BULLETS` arm shipped with a comment naming a kill it could not make. Its comment claimed a lookahead capped at one continuation line would not reach the fixture; the modal sat on the **second** continuation, so a cap of 2 survived — **and with it 623 of 1998 block lines, 31 % of the scanned surface, went unreached in silence.** The control asks for a claim in prose and provides no way to check it, so **an arm can be audited as covering an axis it does not touch, and the audit ticks the box.**
    **What closes it — a fork, and both branches are honest:** **either** the named mutation becomes **machine-readable and is EXECUTED by a harness**, **or** the mandate is **retired** rather than kept as an unverifiable obligation. **How you verify:** an arm whose named mutation does not redden it **fails the harness**, demonstrated on a deliberately mislabelled arm.
    ⚠ **Encode it together with `R-88-10`, which is the same property from the other side:** *`assert non-no-op` is necessary and NOT sufficient for a mutant — it must also still RUN, and a mutant that crashes reads exactly like a survivor.* Measured 2026-08-27: a hand-built mutant of `_V5_NO_LEDGER` spliced away a definition, so the file raised `NameError`, printed **0** arm lines, reported **no FAIL** — and the harness read that as **SURVIVED**; rebuilt correctly it prints **299** arm lines and is killed at exit 1. **A second trap rode with it: the shell's `$?` captured the pipeline, not the interpreter, so the crash also reported exit 0.** The practice has since been adopted **ad hoc** — batch-89 Increment 6 refused to score `M13-nodate-silent` because it crashed at 454 arms with no verdict line — **but it is encoded nowhere**, which is the next item.
  - **▸ (P1, NEW) THERE IS NO CANONICAL MUTATION HARNESS. Every increment writes a throwaway, and the rev51 repair landed inside one that is not on disk.** Measured 2026-08-30: `find ~/.claude -name "mutate_*" -not -path "*/jobs/*"` returns **nothing**, and the same search over `s19_app` returns nothing. So the substring-verdict repair recorded in rev51 — *the false-survivor sweep that scored all twelve applicable mutants SURVIVED* — was made **in a script that does not exist**, and the next batch writes a new one and can reintroduce it. **Four harness defects are on record from a single fortnight**, which is the argument that this is a class and not a run of bad luck: the substring verdict (rev51); **a baseline that was already RED and whose kills were discarded rather than reported — TWICE**, at batch-89 Increments 3 and 6, the second because an arm reading three sibling scripts by walking up from `__file__` cannot run outside a flow-shaped tree; a **crashing mutant scored as a survivor** (`R-88-10`); and at rev45 a `/tmp` path split that meant **the first two mutants never applied**.
    **⚠ STILL OPEN, re-measured 2026-09-03 — the figure has not moved.** `find ~/.claude -name "mutate_*" -not -path "*/jobs/*"` → **0 files**, unchanged from the 2026-08-30 measurement. **Four further revisions ran mutation batteries in the meantime and every one of them was a throwaway:** rev52 (**6 of 6 applicable killed, 0 survivors, 1 crash control reported CRASH at 0 arms**), rev54 (**9 applied, 7 killed, 2 survivors argued equivalent on NTFS**), rev55 (**27 applied, 27 killed, 0 survivors**). **That is 42 mutants across three revisions, none of them re-runnable today**, and rev55's two most valuable results were properties of the harness rather than of the code — a sentinel surviving because an arm asserted the same constant on both sides, and a probe surviving because the filesystem agreed with it. **Those are exactly the findings a throwaway cannot accumulate.**
    **What closes it:** a harness enters the flow's **canon table** under `docs/tools/`, so it is hashed by `V7`, versioned with the flow, and carries the properties already paid for — **verdict from the FINAL line**, **per resolved node id rather than an aggregate**, **a mutant asserted to still RUN**, and **a red baseline that ABORTS rather than scoring**. **How you verify:** `V7`'s aggregate covers the file; a fixture reproducing the mid-run substring must be reported **killed**, not survived; a deliberately red baseline aborts. ⚠ **Read with the standing entry *"a counterfactual verdict must be reported PER RESOLVED NODE ID, never per mutant"* at the end of this file** — that entry is the same control and already cites a working implementation in **this project** (`tools/mutation_harness.py`, `fd9124a`), which the flow's own `--selftest` batteries **do not use**. It has been amended with a pointer here rather than duplicated.
  - **▸ (P1, NEW) BATCH-89 IS UNCLOSED, and by `V28`'s own logic the hole becomes PERMANENT the instant batch-90's directory opens.** `.dev-flow/2026-08-28-batch-89/` holds `01-requirements.md`, `01-requirements-ledger.md` and four increment packets. **No `04-validation.md`. No `05-close.md`.** The record states it itself (`increment-006.md` §6). `V28` has a **one-batch window**, and that window was **bought with a measurement**: of the **72** batch-shaped directories under `.dev-flow/`, **nine** hold neither or only one closing artifact (**batches 25, 63, 66, 71, 73, 75, 78, 79, 85**), so a corpus-wide sweep would print nine notices about closed history at every gate forever and a rule nobody can act on is a rule nobody reads. **So once batch-90 opens, batch-89 leaves the window and nothing judges it again.** This is the identical rollover batch-88 paid for, one batch later.
    **⚠ STILL OPEN, re-measured 2026-09-03, and the clock has NOT started — which is the whole of the good news.** `.dev-flow/2026-08-28-batch-89/` now holds `01-requirements.md`, `01-requirements-ledger.md` and **six** increment packets (`increment-001` … `increment-006`, up from the four this item was written against), and still **no `04-validation.md` and no `05-close.md`**. `state.json` still reads `"current_station": "P3"`, `"phase_status": "in_progress"`. **`ls -d .dev-flow/*batch-9*` returns nothing**, so no batch-90 directory exists and `V28`'s supersession has not triggered. **Two more revisions shipped into this unclosed batch since (rev54, rev55), plus five commits on `claude/batch-89-lean-contract` not yet in `origin/main`** (`a679ee6`, `c4e6fe5`, `ed03b10`, `c118f6e`, `cf24e29`). ⚠ **Closing batch-89 is now COUPLED to the `/dev-flow-sync` pre-requisite-2 item added at the top of this section** — the sync it needs at close is the sync that cannot pass for a `core` batch. **Fix that first, or the close stalls at the step after it.**
    **Also missing from the WORK and not merely from the record:** **Increments 4 and 5 are approved scope and unbuilt** (see the environment item above); and `state.json`'s `batch_objective`, `artifact_homes` and `triggers.record` are still batch-88's or stale — including the **superseded `55000` budget figure** and Increments 2 and 3 listed as *"NOT IMPLEMENTED"* — because the 2026-08-30 write was **scoped to `decisions_log` alone**.
    **What closes it:** batch-89 runs Phase 4, writes `04-validation.md`, then `05-close.md`, **before** a newer batch directory opens beside it. **That close is also what discharges `R-88-19`'s probe-ledger re-run and this file's own routing rule** — which is the whole reason this refresh had to be written by hand. **How you verify:** `V28` reports `ok` for batch-89 at the batch-90 rollover; `V27` reports zero unlogged increments; the `--selftest` arm `HISTORIC-rollover` already replays the batch-88 case for comparison.
    ⚠ **The packets are numbered 001, 002, 003 and 006 and THE GAP IS DELIBERATE. Do not "fix" the numbering.** Renumbering them 1–4 would close the hole visually and **silently rewrite what was planned**, which is precisely what `V26`'s append-only ledger and `V27`'s both-ways correspondence exist to prevent.
  - **▸ (P2, NEW — `R-89-8`) THREE TEMPLATE-MANDATORY FIELDS ARE ENFORCED BY NO RULE.** `Boundary catalog`, `Acceptance test(s)` and `Negative control` are mandatory in `req-template.md` by the 2026-08-28 ruling, and **checked by nothing**. `V4` checks that a `test`/`analysis` requirement carries an executed verification and a numeric threshold; **it knows nothing of these three.** *A mandate carried by convention is carried by nobody* — **and that is the measured argument that returned `Negative control` to the template in the first place**: it was inline in **9 of 10** thresholds, so exactly one requirement had no RED side and **nothing said so**. The same argument applies to the row itself. **What closes it:** a rule — or an extension of `V4` — asserting the three fields are present and non-empty for the requirement classes the template mandates them for, with the declared-empty literal recognised (batch-89 writes `owed at <increment>` in all five of its requirements, deliberately, because `V2` would otherwise harvest a quoted id; see the `V2` item above). **How you verify:** a requirement with `Negative control` deleted BLOCKs; one carrying the declared-empty does not; both arms executed. Named a batch-90 candidate by the record.
  - **▸ (P2, NEW — `R-89-3`) The template's own 38,687-char preamble is still written in the forensic register batch-89 REMOVED from records.** batch-89 split the requirements record into a **live contract** (current state only — no strikethrough, no amendment bullets, no supersession narration, no measurement inside a normative sentence) and an **append-only ledger**, and `V26` enforces the split with a derived budget. **The template that produces those records still carries its normative rules with their parenthetical origins inline**, in exactly the register that was removed. **Deliberately out of batch-89's scope**, and recorded as such rather than as a discovered gap: *"those blocks are normative rules whose parenthetical origins are their evidence; moving them is a second reform needing its own increment and its own ledger."* **What closes it:** that second reform, with its own ledger. **How you verify:** the same budget and strikethrough discipline `V26` applies to records applies to the template file — **or** an explicit exemption row stating why a template is not a record, which is also an acceptable close and is cheaper.
  - **▸ (P2, compact carries — four, each stated in full but not worth its own section)**
    - **`G5-01` — batch-88's per-rule enumeration was never produced and CANNOT be re-derived from today's tree.** `HLR-88.1` forbids discharge by total and *"402 arms"* is a total; the 7-of-7 per-rule enumeration was Phase 4's first obligation and Phase 4 did not run. The validator has since moved rev47 → rev51 and stands at **516 arms**, so re-deriving today produces **rev51's** enumeration, not batch-88's. **Closes:** recover the rev47 validator (`git -C ~/.claude show <rev47 commit>:docs/tools/devflow-validate.py`) and enumerate per rule, **or** retire the gap as unrecoverable **and say so** — silence is the one option that is not available. **Verify:** either the per-rule enumeration exists, or the retirement is written where the obligation is.
    - **`G5-04` — the `dev-flow-lessons` catalog entry for the ELEVENTH shape of the vacuous check is owed and unwritten. This is a C-45 landing: catalog ❌.** The shape, transcribed in `05-close.md` §3 so it is at least not lost: ***a typed sentence pins its WORDING and never its REFERENT.*** `V25` printed a ref count drawn from `ls-remote origin` beside an age drawn from **every** remote on the machine, and **nine typed-sentence arms and six registered-rule E2E arms all passed**, because every fixture had exactly one remote so the sentence's referent and the oracle's domain were the same set; `committerdate` → `authordate` survived for the identical reason. ***The detector is not a better sentence arm — it is a fixture whose domain has two elements that disagree**, and each axis needs its own second element.* **Closes:** the entry lands in `dev-flow-lessons/SKILL.md`. **Verify:** the catalog's own provenance count re-measures and moves — that file already records its derivation command for exactly this reason (batch-66).
    - **`R-88-8` — a numeric EDIT-SIZE threshold is a poor proxy for blast radius, and batch-88 hit the consequence three times.** `LLR-88.12`'s original *"`.github/` shows 0 changed files"*, criterion 8's *"0 files changed under `.github/`"*, and the *"exactly 4 lines"* form each **briefly forbade the cheapest correct fix for a hazard the change itself created** — and the batch had to overturn one of its own thresholds (`C7`) on the grounds that *"empty diff" could be satisfied only by never fixing the defect*. **Closes:** the flow's rule set says a source-scope pin bounds the **FILES** a change may touch and **not the lines inside them** — *a line budget prices a one-line guard the same as a one-line backdoor, and the author who needs the guard is the one who pays.* **Verify:** a criterion phrased in lines is rewritten in files, and the guard case that was forbidden now passes its gate.
    - **`R-88-3` — the whole map-resolution class is LATENT because nothing runs this validator over THIS project.** `hooks/flow-guard.py` (rev19) runs at `/dev-flow` and `/fast-dev-flow` invocation and reports **only the three flow rules** — project findings are the project's own gates, by design. So `V8`'s resolver, and every project-side rule batch-88 repaired, fire **only when a human types the command**, which is the condition C-53 exists to replace. *"The day the validator enters CI is the day it matters, and a batch closing audit gaps is exactly the batch that would put it there."* **Closes:** a CI job or a git hook **in this repo** runs `devflow-validate.py` over `.dev-flow/`. **Verify:** the job exists **and has gone red at least once** — a job that has never failed is indistinguishable from one that cannot.

## ✅ BATCH-82 — `C-54`, the Information Flow Contract · LANE B HALF — **ENCODED 2026-08-15**

> **Reduced to a pointer, deliberately not deleted.** The finding and its evidence originated here,
> and a backlog that erases what it closed cannot show why a control exists. The full record moved
> to artifacts that own it.

- **▸ ✅ (CLOSED) `C-54` — encoded into `/dev-flow` + `/fast-dev-flow` at flow `rev33`.**
  - **What landed, with before/after and the pasted `--selftest` transcript:**
    [`design/C-54-ENCODING-RECORD.md`](design/C-54-ENCODING-RECORD.md) — the propagation is an exit
    criterion, not a closing chore, and that file is its evidence. The flow lives in
    `jav201/claude-config`, outside this repo, so **a merged PR here is not evidence it landed**.
  - **The design, decisions `D-1`…`D-8`, and the worked examples:**
    [`design/C-54-information-flow-contract.md`](design/C-54-information-flow-contract.md).
  - **`rev27 → rev33`** · `flow_hash 656591c996355452 → d851576cfe8f60b3` · `controls C-10…C-54` ·
    `V10`–`V14` each demonstrating RED · selftest 85 → 136 arms.
  - **`D-6` resolved:** the provider declares its consumers and the validator **discovers** them by
    grepping the declared literal `address` — not Pact's inversion, because provider and consumer
    land in the same commit here and inverting would destroy the retrofit's actual deliverable.
  - ⚠️ **What this does NOT close.** The five rules report `SKIP` against this project, because
    s19_app has authored no IFC. **That is the Lane A retrofit** —
    [`BACKLOG-CODE.md`](BACKLOG-CODE.md) — and neither half is complete alone. What is demonstrated
    is that the rules run and can go red; what is not is that the contract is correct over this
    project's surfaces.
  - ⚠️ **Two stale copies registered for the retrofit, not fixed by the control:** the consumer list
    in `s19_app/tui/screens_directionb.py:1953` and in the design doc's §4 worked example each name
    **two** readers of `.loaded-detail`; the tree has **four**. Both were accurate when written.
    `V13` is now what notices when such a list stops being accurate.

- **▸ (P2) `C-55` candidate — registered, NOT encoded** (operator chose C-54 alone at the
  control-encode gate; that ruling stands rather than being quietly widened).
  *A threshold whose oracle NORMALISES something — set equality discards order, containment discards
  arity, concatenation discards structure — must state explicitly that the failure mode does not live
  in what it discarded.* This is the sharper half of the batch-79 finding. **Re-litigate after the
  retrofit**, which is the best evidence generator the project will have for it.

## 🆕 Routed from batch-79 (2026-08-13) — the fix that reproduces its own defect class, and the anchor that cannot go stale

  - **▸ (P1, NEW) `F-8` — a FIX is where this project's dominant defect class reproduces most reliably, and no test can catch it. THIRTEEN instances in one session, across seven independent merge-gate passes.** Registered, **not encoded** (control-encode rule above).

    ⚠️ **Four of the thirteen (10–13) are inside the single test node written to CLOSE this class.**
    `TC-B79-05` guards that a cited symbol anchor resolves. Its exclusion set had three entries naming
    symbols the corpus never cites; the comment correcting that **cited one of them**, making the set
    oscillate; its resolver walked into method bodies and admitted **every local variable** (783 names
    against 276 real), so an anchor naming a local passed the guard built to reject exactly that; and
    the docstring explaining *that* defect gave its example as a backticked class-qualified name,
    which the tightened resolver then failed on. **A control can reproduce the class it encodes, more
    than once, while passing.** #11 and #13 share one mechanism worth its own line: *explanatory prose
    inside the corpus is itself input to the check it describes.*

    | # | The fix | The defect it carried | Found by |
    |---|---|---|---|
    | 1 | the vacuous-input-set guard (`AT-B78-14`) | used a vacuous input set — `FunctionDef` only, so 34 real symbols read as missing | self, in minutes — it failed **loudly** |
    | 2 | `TC-B79-01`, the context bound | no assertion on how it apportions | gate 2 |
    | 3 | the line-citation sweep | created four new stale citations | gate 2 |
    | 4 | …its correction | the "corrected" range was wrong **on arrival** | gate 3 |
    | 5 | `TC-B79-04`, replacing #2 | left unasserted the branch production actually takes | gate 3 |
    | 6 | the `F-9` root-cause record | the stated cause was **physically impossible** — the pathspec excluded the file it blamed | gate 3 |
    | 7 | the citation-mechanism record | "+1 shift" false; the real shifts are non-uniform (+1 / +105) | gate 4 |
    | 8 | the symbol-anchoring sweep | **5 of 6 anchors named the wrong symbol; one was invented** | self, then gate 5 independently |
    | 9 | the `AT-B78-32` paragraph, rewritten **twice** — once expressly to remove false references from it | kept an inherited false boundary symbol (`page_prev_context`, actually the `crc_designer` binding two lines up) and the count of **six** it produced, which is **eight** | gate 6 |

    **Instance 9 is the sharpest and arrived last.** The figure entered at Phase 0's premise table,
    was carried into a test docstring, and survived **two rewrites of that very paragraph** — including
    one whose stated purpose was removing false references from it. *A figure inherited inside a
    sentence you are editing is still an inherited figure, and editing around it is not measuring it.*
    The wrong boundary symbol also **produced** the wrong count: believing the boundary sat two lines
    lower silently dropped `plus`/`minus` from the inventory — one false fact generating a second.

    **And the tally itself went stale.** This table read "FIVE across three passes" in the increment
    packet while the same packet's prose already discussed instances 6 and 7. *A running count of
    defects is a figure like any other.*

    **The mechanism, in the fifth gate's words:** *"HIGH-1 is comments and docstrings, with no runtime surface, which is precisely why 2656 passing tests cannot see it. That is the finding's point: the batch's recurring defect class lives in exactly the region its test suite has no oracle for, which is why it has survived five gates."*

    **Three properties worth encoding, if approved:**
    1. **A correction is itself a change and inherits the full risk.** Three of the eight are corrections *of* corrections. There is no terminal state reached by editing carefully — only by re-measuring.
    2. **The counterfactual is not optional on a fix.** It is the only thing that caught #2 and #5, and in both cases the node *looked* thorough and *read* as complete.
    3. **A green suite is not evidence about this class.** Seven of the eight sit in specs, records, acceptances or prose. The instrument that found them was an independent adversarial pass briefed to attack the author's claims.

    **Evidence strength:** the highest single-session count in the project's record, every instance verified by execution, and five found by an independent reviewer.

  - **▸ (P2, NEW) Symbol anchors must be EXECUTED against the tree, exactly like figures — replacing a line number with a symbol trades a *staleness* failure mode for a *correctness* one.** Registered, **not encoded**.

    batch-79 replaced six stale `file.py:NNN` citations with symbol names. **Five named the wrong symbol, and one — `S19TuiApp.RAIL_ITEMS` — has never existed in this repository**, while the comment carrying it read *"surface fact, verified against …"* — asserting a verification against a symbol that does not exist.

    **Root cause:** the anchors were derived by taking the nearest preceding `def` to each stale line number — *guaranteed* wrong precisely when the line number is the thing that drifted. The one anchor that survived (`S19TuiApp.compose`) is the one whose line had barely moved.

    **Why it is worse than what it replaced.** A stale line number fails **visibly** — the reader lands past end-of-file or on obvious nonsense. A wrong *symbol* lands them inside a real, plausible method: following `_apply_prepared_load` for "the A2L in-image glyph fold" delivers a genuine glyph mechanism that is not the one cited. **Temporarily wrong becomes permanently wrong.**

    **The discharge is cheap and exists:** a harness resolving every anchor against the AST and asserting each *contains* the substring its sentence claims. batch-79 wrote one; all 17 anchors pass. ⚠️ Carry this too: the *original* citation for the rail-key claim (`app.py:687-691`) resolved at the batch base to `_STRIP_CELL_GLYPH` — **false before the batch** — so the sweep added a second false reference to a sentence that already had one.

## 🆕 Routed from batch-77 (2026-08-01) — a control whose prescribed discharge no longer executes

  - **▸ (P2, NEW) `C-33`'s two named liveness mechanisms are BOTH unavailable under the current harness — the control cannot be discharged as written.** C-33 mandates never waiting passively on a delegated gate, and names two discharges: *"an active blocking wait (`TaskOutput block=true`, re-polling on timeout) OR poll\[ing] a genuine progress signal (touched-file mtime …; `TaskOutput` status)."* Both were tested at batch-77 Phase 1 and neither works:
    1. **`TaskOutput` is DEPRECATED for `local_agent` tasks** and its own documentation forbids the C-33 usage — the `.output` path *"is a symlink to the full subagent conversation transcript (JSONL) and will overflow your context window."* Its sanctioned replacement is the completion notification, i.e. **exactly the passive wait C-33 exists to forbid**.
    2. **The transcript-size signal is dead, verified by sampling** — both Phase-1 lanes' `.output` files sat at **0 bytes with mtime frozen at dispatch** (`17:05:11` / `17:05:46`) across a 45 s interval. This reproduces the batch-49 condition that motivated C-33 in the first place, and is exactly why the standing rule is *verify the signal moves before trusting it*.
    3. **A deliverable-file monitor is NOT a liveness signal** — the orchestrator's first attempt watched the agents' output artifacts, which are written once at the **END** of the work, so a 0-byte reading cannot distinguish *"reading hard"* from *"hung."* It is a **completion detector wearing a liveness detector's label** — structurally the same defect as the vacuous acceptances batch-77 spent five revisions removing, this time in the instrumentation rather than in a test.
    **Net:** for a `local_agent` under this harness there is currently **no in-band progress signal**, so C-33's *"STOP and take over once liveness cannot be confirmed"* branch is the only honest discharge — and it must be driven by a **wall-clock bound**, not by a signal. batch-77 adopted **30 min**, enforced by a monitor timeout; it was never hit across 9 review passes. **What this item owes:** either re-word C-33's discharge clause to name a mechanism that exists, or record that the wall-clock bound is now the primary discharge and the signal-polling branch is historical. ⚠️ **Needs its own `AskUserQuestion` before encoding** (control-encode rule above), and C-33's text lives in `~/.claude/commands/dev-flow.md` — **outside this repo** — so that leg needs its own before/after record per the header.

> **Earlier refresh: 2026-07-31 (C-3 closed — no batch).** `origin/main` tip = **`038dfd9`**. **C-3 and the batch-62 "16 of 23" carry are DISCHARGED** by the registry's Lane A guard (#181) - closure was owed at that merge and had not been made. Verified by execution, not by reading the PR: **G2** is the discharging rule, **G4** covers the citation axis, **13 passed**, and **AT-281** proves every rule G1-G7 can go RED. Nothing else in this lane changed.
>
> **Earlier refresh: 2026-07-31 (Amendment A move executed — no batch).** `origin/main` tip = **`fdfdf36`**. The three cross-lane **Lane A halves moved to [`BACKLOG-CODE.md`](BACKLOG-CODE.md)** and the *PENDING MOVE* section is retired to a signpost. **The undone-action record is discharged, but its finding is retained**: an explicitly-addressed one-sentence action survived **four** consecutive batch-74 PRs that each read this file. Nothing else in this lane changed.
>
> **Earlier refresh: 2026-07-31 (AT/TC registry Lane B close — no batch).** `origin/main` tip = **`69f8f42`** — rebased onto batch-74's full close-out (`537f27d` #173 + #175 + #177 + #178) and the spec merge (`0fdc88b`, #174). The conflict with batch-74 was **append-vs-append at EOF**, with no rival edit to any shared text: **both sections are kept**, batch-74's first. Changes: the **Lane B design half is DELIVERED** and marked as such; the **Lane A half is UNBLOCKED** (batch-74 merged, so the id set stopped moving) with its build contract redirected to the spec's §9; **three new carries** registered in the new section at the end; and the MAJOR item's figures flagged in place as **three-of-five irreproducible**, retained verbatim rather than patched. **One undone action surfaced, not fixed:** batch-74 is now fully closed across **four** merged PRs and performed the *PENDING MOVE TO LANE A* transfer in **none** of them.
>
> **Earlier refresh: 2026-07-31 (cross-lane split — no batch).** `origin/main` tip = **`d2e9bdc`** (batch-73 merged). Operator ruled that an item spanning both lanes is **split into one entry per lane**, recorded as **Amendment A** in the [router](BACKLOG.md). Applied to the **three** measured cross-lane items — the AT/TC registry (which subsumes C-3), **1c** and **1d**. Their Lane A halves are staged under *PENDING MOVE TO LANE A* because `BACKLOG-CODE.md` is owned by in-flight batch-74. **One earlier classification of mine was WRONG and is corrected:** the *bytes-committed-as-evidence* candidate is **not** cross-lane — its `.gitattributes` leg already shipped in batch-66 (`.gitattributes:19`), so only the control encode remains and it stays wholly here.
>
> **Earlier refresh: 2026-07-31 (backlog-carry audit — no batch).** `origin/main` tip = **`6524afd`**. Added ONE item: the carry-over control's input-side hole (new section below), routed here from the CODE lane per the router's *move, never copy* rule — its six app-side items live in [`BACKLOG-CODE.md`](BACKLOG-CODE.md). **This lane had not been reconciled since batch-66**, which is consistent with no process batch having closed since; stated rather than assumed.
>
> **Earlier refresh: 2026-07-28 (batch-66 close).** Base ref: `origin/main` tip **`73e3fb9`** (batch-64
> residue routing, #150). Batch-66 closed the §7.9 stale-flow-routing carry and opened four new items
> (see *NEW — carries opened by batch-66*). **Routing note:** this lane's own routing is now encoded in
> [`docs/engineering-rules.md`](../docs/engineering-rules.md) §"Backlog routing", which is where both
> global flow commands are now told to look — so a future batch finds this file from the project doc,
> not from a hard-coded path inside a project-agnostic command.

## 🆕 Control candidate from batch-76 (2026-07-31) — a counterfactual must move the predicate's DECLARED SUBJECT

**C-40 already requires confirming "the mutation actually applied". That check PASSED in all three of
this batch's harness failures and proved nothing in every one of them.** `applied = True` only
establishes that bytes moved in the file, not that the thing the predicate READS changed.

| # | What the mutant actually did | Why it read INERT |
|---|---|---|
| 1 | `_format_address` and `_format_length` carry a **byte-identical** `return` block, so `str.replace(old, new, 1)` mutated the **wrong function** | the mutation never touched the site under test |
| 2 | the substituted call landed **after a `return`** — dead code | program behaviour was unchanged, so nothing could redden |
| 3 | the mutation changed the **log** while the node asserts the **status** | mutation and assertion were about different observables |

**This fails OPEN**, which is what makes it worse than the typo'd-mutation case C-40 already records:
there the mutation does not apply at all and the transcript says so; here it applies, to the wrong
thing, and reports a **live predicate as inert** — inviting the author to rewrite a perfectly good
acceptance. Two of the three were only caught by re-reading the mutant against the node's declared
subject; none was caught by a test.

**Proposed discharge, all three mechanical:**
1. the anchor must match **exactly once** in the file (count it; refuse to run otherwise);
2. the mutated region must be **reachable** (not after a `return`/`raise`, not in dead code);
3. the mutated expression must name an **observable the node actually reads**.

Evidence: `.dev-flow/2026-07-31-batch-76/03-increments/increment-002.md` §4.2 and
`increment-003.md` §4.2. Related but distinct: C-40 (falsifiability), C-31 (input-set vacuity), and
the Lane-B **vacuous-FIXTURE** class — this batch hit that one three times too, and `TC-552` caught
two of them on its own author.

---


## Status legend
`P0` next · `P1` high · `P2` medium · `P3` low · flow ∈ {/dev-flow, /fast-dev-flow, direct}

---

## ✅ SHIPPED — control encoding (batch-63 operator decisions, ENCODED by batch-64 2026-07-27)

  - **▸ ✅ (was P1) batch-63 control candidates — ENCODED by batch-64, 2026-07-27.** The operator ruled *encode P-5 globally, absorb P-6 and P-7 into it, DECOMPOSE P-3 across four destinations*; batch-64 executed that ruling. **Shipped: `C-40`** (global `/dev-flow` Phase 1, P-5 with P-6/P-7 absorbed as limb 2's named instances) · **`C-42`** (`docs/engineering-rules.md`, P-3 leg 2) · **a RIDER on `C-35`** (global, P-3 leg 1 — `C-41` was cancelled as a control and **the id stays free**) · the **`VERIFY.md` extension** (P-3 leg 3, `tui-design` skill) · **`C-29` back-registered** in the lineage. **P-3 leg 4 (the TUI course) is OUT of batch-64** — plan durable at `G:/My Drive/Courses/textual/PENDING-UPDATES.md`, status NOT STARTED. Artifacts `.dev-flow/2026-07-27-batch-64/`. *(The full four-destination assignment below is retained verbatim as the spec that was executed.)*
    - **▸ ENCODE: P-5 → a new control in the GLOBAL `/dev-flow`** — *"can it go RED?" is a gate question distinct from "is it correct?"*. Every acceptance states, at authoring time, the mutation that turns it RED, **and that mutation is EXECUTED**. **Why it needs to exist:** no current control reaches it. C-10 mutates the CODE, C-31 mutates the INPUT SET, C-39 executes THRESHOLDS — batch-63 produced **five** predicates that survived all three, because each related `_line_bytes` to `"
".join` (both pure functions of `lines`) and **the writer never appeared in the expression**. A predicate can be correct, its set complete, its threshold measured, and still not mention the thing under test. Three of the five were written *after* vacuity was already the batch's identified theme, and three were the orchestrator's. **P-6 and P-7 are ABSORBED as named examples inside it, not separate controls:** P-6 (*a positive control must be shaped to the RULE, never to the implementation it certifies* — `AT-193b` was built only from cases its detector already caught, so it certified a completeness the detector lacked, and this happened while the author was actively hunting vacuity) and P-7 (*consolidating artifacts must preserve the union or explicitly retire what it drops* — revision 3 collapsed a 40-row TC layer into three id ranges and silently dropped 8 of ~18 union observables, including the one both Phase-1 lanes had independently agreed on). Operator's reasoning for absorption: all three fail from one cause — *the predicate does not touch what it claims to touch* — and three controls where one suffices is the friction that makes controls get ignored.
    - **▸ DECOMPOSE: P-3 (*assert against the emitted encoding, never a character list or the rendered form*) — ~9 occurrences over 3 batches, the project's most-evidenced un-encoded candidate. Operator ruled it must be SPLIT ACROSS FOUR DESTINATIONS, not encoded in one place.** Assignment, derived by inspecting each destination on disk rather than assumed:
      1. **GLOBAL `/dev-flow`** — the portable principle only: *an assertion about produced output is written against the bytes/tokens the PRODUCER emits, never against a character list, a human-readable rendering, or the spec's own vocabulary. Discharge: run the producer, paste its actual emitted form, then write the predicate against that.* Stack-free, so it belongs here per the placement policy.
      2. **PROJECT `docs/engineering-rules.md`** (currently 125 lines, C-13…C-38) — the stack-bound mechanics, as a new control extending C-32/C-37: markdown-it token streams (assert token TYPES, not substring presence; `&` entity spoofing — `&vert;` forges a table fragment with every token still `text`); **the escaped form of a character is not the character** (`'](' not in note` passed against a CORRECT implementation because the emitted form is `\](`); Mode-B code spans (a heading emitted as `` #### Checklist: `chk.json` `` is unfindable by its bare form — the batch-63 Phase-0 probe returned an impossible `-1` and that is the only reason it was caught); SVG snapshot text carries `&#160;` entities so a literal grep for `"CRC Designer"` returns 0/19; and **AST over regex** for source-line predicates (an implicitly-concatenated f-string is one assembled template, not two lines).
      3. **SKILL `tui-design` → `VERIFY.md`** — **EXTEND the existing `## Pin the truth, not a string [travels]` section (line 34); do NOT add a new one.** What it says today — *assert on a runtime value, never on the presence of a label* — is **necessary but not sufficient, and this batch has the counterexample**: `"CRC Designer" in svg_source` IS a runtime value and is exactly the predicate that returned 0/19. The extension is one step deeper: **pin the truth in the FORM THE PRODUCER EMITS IT**. Terminal/markup output has a source-form ≠ rendered-form gap (entities, escapes, ANSI, Rich spans), and the rendered form is the one a human reads and therefore the one an author reaches for. **CONSTRAINT (operator, 2026-07-27): this leg carries GENERAL knowledge ONLY — nothing s19_app-specific.** The test is *"is this true of ANY Textual/terminal project?"* The mechanism qualifies (a Textual SVG export emits `&#160;` entities for spaces regardless of whose app it is; Rich colour lives in spans, not in the string). The **illustrating example must be de-identified** — write it as *a rail label* or *a menu entry*, never as the s19_app string `"CRC Designer"`. Everything markdown-it / Mode-B / report-composer stays in leg 2, which is where the project-bound mechanics belong. Note the section is tagged `[travels]`, i.e. it already claims to hold beyond Textual — the extension must earn that tag or drop it.
      4. **TUI COURSE — `G:/My Drive/Courses/textual/` (67 files, M0–M21). ▶ THE PLAN NOW LIVES NEXT TO THE COURSE: `G:/My Drive/Courses/textual/PENDING-UPDATES.md` — written 2026-07-27, self-contained, status NOT STARTED.** It carries the M13/M14/M17 assignments, the harness gap, and the where-goes-what table, so whoever opens the course finds it without reading this repo. This bullet is the POINTER; the plan is the source of truth for that leg. Deliberately NOT added to `G:/My Drive/Courses/_learnings.md` — that file is the `/html-visualizer` skill's instructional-craft diary and its own contract says entries come from real lesson-building sessions after headless verification passes; no lesson was built here.** **MY EARLIER CLAIM HERE WAS WRONG AND IS WITHDRAWN:** I searched only the Obsidian vault (`02 - Conocimiento`), found zero TUI material, and concluded "the premise is wrong — this leg is creation, not update." The search was correct for where I looked; **the conclusion exceeded the scope of the search.** The course exists, outside the vault, and is substantial. Operator pointed to `index.html`. **Plan of what is worth adding — the course is NOT to be taken, only extended:**
        - **M13 — Testing with Pilot (PRIMARY home; already `[G]` and already anchored on the s19_app test suite, so project examples are legitimate here in a way they are NOT in leg 3).** Its current **Pitfalls** are entirely about *timing* — missing `await pilot.pause()`, asserting before a worker finishes — i.e. tests that fail FLAKILY. **Nothing in the module covers tests that PASS WRONGLY.** Add a second pitfalls block, "assertions that cannot fail": (a) assert the **emitted** form, not the rendered one — a Textual SVG snapshot emits `&#160;` entities for spaces, so a literal grep for a rail label returns 0 matches while the label is plainly visible; (b) Rich/Textual colour lives in `render().spans`, not in the rendered string; (c) the mutation loop — inject the falsehood, confirm RED, **verify the mutation actually applied** (a typo'd mutation also "fails", for the wrong reason), restore; (d) **"can it go RED?" as a question distinct from "is it correct?"** — which is P-5, so M13 becomes the teaching surface for BOTH candidates. Extend the existing exercise: take the M12 progress/cancel Pilot test and mutate it to prove it can fail.
        - **M14 — Architecture deep-dive (`[G: compositor/driver]`) — the WHY, one paragraph, not a module.** The compositor is literally where source form becomes rendered form; it is the conceptual anchor that makes M13's rule follow from the architecture instead of being a testing folk-rule.
        - **M17 — Images & pixel canvas: CHECK FIRST, do not assume.** A "pixel" here is an emitted glyph + colour codes, so the same gap may already be covered by the density material. Read before writing.
        - **⚠ Structural gap found while surveying, worth its own decision: `verify_m*.js` harnesses exist for M0–M6 and M15–M20, but NOT for M7–M14** — the eight middle modules, **including M13, the testing module itself**. So the module that will teach "prove your assertion can fail" is one of the eight with no verification harness of its own. Flagged, not fixed; deciding whether to backfill M7–M14 harnesses is a separate call from this content plan.
    - **▸ Execution model (operator-ruled): backlog now, encode in its own batch.** Note the global `/dev-flow` command lives OUTSIDE this repo (`~/.claude/commands/`), so that leg is not covered by the project's PR flow and needs its own before/after record in the batch artifacts.
  - **▸ (P3, new — batch-64 §7.3) `C-1` is registered in the lineage with no encoded text.** Corrected from the earlier "`C-1 … C-9`": `C-2 … C-9` are absent from **both** sides and carry no signal in either direction, so the exception is `C-1` alone. Sibling of **OB-2** (the missing AT/TC registry); park beside it. `C-29` is closed by batch-64.
  - **▸ (P3, new — batch-64 §7.4) the P-3 occurrence count disagrees across three registers** — `~7th` (lineage), `~9` (this file), `8+` (`MEMORY.md`) — **and the enumeration itself was under-drawn: 9, not 8.** Executing `AT-B64-04` found the missing occurrence (a `startswith("| 0x00001")` prefix guard reporting 4 rows where 400 were emitted). Reconcile all three registers or delete the totals; the encoded text deliberately carries **none**.
  - **▸ (P3, new — batch-64 §7.5) `PLAN.md:73-74`'s contiguity claim is REFUTED** and must be corrected in the living plan, visibly, as the orchestrator's own instance of this batch's defect class — **the census counted mentions, not encodings.**
  - **▸ (P3, new — batch-64 §7.6) `C-41` is FREE.** Recorded so the next control-minting batch does not assume the id is taken.
  - **▸ (P2, new — batch-64 §7.7) the R-c length check is NOT like-for-like.** It prices a 3-candidate absorbing control against a 1-candidate one, so it **will fire on every future absorbing control regardless of density**. Revise the check, or it becomes a gate everyone learns to wave through. Weight it, do not dismiss the breach.
  - **▸ ✅ DONE — batch-66, 2026-07-28. (was P1, batch-64 §7.9) the GLOBAL flow commands' backlog-reconciliation rule was STALE after the lane split.** Operator ruling 2026-07-28: **generic text in the global flows + the routing encoded in the project.** Shipped: `~/.claude/commands/dev-flow.md` **4 sites** (:122 Phase-0 input, :231 Phase-6 close, :235 the gate sentence, :252 the standing rule) and — **not part of the item as registered** — `~/.claude/commands/fast-dev-flow.md` **2 sites** (:102, :126), plus a new **`## Backlog routing`** section in `docs/engineering-rules.md` carrying the lane table. The global text now says *"the file, or lane files, that the project's `docs/engineering-rules.md` designates; absent such a designation, `.dev-flow/BACKLOG.md`"*, and the anti-drift invariant is preserved generically as *every open item lives in exactly ONE canonical file; a partition is a split, not a copy*. Evidence (the only diff three of these legs get): `.dev-flow/2026-07-28-batch-66/03-out-of-vcs-evidence.md` + committed `pre-bytes/`. **`C-41` deliberately NOT consumed** — the routing is a fact, not a control, so it carries no `C-NN` id. *(Original text retained below for lineage.)*
  - **▸ (superseded, kept for lineage) (P1, new — batch-64 §7.9, found at the post-merge coherence review) the GLOBAL `/dev-flow` command's backlog-reconciliation rule is STALE for this project after the lane split (#143).** It instructs every batch to reconcile *"the project's **SINGLE** canonical cross-batch backlog: `.dev-flow/BACKLOG.md`"* and states *"the batch is NOT closed until `.dev-flow/BACKLOG.md` reflects it"* — but that file is now a **router holding no open work**, so a batch following the instruction literally reconciles the wrong file and drops its carries. **Placement is the real question and it needs an operator ruling:** the global command is project-agnostic and arguably must not hard-name a project's file layout at all, in which case the lane routing belongs in `docs/engineering-rules.md` and the global text should say "the project's canonical backlog, as its engineering-rules doc defines it". Pre-existing text; batch-64 did not introduce it and did not fix it, being outside the operator's encoding ruling. 5 occurrences in `~/.claude/commands/dev-flow.md`.
  - **▸ (P2, new — batch-64 §7.10, security merge-gate F3) out-of-VCS backups live only in a session-scoped `Temp` directory.** The three destinations outside version control (`~/.claude/commands/dev-flow.md`, `~/.claude/skills/tui-design/VERIFY.md`, the lineage memory) have no git history, so their `.PRE` copies ARE the rollback — and they sit under the agent session's scratchpad, which is not durable across sessions. All four were verified to match their recorded PRE hashes, so rollback worked *this* time. A durable location (or committing the PRE bytes into the batch artifacts) would make it work next time.
  - **▸ (P3, new — batch-64 §7.11, security merge-gate F5, a reviewer correcting its OWN prior ruling) the repo is PUBLIC, which was not the basis of the earlier host-path clearance.** At Phase 2 the security lane cleared the operator's account name in batch artifacts on the rationale *"their vault, their Drive"*. `gh repo view` returns **PUBLIC** — `.dev-flow/` is pushed world-readable, the exact trigger that text named. **The conclusion survives** (no credentials anywhere; `jjgh8` already appears in 38 files on `main` since batch-01, so this batch changes nothing about exposure) but the *rationale* was incomplete and is corrected here rather than left standing. Worth a deliberate ruling on whether `.dev-flow/` artifacts should carry host paths at all, given the repo's visibility.
  - **▸ (P1, USER STORY, new — batch-64 §7.12) MEASURE WHETHER C-40 AND C-42 ACTUALLY FIRE, or admit they are paragraphs.**
    *As the operator, I want evidence that C-40 and C-42 change behaviour in a subsequent batch, so that
    I know we encoded a control and not a paragraph.* **Why this is not rhetorical:** C-40 was measured at
    **9/9** discrimination and **0/5** false positives — but against a **laboratory corpus** assembled from
    batch-63's documented failures. Nobody has measured whether it fires **in the wild**. That gap is this
    batch's own thesis applied one level up: we asked *"is the control correct?"* and never asked *"can it
    go RED on a live batch?"*
    **Acceptance (black-box, and it can fail):** over the next **N=3** batches, count acceptances that were
    **rejected or rewritten while explicitly citing C-40 or C-42**, taken from the gate artifacts rather than
    from memory. **If the count is 0, the control is inert regardless of its 9/9** — and the finding is that
    encoding it changed nothing, which is worth knowing and is currently unknowable. Report the count with
    its citations either way; a non-zero count also tells us WHICH limb does the work in practice.
    **Deliberately not a presence check** — "the text is in the file" is precisely what C-40 forbids.
  - **▸ (P3, new — batch-64 §7.13) the occurrence→clause mapping behind `AT-B64-04` is HAND-AUTHORED, not derived from the rule.**
    The measurer wrote which rider clause carries which occurrence, then measured mutations against that
    hand-made map. **That is C-40's own limb 2 violated inside the measurement that certified C-40** — a set
    drawn from the implementation rather than from the rule. It does not invalidate the 9/9 (the mutations
    did bite, on disjoint occurrence sets) but the *set they range over* is an unproven spec claim, exactly
    what C-31 exists to catch. Fix: derive the mapping from the rider's clause text, or guard it with a
    completeness assertion.
  - **▸ (P3, new — batch-64 §7.14) `AT-B64-04` occurrence #2 has NO executed reddening mutation.**
    The union of all lost-sets is `{1,3,4,5,6,7,8,9}`; occurrence #2's carriers are `B + I`, deleting `B`
    alone returns 9/9, and clause `I` was never deleted. Stated as a gap in the shipped artifacts rather
    than hidden, but never closed. One more mutation closes it.

## NEW — carries opened by batch-66 (2026-07-28)

  - **▸ (RECORD, not a task) batch-66 is DELIBERATELY NOT synced to the vault. Do not "close the gap".** `/dev-flow-sync` was invoked at the operator's request on 2026-07-28 and **correctly did not apply**, for three independent reasons, each verified rather than assumed: (a) the command copies `01-requirements.md` / `02-review.md` / `04-validation.md` / `05-postmortem.md` / `06-docs/` and batch-66 has **none of them** — it ran a fast-flow shape and its only artifacts are `03-out-of-vcs-evidence.md` + `pre-bytes/`; (b) **precedent — `2026-07-24-batch-61`, the comparable `/fast-dev-flow`, is ABSENT from the vault**; the vault carries full `/dev-flow` batches only; (c) `state.json` never described batch-66 at all. **Forcing a sync would write a folder with five missing artifacts and near-all-`null` metrics — a record that appears to cover what it does not**, which is precisely the defect batch-66 corrected in the `dev-flow-lessons` skill. Written here because this project has already burned a batch on a **false** sync gap: the batch-61 router entry records the claim *"b56 + b57 + b58 + b59 STILL PENDING"* as **stale and wrong on all four**, discovered only by checking the vault on disk. A missing `batch-66` folder is **expected state**, not a gap.
  - **▸ (P2, NEAR MISS — structural, worth an operator ruling) `.dev-flow/state.json` is a SINGLE-batch file and this project routinely runs CONCURRENT batches.** batch-66's closeout was about to correct what looked like a stale `phase_status: "awaiting-sync"` left over from batch-64. **It was not stale — it was gone:** `b691f21` (batch-65, the parallel session, merged between batch-66's branch base and its merge) had already overwritten `state.json` to `batch_id: "2026-07-28-batch-65"`, `phase_status: "in-progress"`. Editing it would have corrupted **another agent's live working state** mid-batch. Caught only because the pre-edit read disagreed with a read taken minutes earlier, and the discrepancy was traced instead of reconciled. **The structural problem:** one mutable file, last-writer-wins, no owner field, no concurrency guard — so *"whose batch is state.json describing right now?"* has no reliable answer while two batches are open, and every `/dev-flow-sync` pre-requisite is keyed on it (a sync run while it points elsewhere targets the WRONG batch). Same id-allocation disease as **OB-2**, one layer over: batch-64 vs batch-65 collided on the artifact namespace, and here they collide on the state file. **Options for the ruling:** per-batch state files (`state-<batch_id>.json`) with a pointer, an `owner`/`session` field plus a read-verify-write guard, or an explicit convention that concurrent batches never write it. **Also note batch-64's real residue survived the overwrite unrecorded:** its final state was `phase_status: "awaiting-sync"` **with** `obsidian_synced: true` — the sync set the flag and never advanced the status, so the pre-requisite-2 check would have passed a second sync against an already-synced batch.
    **▸ AMENDED 2026-08-30 (flow rev51, routed from batch-88 + batch-89) — the ROLLOVER half of this item is now RULED; the CONCURRENCY half is NOT, and the item stays open on that half alone.** What landed: `/dev-flow` gains **§Batch rollover**, a retire-or-carry table over **every** single-slot field, ruling that outgoing `decisions_log` entries are **ARCHIVED into the closing batch's own record and MOVED, never copied** (`.dev-flow/<outgoing>/decisions-log.json`), then the slot resets to `[]`, written by **exactly one act** — the rollover, before `batch_id` changes. `/dev-flow-sync` takes a **batch-id argument** so `state.json` is the DEFAULT source and no longer the only one, and its SUPERSEDED branch **writes nothing to `state.json`**. `V29` executes the property. **The one growing log with a `batch` tag per entry was considered and REJECTED** on C-50 (an artifact has ONE home). **What this item asked for and did NOT get: an `owner`/`session` field, a read-verify-write guard, or an explicit convention that concurrent batches never write the file.** ⚠ **And this bullet's own last sentence was too generous to the check it criticised:** rev51 measured that pre-requisite 2 tested **`current_phase >= 6`, a key this project's schema does not have** — it uses `current_station` — so the condition **never failed, never passed and never ran**, for every batch here, and always had (`S-4`). It could not have "passed a second sync"; it was not evaluating anything. **The near miss this bullet records remains unguarded**, and the file is still last-writer-wins across concurrent batches.
  - **▸ (RECORD) `tui-design` / the TUI course (P-3 leg 4) are under ACTIVE work by parallel agents as of 2026-07-28 — do NOT pick up that leg.** Operator-stated at the batch-66 close. The leg stays open in this lane and its plan stays authoritative at `G:/My Drive/Courses/textual/PENDING-UPDATES.md` (whose cross-reference batch-66 corrected), but **whoever reads this backlog next must not start it without re-confirming ownership.** This project's collision history is not hypothetical — batch-58 needed operator adjudication between two sessions, batch-64/65 collided on a batch number and forced a renumber + rebase, and the near miss above is the third instance in a week.

  - **▸ (P2, CONTROL CANDIDATE — needs its own AskUserQuestion before encoding) a defect registered against ONE flow command must be swept across BOTH.** batch-64 §7.9 named only `~/.claude/commands/dev-flow.md`; executing it found the **identical defect in `fast-dev-flow.md` at 2 sites**, unregistered. Had the item been implemented as written, it would have shipped a half-fix leaving `/fast-dev-flow` still pointing at a router with no open work — and the batch would have closed reporting the carry DONE. The two commands share their close contract almost verbatim, so **any change to a shared clause is a two-file change by default**. This is the C-38 shape ("a widget-type swap is a test-API change: sweep every query of that class") one layer up, at the process-document layer, and the same family as the un-encoded **1d markup-sink SWEEP rule** below — consider encoding them together as one *sweep every site of the class* control rather than two narrow patches (`feedback_general-controls-not-narrow-patches`).
  - **▸ (P2, CONTROL CANDIDATE — needs its own AskUserQuestion) a catalog's self-declared SCOPE is a claim, and it must be measured at every extension, not carried.** The `dev-flow-lessons` skill declared **38 post-mortems** in its description, intro, honesty note and provenance while its own body cited controls earned as late as `b64` (C-39/C-40/C-42) — an index **understating its own coverage**, so a reader deciding whether to consult it was doing so on a stale figure. Measured 2026-07-28: **54**. Fixed in batch-66, and the provenance now records the derivation (`ls .dev-flow/*/05-postmortem.md | wc -l`) so the next extension re-measures. **General form:** any artifact stating its own coverage ("distilled from N", "covers X through Y", "all N call sites") carries a number that drifts silently the moment the corpus grows — the same failure class as C-39 (executable thresholds) applied to *documents about the work* rather than to gates. **Two traps worth carrying with it:** the corpus size is **not** the highest batch number (56 batch dirs exist, numbering has gaps, so neither 64 nor 56 is the answer); and the fix is **not** a find-replace — 11 of the 16 "38"s in that file are batch citations (`b38`, `35-38`) and one is a hue value (`38.44°`).
  - **▸ (P3) audit the other self-declared scope figures across the process surface.** Direct consequence of the candidate above, and deliberately NOT done in batch-66 (out of the approved scope). Unaudited: the `tui-design` skill and its `VERIFY.md`, `~/.claude/skills/README.md`, the distilled catalog at `G:\My Drive\Courses\dev-flow-lessons\index.html` (which mirrors the skill and therefore almost certainly still says 38), and this repo's `CLAUDE.md`/`REQUIREMENTS.md` count claims. Cheap to check, and each one is read as authoritative.
  - **▸ (P1, CONTROL CANDIDATE — needs its own AskUserQuestion) bytes committed as EVIDENCE must be stored verbatim, and the store must be verified, not assumed.** batch-66 committed `.PRE` copies of four out-of-VCS files whose **recorded SHA256 is the entire rollback contract** — and `core.autocrlf=true` silently normalised them on the way in: `dev-flow.md.PRE` was stored as `d46bb314…` against a recorded `e103af29…`. The commit looked healthy; a rollback from it would have restored bytes that do not hash to the original. Caught only because the ledger was re-checked against the *index blob* rather than the working copy. **Two traps to carry:** the corruption is **bidirectional and half of it is invisible at commit time** (a CRLF file normalises to LF going in; an LF file converts to CRLF coming out at the next checkout, so its blob matches and its checkout does not); and **`text eol=lf` is the WRONG fix** even though it is the pattern this repo already uses for snapshots and goldens — evidence bytes carry mixed line endings by nature, so only `-text` is correct. Fixed for this batch via `.gitattributes` (`.dev-flow/**/pre-bytes/** -text`), verified `MATCH` on all four blob-vs-worktree pairs. **General form:** whenever a hash is the contract, the storage layer is part of the system under test — assert the stored artifact, not the one you handed to the store. Same family as C-42 (*assert the EMITTED form of the producer*) with git as the producer, and it is batch-63's CRLF lesson recurring inside the evidence mechanism of a batch that cites batch-63.
  - **▸ (P2, note on batch-64 §7.10 — NOT closed, but not repeated) batch-66 committed its own `.PRE` bytes into `pre-bytes/` inside the batch artifacts** rather than leaving them in a session-scoped `Temp` directory, which is one of the two remedies §7.10 names. **The general rule is still unencoded** — the next batch touching out-of-VCS destinations will do whatever it decides to. §7.10 stays open.

## NEW — 2026-07-31: the backlog-carry control has a hole on the INPUT side

  - **▸ (P1, CONTROL CANDIDATE — needs its own AskUserQuestion before encoding) a deferral written in a PROTOTYPE, ADR or AUDIT never reaches the canonical queue, because carry-over only fires at batch CLOSE.** Operator-raised 2026-07-30 (*"¿se está haciendo carry del backlog? no veo algunas features pendientes"*) and then measured.
    **The control that exists and what it does not reach.** `feedback_backlog_carryover_enforced` makes reconciliation a mandatory close step, so it sweeps what a *batch* produced. A deferral born **before** a batch — in a `/prototype` verdict's "Non-goals" section, an ADR roadmap, or an audit's findings list — is not a batch artifact, so no step owns it. **It is the mirror image of what the flow already guards:** carries OUT of a batch are enforced; deferrals INTO the pipeline are not.
    **Measured, not suspected.** Audited 2026-07-31 against `6524afd`. ▸ The 2026-07-27 lane split (`082ada9`) is **CLEAN** — every pre-split item was compared one by one against the union of the two lane files and all migrated (144 lines → 191); the leak is **not** in the split. ▸ **Six** items were found deferred in `prototypes/screen_upgrades.HANDOFF-PLAN.md` §9, the 2026-07-15 screens audit and the 2026-07-14 patch-editor verdict, with **0 occurrences** across all three backlog files for each of `omni-search`, `linked workbench`, `identity header`, `curated footer`, `S0 header`, `120-col`, `layout cap`, `column cap`, `font scale`, `per-line`, `ScreenScaffold`. Now registered in [`BACKLOG-CODE.md`](BACKLOG-CODE.md).
    **The precedent is in this repo and it was closed by hand, not by a control.** `BACKLOG-CODE.md:108` records the identical failure at the 2026-07-21 Flow Builder reconciliation — *"Items 4-6 below were only in memory/ADR and had NOT been written into this canonical queue — now captured + prioritized."* One section, one time, no mechanism. **Two independent occurrences of one failure mode is this project's own historical threshold for encoding.**
    **Proposed shape (do not encode without the AskUserQuestion).** At the close of any `/prototype`, design pass or audit that produces a deferral list, **transcribe every deferred item into the canonical queue before the artifact is considered done** — the artifact's own "Non-goals / Deferred / Parked / Out of scope" headings are the checklist, and the discharge is a per-item line in the queue, not a pointer back to the document. **Two traps measured while registering the six:** (a) **three of the six came back NARROWER than their source** once re-executed — batch-47 had shipped part of the identity header and the loader-facts count, so a verbatim transcription would have planted three false claims, meaning the rule must require **re-execution at transcription time, not copying** (the C-40 / *"a carried number is re-derived, never copied"* family); and (b) one item (curated footer) was **already tracked under a different name**, so the rule needs a dedupe step or it will manufacture duplicates — the exact anti-drift invariant the router protects.

## NEW — operator-flagged 2026-07-28: `/tui-design` did not catch two shipped design defects

> **Written here by the CODE lane, per the router's "move, never copy" rule.** The app-side defects live in [`BACKLOG-CODE.md`](BACKLOG-CODE.md) under the 2026-07-28 operator-flagged section; **this** entry is only the skill-side question. Leave no stub in the other file.

- **▸ (P1, NEW) the `/tui-design` skill has no CONTROL-SEPARABILITY guard, and user testing found the gap before the skill did.** Operator, on the CRC Designer: *"hay switches que parecen uno sólo, faltan agregar guardas de diseño, espero que la siguiente vez la habilidad de tui-design atrape estos problemas."*
  **The concrete miss, measured (see the CODE-lane bullet for the full mechanism):** two adjacent `Switch` widgets styled `border: none; height: 1` inside `height: auto` rows with no separator abut and read as a single wider control. The skill's PROTOTYPE mode captures headless SVGs and its checklist covers hierarchy, grouping, focus and render budget — but **nothing asks whether two adjacent interactive controls remain visually distinguishable at the density the theme produces.**
  **Why this is the skill's problem and not just the screen's:** batch-59 shipped design-fidelity ATs that were considered the fix for exactly this class (`AT-B59-03/08`, asserting 3 distinct bench-column ancestors). Those assert **structure** — that containers exist — and are satisfied by a layout whose controls have merged. So the encoded control and the skill both pass a screen a user cannot operate. **A structural assertion is not an affordance assertion**, which is the same shape as the project's standing lesson that a predicate must test what its label claims.
  **Proposed direction (needs its own AskUserQuestion before touching `~/.claude/skills/`):** add a *separability* item to the skill's self-critique pass — for every pair of adjacent interactive controls, is there a rendered boundary (border, margin, label gutter, or grouping chrome) in the captured SVG? It is checkable against the artifact the skill already produces, so it costs one more question, not new tooling.
- **▸ (P2, NEW) `/tui-design` should be re-run over the Legend modal family, and the skill should say when a re-run is due.** Operator: *"las pop up screens con la leyenda necesitan mejor reorganización… necesitamos checar de nuevo con tui-design."* The app-side work is in the CODE lane. The **process** question is that `LegendScreen` took two purely additive passes (N1 per-screen filtering, N8 example cards) with **no layout pass between them**, and nothing in the flow flagged that an additively-grown screen was due for redesign. Worth a cheap heuristic in the skill or in `docs/engineering-rules.md`: *N additive changes to one screen without a layout pass ⇒ schedule one.*

## MAJOR — there is no AT/TC registry

> ⚠ **THE FIGURES IN THE BULLET BELOW WERE RE-DERIVED 2026-07-31 AND THREE OF FIVE DO NOT REPRODUCE** — see
> *NEW — 2026-07-31: carries opened by the AT/TC registry Lane B design* at the end of this file. **They are
> retained verbatim** as the record of what was claimed and acted on; do not silently patch them in place.
> Cite the corrected values from `AT-TC-REGISTRY-SPEC.md` §1.1, never from this bullet.

  - **▸ (MAJOR, new — batch-63 OB-2, operator-identified) THERE IS NO AT/TC REGISTRY, and that is why id collisions keep recurring.** The operator's diagnosis, then measured. **The collision mechanism, isolated:** "the next free `TC` id" evaluates to **345 / 398 / 479** depending on whether you grep `REQUIREMENTS.md`, `+ tests/`, or `+ .dev-flow/` — a **134-id spread**, so two authors consulting different subsets *both* believe they are in free space. That is structural; it is not anyone's carelessness, and it defeats mitigations aimed at authors. **Measured on `031ca8d`, in three directions:** (a) `REQUIREMENTS.md` registers **20 of 73** live `AT` ids (**73 % unregistered**) and **97 of 188** live `TC` ids (**52 % unregistered**); (b) **6 phantom `TC` ids** — `TC-319/320/324/325/326/327` — are registered with **no node in either `TC-NNN` or `def test_tcNNN` form**, so the requirements doc asserts verifiers that do not exist; **`TC-319` is the exact node `CLAUDE.md` cites as the origin of control C-26** (`test_tc319_regroup_section_structure_census`), removed or renamed without the registry noticing; (c) **75 `AT` and 119 `TC` ids exist only in `.dev-flow/` batch docs**, minted by superseded designs and polluting the free pool. A fourth layer: **132** ids appear as function names vs **188** as `TC-NNN` text, so 56 are cited in tests with no node bearing the name — the same gap as the open C-3 item below, at full scale. **Evidence of the cost, twice in one batch:** batch-63's REV-4 lanes minted overlapping `TC-376..387`/`AT-157..159` with different content *and* reused live batch-48 ids `R-TUI-079/080/081` (2 of 11 Phase-2 blockers); the re-scoped Phase 1 then did it again — fixing id *ranges* before dispatch prevented range overlap but not per-id *semantics*, and 9–10 of 12 `AT` ids bound to different observables, found independently by all three Phase-2 lanes. **Shape of the fix:** a single registry file that is the one authority for id allocation, plus a guard test that FAILS when the registry and `tests/` diverge in either direction (unregistered node, or registered id with no node). Subsumes C-3 below and the batch-62 "16 of 23" carry. Deliberately NOT pulled into batch-63 (operator ruling 2026-07-27) — it is a project-wide process change and batch-63 was already at its iteration cap.
  - **▸ ✂️ SPLIT PER ROUTER AMENDMENT A (operator ruling 2026-07-31). The item above is CROSS-LANE and its two halves are now separately actionable.**
    **▸ ✅ Lane B half — DESIGN: DELIVERED 2026-07-31, [`.dev-flow/AT-TC-REGISTRY-SPEC.md`](AT-TC-REGISTRY-SPEC.md)
    (PR #174, `0fdc88b`), operator-reviewed and merged.** Done-condition *"the spec exists and is reviewed"* met.
    Operator rulings at kickoff: **AT + TC only** (`R-*`/`LLR-*`/`US-*` scoped out — residual exposure now
    registered below), **no batch number**, gate-per-increment. The spec departs from the letter of this item on
    three approved points: the guard also reads `REQUIREMENTS.md` (§5.2 — the two-way guard as worded below
    **would not detect a single one of the 6 phantoms** it cites as evidence), JSONL over TOML (§3.1), and
    ~167 unaudited ids burned (§6.3). **This does NOT close the item** — see the two-line rule at the end of
    this bullet. *(Original charter retained verbatim below.)*
    **Lane B half — DESIGN (this file, actionable now).** Produce the spec: the registry format and where it
    lives; the guard's semantics in **both** directions (unregistered node · registered id with no node); the
    allocation rule that closes the **134-id spread** (*"the next free `TC` id"* evaluates to **345 / 398 / 479**
    depending on whether you grep `REQUIREMENTS.md`, `+ tests/`, or `+ .dev-flow/`, so two authors both believe
    they are in free space); the disposition of the **6 phantom TCs** (`TC-319/320/324/325/326/327` — note
    **`TC-319` is the node `CLAUDE.md` cites as the origin of control C-26**) and of the **75 `AT` / 119 `TC`**
    ids that exist only in `.dev-flow/`; and how the guard stays cheap in CI. **Done-condition: the spec
    exists and is reviewed.** ⚠ **Does NOT depend on today's id values, so it is NOT blocked by batch-74** —
    which is actively renumbering ids (`96bcbd7`, the **third** collision in two days).
    **Lane A half — BUILD:** staged below under *PENDING MOVE TO LANE A*.
    **Neither half closes the item.** A batch that ships the registry file and reports "AT/TC registry DONE"
    has shipped the half without enforcement — a document that drifts silently, i.e. the defect itself.
  - **▸ ✅ CLOSED 2026-07-31 — C-3 is discharged by the registry's Lane A guard (PR [#181](https://github.com/jav201/s19_app/pull/181)); this bullet is the explicit statement the item asked for.** The closure was **owed and had not been made**: the Lane A bullet said *"closes C-3 and the batch-62 '16 of 23' carry — state that explicitly at close rather than letting them lapse"*, PR #181 merged, and C-3 stayed open. **Verified by execution on `038dfd9`, not by reading the PR:** the discharging guard is **G2 — `tests/test_id_registry.py::test_tc601_g2_live_entries_name_existing_nodes`**, whose subject is exactly C-3's direction (*a registered entry must name an existing node*), with **G4** (`test_tc603_g4_requirements_citations_are_live`) covering the `REQUIREMENTS.md` citation axis. **13 passed.** And the guard is not *assumed* able to fail — **`AT-281` (`test_at281_every_guard_rule_can_fail`) drives a reddening substitution for every rule G1…G7**, which is the C-40 discharge and the reason this closure is stated rather than hoped. **The batch-62 "16 of 23" carry closes with it, same mechanism.** ⚠ **For the record: this is the SECOND explicitly-addressed one-sentence action in a week to survive its own named gate** — the first was the *PENDING MOVE TO LANE A* transfer, which four batch-74 PRs skipped. Both are evidence for the input-side carry-over control open in this lane: *naming an owner in prose does not schedule the work.* Original text follows.
    ~~**▸ (P2) C-3 — 16 of 23 TC ids are not traceable to any node.** Seven now are, by node name; the rest are covered by consolidated batteries with the mapping in `04-validation.md` §3. Folded into P-2. **Subsumed by the AT/TC registry above — it is the registry's Lane A guard that closes this, not the Lane B spec.**~~

## Control candidates raised and DECLINED (carried, not dropped)

  - **▸ CONTROL CANDIDATES RAISED AND DECLINED at batch-62 Phase 5 — carried, not dropped.** Each needs its own AskUserQuestion before encoding. **(P2) P-2 — every normative ruling gets an owning increment, checked at the validation gate.** THE ONE I WOULD RAISE AGAIN: it is the only candidate covering a gap **no existing control touches**. `D-20` (`MAX_REPORT_ISSUES_PER_VARIANT`) was ruled `shall`, recorded in §6.5 with a Before/After, and then appeared in **no increment's file list** — it sailed past five increments and was caught only because Phase 4 grepped for the symbol, while the batch had meanwhile *doubled* that section's per-issue cost. C-18 gives every AT a node; nothing gives every *ruling* an owner, and the amendment log is write-only. Would subsume the open code↔requirement reverse-index item (1c) rather than adding a parallel control. **(P2) P-3 — assert the emitted encoding, never a character list or the rendered form.** ~7th occurrence over 3 batches, **4 in batch-62 alone**: `&` unescaped so `&vert;` forges a table fragment with every token still `text`; an amendment naming `Cc`/`Cf` that missed `U+2028` (`Zl`); a source-line regex guard that false-positived on an implicitly-concatenated f-string (fixed with AST); and `"](" not in note` failing against a CORRECT implementation because the escaped form is `\](`. Split placement: general form → global `/dev-flow`, markdown-it / Rich-vs-Textual specifics → `docs/engineering-rules.md`. **(P3) P-4 — a guard must be shown able to fail**: a guard that fires nowhere on install is indistinguishable from one that cannot; complements C-32. Handled ad hoc in batch-62 (`test_a16_*` drives the residue guard to `AssertionError`).

## Deferred control-encodes (need their own AskUserQuestion before touching `~/.claude/commands/`)

- **1c — code↔requirement REVERSE-index control** (deferred batch-48): `resource → {claimants}`, flag ≥2 claimants in one scope, WITH a CI staleness guard (every `R-*`/`LLR-*` code tag names a live requirement). Half-built: 25 back-refs in `screens_directionb.py`; `REQUIREMENTS.md` maps `R-*`→files. Detail: `project_devflow_control_lineage`.
  **✂️ CROSS-LANE (Amendment A).** **Lane B half (here):** define the control — what a "claimant" is, what
  scope it ranges over, and when ≥2 claimants is a defect rather than legitimate shared ownership. **Lane A
  half:** the CI staleness guard, staged below. **Dependency: B → A** — the guard cannot be written before
  the control says what it enforces.
- **1d — markup-sink SWEEP rule** (un-encoded candidate): when a markup sink is found, sweep EVERY site of that widget class (4 surfaces, 3 separate discoveries: batch-33 `screens.py` → batch-43 tooltips → batch-48 DataTable + Select). Assert `plain` verbatim AND `spans == []`.
  **✂️ CROSS-LANE (Amendment A).** **Lane B half (here):** encode the *rule* — a markup sink found at one
  site obliges a sweep of every site of that widget class. Consider encoding it **together** with the
  *"a defect registered against ONE flow command must be swept across BOTH"* candidate above: both are
  instances of **sweep every site of the class**, and this project's standing preference is a general
  control over two narrow patches (`feedback_general-controls-not-narrow-patches`). **Lane A half:** the
  actual sweep assertions over the 4 known surfaces, staged below.

## ✅ MOVED TO LANE A 2026-07-31 — the three cross-lane Lane A halves

> **Executed, not staged.** The three Lane A halves (AT/TC registry BUILD · 1c CI staleness guard ·
> 1d markup-sink sweep assertions) now live in [`BACKLOG-CODE.md`](BACKLOG-CODE.md) under
> *"Arrived 2026-07-31 by the router's Amendment A"*. **A move, not a copy — nothing of them remains
> here.** Their **Lane B counterparts stay in this file** (the registry's design half, and the control
> definitions for 1c and 1d), per router [Amendment A](BACKLOG.md): *neither half closes its item.*
>
> This heading is a **signpost, not open work** — it exists so a reader following an older reference to
> *PENDING MOVE TO LANE A* lands on the disposition instead of on nothing. Delete it once no live text
> points here.

## 🆕 Registered 2026-07-31 (batch-74) — control candidate: vacuous FIXTURES

  - **▸ (CONTROL CANDIDATE — new class, not covered by the existing catalog) the catalog covers vacuous PREDICATES; it does not cover vacuous FIXTURES.** A predicate can be correctly written, correctly scoped, and quote exactly the right formula, and still be unable to fail — because the **fixture's own symmetry** collapses the distinction the predicate tests. **Executed at batch-74 Inc-2:** every `Address` fixture was all-`F`, and on an all-`F` value (i) rendering the **trailing** K hex digits is **byte-identical** to rendering the **leading** K, and (ii) `bit_length() // 4` is byte-identical to `(bit_length() + 3) // 4`, because the `+3` is a no-op whenever the top nibble is ≥ 8 — which every `F`-leading value satisfies. The node that could not discriminate was **`TC-549`, whose own assertion QUOTES that ceiling formula**. Both defects survived a 29-node suite and were found by the independent reviewer, not by the author's C-40 pass, because **the mutation changes no output on that fixture**. **Proposed control:** for any predicate over a positional, digit-wise or order-dependent property, ask *"what does this fixture make indistinguishable?"* and add one **deliberately non-uniform** arm with its discriminating precondition ASSERTED (batch-74's arm asserts `bit_length() % 4 != 0` and `total == len(raw) - 2` before it asserts the behaviour, so a fixture that stopped discriminating fails loudly instead of passing quietly). Sibling of C-40 — C-40 asks whether the PREDICATE can go red; this asks whether the FIXTURE lets it. Needs operator approval before encoding (per feedback_devflow_control_encode_approval).

## 🆕 Registered 2026-07-31 (batch-74) — a phase can pass its gate with NO artifact at all

  - **▸ (MAJOR — process defect, caught by `/dev-flow-sync`'s template-completeness detect, which is exactly what it was written for) batch-74 reached MERGE with no `04-validation.md` and no `06-docs/`.** Phase 4 genuinely ran — the full gate suite executed twice, the 17-mutation counterfactual ledger was built, both engine-frozen guards were checked, CI was read with its blind spots named — and Phase 6 genuinely ran. **But neither produced its artifact**, and every prior batch in this project ships both. The orchestrator self-approved Phase 4 against runs it held *in conversation*, not against a document, so **there was nothing for a gate to read**. The existing detect is scoped to an artifact that EXISTS but is structurally empty (origin: the 2026-06-23 audit, RC-1); it has no rule for an artifact that is **absent**, and absence is the weaker failure to notice because nothing is there to look wrong. **Proposed control:** the phase gate asserts `Test-Path` on its own artifact BEFORE it may record an approval, and `/dev-flow-sync` treats a missing required artifact as the same blocker class as an empty one (it currently enumerates only 01/02/04/05/06-docs for *content*, and silently proceeds when the file is not there). Cheap, mechanical, and it fires at the gate rather than after the merge. **Second-order note worth keeping:** the artifacts were reconstructible only because every number had been recorded in `state.json`, the increment packets and the close-out as it happened — a batch with a thinner decisions_log would have had to re-run a 30-minute suite to write its own validation record, or would have invented it. Needs operator approval before encoding (per feedback_devflow_control_encode_approval).

## NEW — 2026-07-31: carries opened by the AT/TC registry Lane B design

> Opened by [`AT-TC-REGISTRY-SPEC.md`](AT-TC-REGISTRY-SPEC.md) (PR #174). Registered here rather than folded
> into the spec, so that a reader of the queue does not have to read the spec to find the open work.

  - **▸ (P2, NEW) the `R-*` / `LLR-*` / `US-*` id spaces are unregistered and have DEMONSTRABLY collided — and the AT/TC registry does not cover them.** Operator ruling 2026-07-31 scoped the registry to **AT + TC only**; this bullet exists so that scoping is a recorded decision rather than an oversight discovered later. **The exposure is measured, not speculative, and it is in the parent item's own evidence:** batch-63's REV-4 lanes reused **live batch-48 ids `R-TUI-079/080/081`**, counted there as 2 of 11 Phase-2 blockers. That is an `R-*` collision, not an AT/TC one — so shipping the AT/TC registry closes the mechanism in 2 of roughly 5 id spaces and **leaves the demonstrated instance uncovered**. ⚠ **Therefore: whoever closes the Lane A build must NOT report "id collisions are fixed."** The honest close is *"AT and TC are now allocated from a single authority; `R-*`/`LLR-*`/`US-*` are not."* **Why deferral is defensible rather than merely convenient:** the spec's §2–§5 are written over a generic "id space" and its schema already carries a `space` field, so extending to `R-*` is a seed + a widened regex, not a redesign. **What has to be decided first, and is why this is not a trivial follow-on:** `R-*` ids live overwhelmingly in `REQUIREMENTS.md` with *requirement* semantics, not *verifier* semantics — an `R-*` id has no node, so guard rules **G1/G2 do not transfer** and the equivalent of "registered id with no node" has to be defined from scratch. Do not start this by copying the AT/TC guard.
  - **▸ (P3, NEW) THREE of the five figures in the MAJOR item above do not reproduce, and a fourth attaches a correct percentage to a false sentence.** Re-derived 2026-07-31 on `031ca8d` — the item's **own** stated ref — with the pattern and corpus declared for each. Full table + derivations: spec §1.1.

    | Figure as carried | Re-derived on `031ca8d` | Verdict |
    |---|---|---|
    | 6 phantom TCs `TC-319/320/324/325/326/327` | exactly those six | ✅ **CONFIRMED** id-for-id (a 7th, `TC-496`, has since appeared) |
    | TC **52 %** unregistered — *"registers 97 of 188"* | 52 % ✅ but it registers **91** of 188. The two 97s are different quantities that coincide: `97 registered − 6 phantoms = 91`, and `188 − 91 = 97 unregistered` | ⚠️ percentage right, **sentence false** |
    | AT **73 %** unregistered (*"20 of 73"*) | **50 %** (60 of 119) under `\bAT-(\d+)`. 20/73 reproduces **only** under `\bAT-(\d{3})\b`, which silently discards every suffixed id (`AT-065b`) | ❌ **does not reproduce** |
    | spread of **134** — *345 / 398 / **479*** | 345 ✅ · 398 ✅ · **`479` has ZERO occurrences anywhere in the repo.** What is real: a naive max-scan returns **1729**, from the single outlier `TC-1728` | ❌ **does not reproduce**; the third term and therefore the spread are unsupported |
    | **75 `AT` / 119 `TC`** exist only in `.dev-flow/` | **30 AT / 65 TC** at that ref, under any exclusion variant tried | ❌ **does not reproduce at the stated ref** |

    **The finding is not the arithmetic — it is that no figure declared its grep pattern or its corpus.** That is the parent item's own thesis one level up: *if "an id" is undefined, every measurement invents a definition, and two honest measurements disagree.* The 73 %/50 % divergence **IS** that ambiguity, measured — both patterns are defensible, they range over different universes, and nothing in the citation says which. **Consequence, and it is why the spec opens with a section the item does not ask for:** the root cause sits one layer **above** allocation. A registry that assigns ids without first defining what an id *is* inherits the bug it was built to fix. **Disposition: the originals are RETAINED verbatim** in the MAJOR item (flagged in place) as the record of what was claimed and acted on — patching them silently would destroy the evidence of how they got there. **Control candidate, NOT encoded** (needs its own AskUserQuestion per `feedback_devflow_control_encode_approval`): *a cited figure must carry the pattern, the file set and the ref that produced it, or it is not re-derivable — only re-inventable.* Companion to *"a carried number is re-derived, never copied"*: **re-derivation is impossible unless the original stated its derivation.** Same family as C-39.
  - **▸ (P3, NEW) the MAJOR item mislocates `TC-319`'s citation.** It states *"`TC-319` is the exact node `CLAUDE.md` cites as the origin of control C-26."* **In this repo `CLAUDE.md` contains neither `TC-319` nor `C-26`.** `TC-319` is cited 3× in `REQUIREMENTS.md` (`:3656`, `:3694`, `:3902`); `C-26` 5× in `REQUIREMENTS.md`. **The substance stands and is if anything worse than stated** — a control's cited census node has no implementation, and `test_tui_patch_layout.py` (the file `TC-319` names) *does* exist while `test_tc319_*` does not, so this is a rename-or-removal that has to be determined rather than assumed (spec §6.1). **Why this is worth a bullet rather than a silent fix:** anyone who greps `CLAUDE.md` to verify the finding gets zero hits and may conclude it was withdrawn.
  - **▸ ✅ DISCHARGED 2026-07-31 — the move was executed** (see *"MOVED TO LANE A"* above; the three halves are in `BACKLOG-CODE.md`). **The finding this item made is what survives, and it is worth keeping:** the instruction named *"whoever closes batch-74"*, batch-74 then closed across **four** merged PRs including a dedicated state/merge-record pass, and **the move was in none of them** — an explicitly-addressed, single-sentence action was skipped by four consecutive passes that each read the file. It took a fifth, separately-directed pass to execute. *A staged move is not a scheduled one; if nothing owns it at a named gate, it does not happen.* **Candidate evidence for the input-side carry-over control** registered earlier in this file. Original text follows.
    ~~**▸ (RECORD, not a task — but it is an UNDONE action, do not read past it) the *PENDING MOVE TO LANE A* section above is STILL PENDING.**~~ It instructs *"ACTION FOR WHOEVER CLOSES batch-74, OR THE NEXT LANE-A BATCH: move these three bullets verbatim into `BACKLOG-CODE.md` and delete this section."* batch-74 is now **fully closed — four merged PRs, `537f27d` (#173) + #175 + #177 + #178 — and performed it in none of them.** Verified against `origin/main` @ `69f8f42`, not assumed: the only batch-74 edit to this file is an append of two new sections. **The close-out PRs are what makes this worth registering rather than merely noting** — the instruction names *"whoever closes batch-74"*, the close-out ran across three further PRs including a dedicated state/merge-record pass, and the move was not among them. The three Lane A halves therefore still sit in the PROCESS lane, which is the exact anti-drift invariant the router protects (*every open item lives in exactly ONE canonical file*). Deliberately **not executed here**: this session's scope is the Lane B design and its carries, and the move writes to `BACKLOG-CODE.md`. **Next Lane-A batch owns it.**

## 🆕 Registered by batch-75 (2026-07-31) — flow-manifest drift

  - **▸ (P2, NEW — found at batch-75 Phase 0 by running the C-45 currency check) the `FLOW-VERSION.md` manifest is STALE against the shipped lessons catalog.** Local aggregate `896dcca61cf68d78` ≠ manifest `flow_hash 0127a2767ff11c8a` (`flow_version 2026.07.28-rev1`). **Direction executed: local is AHEAD, the manifest is behind.** All **11** control-bearing files (`commands/*.md` ×4, `templates/dev-flow/*.md` ×7) are **byte-exact**; the sole divergence is `skills/dev-flow-lessons/SKILL.md` — **641 lines local vs 620 stamped**, hash `01d608bb14069613` vs `5c47db86ac2cf4ae`. The file is **untracked** in `~/.claude` (`.gitignore:3` = `/*`; `git ls-files skills/` → 0), its canonical home being `jav201/claude-skills`. **Fix: re-stamp the manifest for the 641-line catalog, or record in `FLOW-VERSION.md` that the catalog is deliberately outside the hash and why.**
    **Why this is worth a bullet and not a silent re-stamp:** the manifest exists to make C-45's PULL obligation *mechanical* — *"a project can prove in one command whether the flow it is about to run is current."* Today that command returns a **mismatch for a flow that is, on every enforceable surface, current**. A check that cries wolf on a clean tree gets ignored, and the next batch that sees a mismatch will wave it through — which is exactly the failure C-45 was encoded to prevent. **The scoping work (11/11 exact, divergence confined to a reference document) had to be done by hand at Phase 0 to reach a non-blocking verdict; the manifest should make that verdict cheap.** Candidate improvement to carry with the fix: split the aggregate into a **controls hash** (commands + templates, gating) and a **catalog hash** (lessons, informational), so the one-command check answers the question that actually matters.

## 🆕 Registered by batch-76's merge-gate closure (2026-07-31) — counterfactual harness design

  - **▸ (P1, NEW — the strongest single-occurrence evidence in the project) a counterfactual verdict must be reported PER RESOLVED NODE ID, never per mutant.** This is §3 item 1 of `HANDOFF-2026-07-31-batch-76-merge-gate.md`, promoted from a hand-off note to a registered candidate because it is now **executed evidence, not a hypothesis**. batch-76's Inc-1 matrix derived one verdict per mutant **from the process exit code** over a node list containing parametrized tests. `pytest` exits non-zero if *any* arm fails, so `M1 gate disabled AT-250/251/252 applied=True RED 3 failed, 4 passed` was recorded as **"RED · INERT: none"** — and the string `4 passed` sat in the transcript unread. Re-executed per-arm at the merge gate: **4 of 7 arms stayed GREEN with `_EmissionGate.fits` substituted for `return True`, i.e. with the byte gate FULLY REMOVED** — `AT-251[1]`, `AT-252[1]`, `AT-252[3]`, `AT-252[10]`.
    **Why the aggregate is not merely lossy but *optimistically* lossy, which is the part worth encoding:** the question a counterfactual asks is a **map** from arm to verdict; an exit code is a **boolean**. Collapsing the map with `any()` discards exactly the inert arms being hunted, and it fails safe in the wrong direction — the aggregate reads RED whenever a *single* arm is RED, so **the more arms a node carries, the more inert ones it can hide**. A representative arm is not a sample, it is a mask. And the four hidden arms were **not random**: they were precisely the four where `raw > ceiling` was false — one systematic calibration error, uniformly spread over the arms it touched, rendered as "no finding".
    **Now mechanically enforced, so this candidate is about the RULE, not the tooling gap:** `tools/mutation_harness.py` (landed `fd9124a`) reports per resolved node id and never parses the summary line. It also enforces §3's items 2–4 (single-match anchor with a CRLF-mismatch hint; reachability probed **only where an arm stayed green**, since a RED arm already proves reachability; a green arm with no position-valid probe is an **abort**, not a pass), mutates a throwaway `git worktree` so it cannot contaminate a concurrent gate run, and is **positive-controlled** — a substitution after a `return` is correctly reported inert **and** never-executed. Items 2–4 were already registered in this file as a Lane B candidate; **that entry can now cite an implementation.**
    **Two further candidates fell out of using it, both found in tests written in the same session** (registered here because they are verification-apparatus rules, not app code): **(a) a discriminating arm must vary exactly ONE term of the derivation it probes** — a first-draft `TC-612` probed the closed label set with a 21-character label against an 18-character maximum, so it moved the *row-width* term rather than the *cardinality* term and passed with the cardinality factor gutted to the literal `1`; **(b) assert the RATE separately from the TOTAL** — `TC-555`'s measured floor was inert on 4 of 5 arms against a 2× slope error, because at small `V` the constant terms dominate and swallow it. A total bound cannot police a rate.
    **Control candidate, NOT encoded** (needs its own AskUserQuestion per `feedback_devflow_control_encode_approval`). Same family as C-40; this is the **reporting granularity** clause C-40 lacks.
    **▸ AMENDED 2026-08-30 (routed from batch-88 + batch-89) — this candidate has earned TWO MORE PROPERTIES and a scope correction, and it should be encoded ONCE with all of them rather than split.** ⚠ **Scope correction first:** `tools/mutation_harness.py` is a **project** artifact, and **the flow's own `--selftest` mutation batteries do not use it** — measured 2026-08-30, `find ~/.claude -name "mutate_*"` (outside `jobs/`) returns **nothing**, so every flow-side battery is a throwaway written per increment and thrown away. **The properties, each paid for in the last fortnight:** **(i)** the verdict must come from the **FINAL** verdict line and never from a substring anywhere in the output — a harness searching for `SELFTEST PASSED` matched a string **printed mid-run from a child process** by rev49's own arm and scored **all twelve applicable mutants SURVIVED at exit=1**, a false-survivor sweep (repaired in rev51's throwaway, which is not on disk); **(ii)** a mutant must be asserted to **still RUN**, not merely to be non-no-op — a crashing mutant prints no FAIL and **reads exactly like a survivor** (`R-88-10`: **0** arm lines and no FAIL scored as SURVIVED, against **299** when rebuilt, with the shell's `$?` capturing the pipeline so the crash also reported exit 0); and **(iii)** a baseline that is already RED must **ABORT rather than score** — it happened **twice** in batch-89 (Increments 3 and 6) and both times the kills were discarded rather than reported, *and lowering the bar to accommodate it would have been the vacuous check one level out.* **The natural home is the flow's canon table under `docs/tools/`**, so the harness is hashed by `V7` and versioned with the flow — see the batch-88/89 routed section at the top of this file.

  - **▸ (P2, NEW) a harness that guarantees a byte-exact restore must do its I/O in BYTES.** `tools/mutation_harness.py`'s own first restore **failed its own SHA-256 check**: `read_text`/`write_text` apply newline translation on Windows, so round-tripping an LF file rewrote it as CRLF — content-equal, **not** byte-equal. **The check it carries for the code under test is what caught the defect in itself**, which is the useful part: the guarantee was real enough to fire on its author. Companion to `reference_evidence_bytes_need_gitattributes_text` (*assert the stored blob, not the file you handed git*) — same root cause, different door. Fixed in `fd9124a`; registered so the next harness starts in bytes.

  - **▸ (P2, NEW — found by the batch-76 merge gate, round 1 BLOCK) `G4` cannot see NODE NAMES, so `REQUIREMENTS.md` can name verifiers that do not exist and omit verifiers that do — while the registry guard passes 13/13.** `tests/test_id_registry.py:251` checks that an **id token** cited in `REQUIREMENTS.md` (`TC-611`) resolves to a `LIVE` entry. It never looks at the **node name** (`test_tc611_...`) that sits beside it in the `- Validation:` enumeration, and it has no notion of *under*-citation. Both failure directions were live simultaneously in `fd9124a`: `REQUIREMENTS.md:5614`/`:5625` named `test_tc552_..._its_own_limit` and `test_tc555_allowance_is_recomputed_independently_and_discriminates` — **two nodes that the same commit had renamed** — while `TC-611`/`TC-612`, the two ids minted specifically to close H-2 and H-3, appeared **nowhere** in the enumeration. `AT-TC-REGISTRY.jsonl` was correct throughout and every one of its 44 rows resolved, which is exactly why the guard was silent.
    **Why this is worth encoding rather than fixing once:** the registry is machine-checked and the requirement is not, so **the two drift apart in the one direction nobody watches** — a reader looking for a requirement's verifier reads `REQUIREMENTS.md`, not the JSONL. Executed baseline, re-derived at three refs: **15** cited-but-undefined `test_*` names already exist on `origin/main` (`test_tc028`, `test_ac1`, `test_f17`, …) — `origin/main` 15 -> `fd9124a` 17 -> `1db087c` 15, so the branch broke 2 and repaired the same 2 without reducing the debt. This is not a one-off — it is an accumulating class with no guard, and batch-76 added 2 more before the merge gate caught them. **Cheap mechanical form:** extend the corpus scan to also extract `` `test_[a-z0-9_]+` `` tokens from `REQUIREMENTS.md` and assert each is defined in `tests/`; freeze the 15 legacy misses at their current count the way `G5` freezes the non-conforming id set, so the number can only go down.
    **⚠ This form covers ONE of the two directions, and the round-2 gate proved it by simulation rather than by argument.** Run against the three refs it goes green / **RED** / green — the RED at `fd9124a` being exactly the F-1 pair, so it *would* have caught F-1. It would **NOT** have caught F-2: the scan only walks names that are already cited, so an id that is omitted entirely gives it nothing to test and it stays green. Do not let the eventual encoding claim "both directions" on the strength of this half.
    Companion direction (harder, and it needs SCOPING before it can be encoded): assert every `LIVE` registry id whose origin is the current batch is **cited** in `REQUIREMENTS.md`. Executed: a bare freeze is far too coarse — **581** LIVE ids are not so cited today. Scope it to ids whose requirement section exists, or to ids added in the same PR. **Control candidate, NOT encoded** (needs its own AskUserQuestion).
    **Provenance note for whoever picks this up:** the defect was in the closure whose entire subject is traceability between an id, its node and its charter. That is the third time in batch-76 that an item's own thesis reproduced inside its fix, and it is the argument for a mechanical check over a careful reviewer.
