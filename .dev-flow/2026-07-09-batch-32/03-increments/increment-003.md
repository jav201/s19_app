# Increment 003 — check/inject/notes/report wiring (LLR-GRP-001.6/.7/.8/.9/.10/.11, LLR-WID-001.3/.5)

1. **What changed** — `CrcRegionResult.output_bytes: int = 4` (+ `to_dict` key, LLR-GRP-001.11); `check_regions` rewritten over `normalized_targets` (one uniform loop; legacy byte-identical via the AT-045d bridge + 32-bit mask no-op; LLR-WID-001.5 compare rule incl. width-8 zero-extend equality); NEW `crc_diagnostics` (per-group aggregate coverage note, truncation warnings, S-7-SCOPED overlap warnings — legacy-only pairs silent); `inject_crcs` derives width from the RESULT field (screens re-inject safe, LLR-GRP-001.10); execute path appends diagnostics AFTER the unchanged legacy summary (`_summarize_check` wording deliberately kept "region(s)" — compat over cosmetics, F-5c decision).
2. **Files** — crc.py, model.py, tests/test_crc_operation.py, tests/test_operations.py (4 of <=5).
3. **How to test** — `pytest tests/test_crc_operation.py tests/test_operations.py -q`.
4. **Results** — 5 suites 101 passed. 11 new operation tests (TC-203.1-.11): AT-047a mixed verdicts+order, AT-045c note half + legacy-silent, AT-046b parametrized truncation, AT-047c self-overlap B10, AT-047g cross-overlap distinct wording, S-7 scope pin legacy-silent (notes == exactly the legacy summary), AT-047f pristine-computes (m-5 ordering precondition honored), AT-046a width-8 inject + range extension, AT-046d operation half, **AT-047b C-12 joined node** (mixed config, widths 8+2+legacy-4, path-from-result, fresh S19File re-parse, dual oracle, verify_status), **AT-044a golden compat** (frozen zlib-derived literals, gapped legacy region, zero new notes, serializer keys, verified write). **RED counterfactual**: stash crc.py+model.py -> 11 failed (group wiring absent, specced reason) -> pop -> 26 green. One pre-existing node rewritten-in-place: test_operations.py serializer pin gains "output_bytes": 4 (AT-047d contract — recorded for the AT-044a inspection clause).
5. **Risks** — group compute cost O(spans x mem_map) bounded by the parse ceiling; diagnostics pair scan is O(targets^2) — negligible at the ceiling.
6. **Pending** — Inc-4 (screens "(N LE bytes)" parameterization, DUMMY_CONFIG_TEXT + example group, AT-044e, AT-047e handler AT + hostile-input, AT-047h write-flow AT); combined Inc-2+3 code review dispatched.
7. **Next** — Inc-4.
