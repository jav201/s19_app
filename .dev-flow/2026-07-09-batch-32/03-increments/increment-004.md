# Increment 004 — TUI surface + config guidance + Layer-B ATs (LLR-GRP-001.12/.13) + Inc-2/3 review folds

1. **What changed** — screens.py write row parameterized "({output_bytes} LE bytes)" (legacy still renders "(4 LE bytes)" — the pre-existing literal pin stays green); DUMMY_CONFIG_TEXT + examples/crc_config.example.json gain one demo group (byte-for-byte synced); AT-044e both-forms test (+ the Inc-1 legacy-only sanity test repointed at a legacy-only literal); AT-047e handler-check AT (group row + truncation note on the surface, non-vacuous mismatch flip); AT-047e-hostile (C-17: "[red]x[/red]" payload in output_bytes -> named-field error rendered VERBATIM on the markup=False surface); AT-047h confirm-write flow AT (real Write button + modal, "(2 LE bytes)" + "(4 LE bytes)" rows, on-disk oracle re-read). PLUS the Inc-2/3 code-review folds: F1 golden frozen literal 0x156424B4 (reviewer double-proved against origin/main pre-change engine — the m-4 obligation is now CLOSED with evidence); F2/F3 check_regions + inject_crcs docstrings rewritten for the group/width reality; F4 encode_le32 delegates to encode_le; F5 parser error indices now 1-based (matching the "CRC group N" notes); F6 AT-047f names its m-5 ordering precondition.
2. **Files** — screens.py, crc_config.py, crc.py, examples/crc_config.example.json, tests (surface/config/operation/engine) — review folds ride this increment; net-new code files: 4.
3. **How to test** — `pytest tests/test_tui_crc_surface.py tests/test_crc_config.py -q`.
4. **Results** — 5 suites **105 passed** (49 base -> 105: +57 new, -0 deleted, 1 rewrite-in-place [serializer pin]). RED captured: stash screens.py+crc_config.py -> AT-047h + AT-044e fail (specced reason) -> pop -> green. Ruff clean on every touched file (repo-wide residue pre-exists in untouched files).
5. **Risks** — none new; operations screen has NO snapshot cell, so the dummy-prefill change causes NO snapshot drift (RK-4 avoided entirely).
6. **Pending** — full suite + engine-frozen guard run at Phase-4 entry; REQUIREMENTS.md rows; Phase 4-6.
7. **Next** — Phase 4 validation.
