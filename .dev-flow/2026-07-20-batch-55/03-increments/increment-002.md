# Increment 2 — the inline-axis length summer (LLR-P1b.1–6 + DoS.1 + SUP.1)

**BLUF: APPROVED.** The CURVE/MAP inline-axis length summer is implemented in `a2l.py` (private symbols, no facade), wired as a post-axis-walk pass with the DoS clamp and the full-span-or-None safety contract. C-35 probe reproduces all 5 oracles (25/51/12/None/None, 0 false-None). Independent code review: no HIGH; one MEDIUM (F1 alignment) → operator-approved force-None fix applied. All verify runs green.

1. **What changed** — `a2l.py` (+192): constants (`MAX_A2L_DECODE_BYTES`, `_DERIVABLE_AXIS_KINDS`/`_EXTERNAL_AXIS_KINDS`/`ALL_AXIS_KINDS`); `_inline_axis_counts` (base-10 try/except, external+kind gate); `_record_layout_full_span` (token[2] datatype, taxonomy, **full-span-or-None incl. ALIGNMENT/unmodeled → None**, cap); `_extract_raw_bytes` clamp; post-axis-walk length block. NEW `tests/test_a2l_inline_axis_length.py` (18 tests: TC-133/133b..139, AT-104..111, AT-107b). AT-102 supersession in `test_a2l_multiline_headers.py`. REQUIREMENTS.md §6.5 A1 prose.
2. **Files (4)** — `s19_app/tui/a2l.py`, `tests/test_a2l_inline_axis_length.py` (new), `tests/test_a2l_multiline_headers.py`, `REQUIREMENTS.md`. (Inc-1's 2 guard files + state.json are separate.)
3. **How to test** — `pytest -q tests/test_a2l_inline_axis_length.py`; `pytest -q tests/test_a2l_multiline_headers.py tests/test_a2l_record_layout_length.py tests/test_tui_a2l.py`; the C-27 dual-guard; a C-35 demo probe.
4. **Test results** — new file `18 passed` exit0; A2L suite `54 passed` exit0; C-27 dual-guard `11 passed` exit0; ruff clean. C-35 probe: `CURVE.STD_AXIS=25 · MAP.STD_AXIS.STD_AXIS=51 · FIX_AXIS.PAR_DIST=12 · COM_AXIS=None · CUBOID=None`, ALL MATCH, 12 CURVE/MAP total → 8 derived / 4 grey, 0 false-None.
5. **Independent code review (code-reviewer agent)** — no HIGH; verified crux clean (swap-discrimination live-confirmed, cap boundary, full-span-or-None, clamp byte-identical shape, wiring, non-vacuous tests incl. TC-135 derived census + TC-133 swap oracle). **F1 (MEDIUM):** ALIGNMENT_* 2-token directives were silently skipped → under-report/false-green on real A2Ls → **operator ruling "safe now + alignment-aware follow-up"** → force-None guard + TC-133b applied; re-verified 5 oracles unchanged. **F2 (LOW):** pre-existing size-encoded-deposit bypass, out of scope, noted.
6. **Phase-3 catches (fail-loud):** (a) dev caught my Phase-2 fold error — `'08'→None` was a **C-36 phantom** (base-0-only); corrected to base-10 `'08'→8` (§6.5 A3). (b) F1 alignment (§6.5 A4).
7. **Risks / pending** — snapshot drift on 8 grey→checked A2L rows (canonical-CI regen only, NOT asserted here); PR-B re-freeze (post-merge); **batch-56** alignment-aware sizing (operator-approved follow-up).

**Gate axes:** Coverage (every Inc-2 LLR → a green TC; every gate-blocking AT → one node) · Certainty (C-35 oracles executed; counterfactuals value-discriminating; F1 false-green closed) · Evidence (verify summaries + exit codes cited; code-review report). Met → approve. **Phase 3 COMPLETE** (Inc-1 + Inc-2).
