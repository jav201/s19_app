"""Core domain layer — pure Python, no Textual imports.

Keep parsers, validators, CRC engines etc. here so they're trivially unit-testable
and can be reused by a future GUI / web frontend without dragging in the TUI.
"""
