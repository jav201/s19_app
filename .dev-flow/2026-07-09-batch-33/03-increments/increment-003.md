# Increment 003 — service/UI wiring + funnel scrub + Layer-B ATs (LLR-051.4-.8)

1. **What changed** — `change_service.py`: blocked runs return the LOUD message `Checks: not run — {reason} ({counts})` with ok=False (LLR-051.4); `check_rows` suffixes every uncheckable row with its reason (LLR-051.5). `app.py`: the four `#log_line_*` labels constructed `markup=False` (LLR-051.8 funnel scrub, placed AFTER the 50-char cap; closes the five pre-existing sibling exposures). `screens_directionb.py`: `#patch_checks_status` constructed `markup=False` (LLR-051.6 — it renders the untruncated file-derived reason).
2. **Files** — change_service.py, app.py, screens_directionb.py, tests/test_tui_patch_editor_v2.py, tests/test_change_service.py (5 of <=5; AT-051f + TC-051.5 deliberately deferred to Inc-4 to hold the cap).
3. **How to test** — `pytest tests/test_tui_patch_editor_v2.py -k "at050 or at051" tests/test_change_service.py -q`.
4. **Results** — 5 new ATs green through the REAL surfaces: AT-050a-pilot (real Run-checks button, mixed rows, pair reasons visible — US-050 Layer B), AT-051a (containment reasons in rows), AT-051b (full loud reason on the untruncated status label; `Checks: not run` prefix within the <=50-char log lines; ok=False observed at the ChangeActionResult return per m-3), AT-051e (hostile `zz[red]boom[/red]` kind sized to BISECT at the log cap: literal on status, no MarkupError on any log label render, rows carry only the short pointer by construction), AT-051c (composed path: pasted CHG-VALUE-MODE-UNKNOWN envelope + editor-added BYTES entries per m-1 -> doc-fault block naming the code). 116 green across the 5 affected suites. ONE censused supersession: the :406 pinned literal rewritten-in-place to the `Checks: not run` prefix (its full-reason assertion moved to AT-051b, P1 design). **RED captured**: stash the 3 source files -> 5 ATs fail (specced reason) -> pop -> green.
5. **Risks** — none new; the one ruff F401 hit is pre-existing repo debt (P-2).
6. **Pending** — Inc-4: help affordance (AT-052a/b), AT-051f, TC-051.5 (engine+report halves), TC-051.4 hostile-encoding sibling, REQUIREMENTS.md rows; combined Inc-2/3 code review DISPATCHED.
7. **Next** — Inc-4.
